# Series C Implementation Plan

> **For agentic workers:** Use subagent-driven-development or execute task-by-task. Checkboxes track progress.

**Goal:** Establish Series C calibration baseline under R-INTERLOCK (QUIET∧ATTEST + D4), including `scenario_5_patient_cycle`, with attribution report fields. No B→C FP/P/R claim deltas.

**Architecture:** Add S5 inject (ladder split across attestation quiet windows) → include `eval_scenario_5` in builder partitions → seed-42 day-window sweep → write `docs/calibration_series_c_metrics.json` + governance/residual updates. Detection YAML stays v2.2 @ thr=45 unless a separate evidenced revision is approved.

**Tech Stack:** `batch/synthetic/generator.py`, `batch/profile_builder/builder.py`, `scratch/run_series_c_sweep.py`, pytest, sqlite eval DB.

## Global Constraints

- Generator seed **42**; config **v2.2**; `anomaly_threshold` **45.0**
- No silent weight/threshold YAML edits
- B→C cross-series FP/P/R comparisons **prohibited**
- Headline recall requires attribution decomposition
- `eval_scenario_5` feeds builder (Design 1 §0.2)
- Not CALIBRATED unless evidence earns it

---

## Task SC0 — scenario_5 inject

**Files:**
- Create: `tests/fixtures/boil_the_frog/s5_patient_cycle.yaml`
- Modify: `batch/synthetic/generator.py` (`inject_scenario_5_patient_cycle`)
- Modify: `batch/profile_builder/builder.py` (`builder_partitions` += `eval_scenario_5`)
- Modify: `docs/superpowers/specs/2026-07-18-boil-the-frog-invariants-design.md` (table: planned → live)
- Create/Modify: `tests/test_generator.py` (S5 partition + shape asserts)

**Schedule (declared):**
- Reuse S2 ladder rungs; split `[0,1] | quiet≥4d in-family | [2,3] | quiet≥4d | [4,5,6]`
- Quiet events: baseline_family only, `is_malicious=False`, partition `eval_scenario_5`
- Attack events: ladder processes, `scenario_5_patient_cycle`, partition `eval_scenario_5`
- Victim: Engineer distinct from S2 victim when both injects run

- [ ] Write failing generator tests for S5 partition / label / quiet gaps
- [ ] Add YAML fixture + inject method
- [ ] Add `eval_scenario_5` to builder partitions
- [ ] Update Design 1 scenario table
- [ ] Pytest green for scoped tests

## Task SC1 — Sweep harness + report fields

**Files:**
- Create: `scratch/run_series_c_sweep.py` (extend window to fit S5; wire S5 inject)
- Modify: metrics aggregation to include §4.7 + Design 2 attribution fields where computable

- [ ] Sweep script generates S1–S5 + tooling mix
- [ ] Metrics JSON includes blocked-entity-days / promotion coverage / auto_resolved counts if queryable
- [ ] Attribution fields for S2/S5 when TPs exist

## Task SC2 — Execute Series C sweep

- [ ] Run seed-42 sweep → local DB + `scratch/series_c_metrics.json`
- [ ] Write `docs/calibration_series_c_metrics.json` (new baseline; do not overwrite Series B)
- [ ] Optionally refresh `calibration_final_metrics.json` with Series C pointer note (do not silently replace Series A narrative)

## Task SC3 — Governance + residual-risk

- [ ] `docs/scoring-config-governance-series-c.md`
- [ ] Update `docs/residual-risk-drift-hypotheses.md` §2.7/§2.1/H11 with Series C evidence
- [ ] Update OPS + memory-bank; leave YAML untouched unless approved revision

## Task SC-EXIT

- [ ] `pytest --ignore=tests/live` + `ruff check .`
- [ ] Skeptic ACCEPT / ACCEPT-WITH-GAPS
- [ ] `.workflow/2026-07-19-series-c/` state drained
