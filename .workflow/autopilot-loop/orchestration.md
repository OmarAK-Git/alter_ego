# Autopilot loop

Queue SoT: `.workflow/autopilot-queue.json`  
Active run: `.workflow/2026-09-18-eval-kernel-sprint1/`  
Memory-bank mirrors follow that run's `state.json`.

Drain protocol is `.cursor/commands/gsd-autopilot-loop.md`.
