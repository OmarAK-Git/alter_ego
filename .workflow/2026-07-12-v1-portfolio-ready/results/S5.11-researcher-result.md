# S5.11 Researcher Result — Cohort artifacts + advanced prior-update gates

**Decision: B — defer (docs-only Path B)**

Defer independent versioned cohort artifacts (from S2.5) and advanced §7.3 prior-update gates. §13.1:664 already lists advanced cohort poisoning gates beyond basic shrinkage as deferrable. v1 ships profile-embedded `cohort_data`, blocked-entity exclusion, and scoring-time novelty suppression — not prior-update rejection or `min_clean_observation_count`.

Do **not** stub a hollow cohort_builder or wire `min_clean_observation_count` without rejection semantics.

## Implementer ACs (Path B)

1. SPEC §7.3 ▲ v1 deferral (S5.11 / Path B) banner; root SPEC synced.
2. §3.2 / Scenario 3 / §13.1 cross-refs honest.
3. SPEC_V3 aligned (novelty ships; min_clean + independence deferred).
4. progress.md S5.11 done Path B; OPS one-liner; optional YAML comment.
5. No new Python cohort infrastructure; rg confirms no production `min_clean_observation_count` reader.

Write scope: `docs/`, `SPEC.md`, `memory-bank/progress.md`, `OPS.md`, optional `config/scoring_config.yaml` comment only.
