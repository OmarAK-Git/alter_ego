# S2.8 skeptic-verifier result (re-verify)

**Packet:** S2.8 — scoring-config inventory honesty after S1/S2  
**Date:** 2026-07-13  
**Pass:** re-verify after root `SPEC.md` sync  
**Verdict:** **SURVIVES**

## Claim under test

After implementer root↔docs SPEC sync: root and docs SPEC are identical (or both honest on `decay_lambdas`); inventory **28 wired / 0 wire / 7 defer / 0 delete**; YAML clean of orphan `decay_lambdas`; no fake calibrated language for stripped knobs in the dual SPEC mirrors.

## Evidence gathered (independent)

### Check 1 — prior decisive fail cleared (dual SPEC)

| Probe | Result |
|---|---|
| Byte compare `SPEC.md` vs `docs/SPEC.md` | **identical** (`len=58093`, sha256 prefix `446e3c0cd918ca7f`) |
| `decay_lambdas.staleness` / `.drift` as live calibrated values | **0 matches** in either SPEC |
| §6.8 language | Both: `drift_half_life_days: 7 — replaces legacy decay_lambdas.drift (removed S2.8; never read)` + explicit deferred/unwired list (`SPEC.md:279-288` / `docs/SPEC.md` same) |

**Pass.** Prior refute (root still listing calibrated lambdas) is gone.

### Check 2 — YAML clean; no production readers

| Probe | Result |
|---|---|
| `config/scoring_config.yaml` | No `decay_lambdas` block; ends at `drift_weights` (35 leaf tunables) |
| `rg decay_lambda` in `*.py` / `*.yaml` / `*.yml` | **0 matches** |
| `total_volume_delta` honesty | YAML comment `# Deferred (S2.6)` present |

**Pass.**

### Check 3 — inventory 28 / 0 / 7 / 0

Independent recount of status cells in `memory-bank/progress.md` §Scoring config knob inventory (excluding Removed table):

| Label | Recount | Claim |
|---|---|---|
| wired | **28** (14 top + 2 cohort + 7 features + 5 drift_weights) | 28 |
| wire | **0** | 0 |
| defer | **7** (`max_calendar_adjustment`, `max_profile_build_block_days`, `age_jitter_hours`, `min_clean_observation_count`, `gap_correlation_window`, `investigation_context_window`, `total_volume_delta`) | 7 |
| delete | **0** (stripped listed under Removed, not live delete) | 0 |

OPS one-liner matches: `OPS.md:38` — **28 wired / 0 wire / 7 defer / 0 delete**.

Reader spot-checks:
- `confidence_k` / `containment_threshold`: `worker/scorer.py:428`, `:448-450`
- geo rarity + drift KL: `worker/scorer.py:334-343`; `batch/profile_builder/builder.py:157-242`
- `score_vol = 0.0` + `volume_delta_deferred`: `worker/scorer.py:378-380` (defer honest)

**Pass.**

### Check 4 — AC5 calibrated overclaims (in packet scope)

| Surface | Status |
|---|---|
| Root `SPEC.md` §6.8 | Honest (legacy note only) |
| `docs/SPEC.md` §6.8 | Identical to root |
| Live YAML | Orphans stripped |

**Pass** for packet-scoped calibrated surfaces.

Residual (non-decisive): `docs/phase2-audit-result.md:44` still says `decay_lambdas` “Implements staleness and drift decay”. Out of S2.8 write scope; historical audit artifact; does not restore live calibrated operating-point language in SPEC/YAML. Track as follow-up scrub if desired.

## Acceptance criteria vs packet

| AC | Status |
|---|---|
| 1. progress inventory refresh post-S1/S2 | Met |
| 2. `decay_lambdas` removed or deferred-orphan with honesty | Met (stripped) |
| 3. Remaining wire items wired or deferred with owner | Met |
| 4. OPS one-liner current | Met |
| 5. No fake calibrated language for unwired knobs | Met (root+docs SPEC + YAML); phase2 residual noted only |

## Strongest reason

**SURVIVES** because root `SPEC.md` is now byte-identical to `docs/SPEC.md`, both treat `decay_lambdas` as removed legacy (not live calibrated), YAML has zero `decay_lambdas`, and the progress/OPS inventory independently recounts to **28/0/7/0**.
