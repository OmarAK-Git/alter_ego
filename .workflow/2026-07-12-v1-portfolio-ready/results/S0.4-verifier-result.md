# S0.4 Verifier Result — Scoring config knob inventory

**Verdict: SURVIVES**

Verified 2026-07-12 by skeptic-verifier. Implementer report treated as unevidenced; all evidence re-gathered independently via Read + Grep over `worker/`, `batch/`, `core/`.

## Claim (restated)

Every knob in `config/scoring_config.yaml` (v2.2) is inventoried in `memory-bank/progress.md` as wired/wire/defer/delete; YAML numeric values unchanged; classifications match code usage.

## YAML numeric values — CONFIRMED unchanged

`config/scoring_config.yaml`:
- `anomaly_threshold: 45.0` (line 2) ✓
- `drift_threshold: 5.0` (line 14) ✓
- `containment_threshold: 85.0` (line 19) ✓
- `features.drift_alert.weight: 100.0` (line 55) ✓

## Coverage — CONFIRMED complete

Cross-checked every YAML key (37 knobs: 16 top-level + decay_lambdas×2 + cohort_gating×3 + gap_windows×2 + features×8 + drift_weights×5) against the inventory tables in progress.md §"Scoring config knob inventory (2026-07-12)". Every YAML knob appears. `confidence_k` correctly listed as "missing from YAML (code reads default)". Summary counts (25 wired / 5 wire / 5 defer / 2 delete) recomputed and match the tables exactly.

## Classification spot-checks

### All wire/delete/defer (claim = NOT read in production path) — CONFIRMED

| Knob | Claim | Evidence |
|---|---|---|
| `containment_threshold` | wire, not read | No match in any `.py` |
| `max_calendar_adjustment` | defer, not read | No match |
| `max_profile_build_block_days` | defer, not read | No match |
| `age_jitter_hours` | defer, not read | No match |
| `gap_correlation_window` | defer, not read | No match |
| `investigation_context_window` | defer, not read | No match |
| `decay_lambdas.staleness` | delete, not read | No match |
| `decay_lambdas.drift` | delete, not read | No match |
| `min_clean_observation_count` | wire, only scratch | Only `scratch/test_cohort_gate.py:86` |
| `total_volume_delta` | wire, always 0 | `worker/scorer.py:345 score_vol = 0.0`, `:347` flag `volume_delta_suppressed_no_current_count` |
| `drift_weights.geolocation` | wire, not in builder deltas | `builder.py:231` deltas dict = {login_hour, endpoint_set, process_name, embedding} — no geolocation; `:241` sums only over avg_deltas keys |
| `confidence_k` | wire, code reads default, absent YAML | `worker/scorer.py:395 config.get("confidence_k", 10.0)`; not in YAML |

### Wired spot-checks (>5) — CONFIRMED

- `anomaly_threshold` → `scorer.py:398`
- `drift_threshold` → `scorer.py:358`, `builder.py:64,343` (dual semantics confirmed)
- `contribution_scale_max` → `scorer.py:227`
- `laplace_alpha` → `scorer.py:226`, `builder.py:67`
- `cohort_gate_window_days` → `scorer.py:241`
- `min_cohort_size` → `scorer.py:240`; `max_changed_fraction` → `scorer.py:239`
- `max_profile_staleness_days` → `scorer.py:244`
- `drift_comparison_history_count` → `builder.py:65`; `drift_half_life_days` → `builder.py:66`
- `confidence_floor` → `scorer.py:399`
- `max_replay_window_days` → `builder.py:80`; `recent_drift_window_days` → `builder.py:81`
- Feature weights all read via `features_config.get(...).get("weight")`: login_hour_rarity/geolocation_rarity/endpoint_set_rarity/process_name_rarity `scorer.py:309-312,291`, command_line_embedding_similarity `:325`, service_account_execution_frequency_deviation `:337`, drift_alert `:359`
- `geolocation_rarity`: weight IS read (`scorer.py:310`) but builder histogram queries (`builder.py:153-165`) build only hour_of_day/endpoint_id/process_name — no geo histograms. Matches nuanced "wired; geo histograms not built" claim.
- drift_weights `login_hour`/`endpoint_set`/`process_name`/`embedding` consumed in `builder.py:233-238,241`

## Attempts to refute (all failed)

1. Searched for any production reader of the 12 wire/defer/delete knobs → none found outside `scratch/`, `tests/`. Classifications hold.
2. Checked whether `geolocation_rarity`/`drift_weights.geolocation` were over- or under-classified → inventory's split (weight wired, histogram/KL missing) is precise, not glossed.
3. Checked YAML values against code defaults for tampering → `builder.py:64` uses fallback default `45.0` for drift_threshold, but the loaded YAML value is `5.0`; this is a pre-existing code default fallback, not a YAML change, and does not affect the claim.

## Strongest surviving reason

Every one of the 12 non-"wired" classifications was independently reproduced (grep shows no production reader), all four load-bearing YAML numbers match, and full YAML→inventory coverage plus recomputed counts (25/5/5/2) hold. No discrepancy found.
