# S3.4 verifier result

verification_model: cursor-grok-4.5-high

## Verdict

**survives**

## Claim restated

S3.4 done: operating point thr=45 documented with fresh S3.1/S3.2 metrics; S2 catch + residual modes (high FP, S3 FN, thr=55 diagnostic only / not applied); `memory-bank/progress.md` Saved eval point updated away from stale S2 R=0.0 / global R≈0.42; no CALIBRATED success claim; no YAML threshold change.

## Strongest reason

`docs/phase2-s3-operating-point.md` and `memory-bank/progress.md` Saved eval point both reproduce `docs/calibration_final_metrics.json` thr=45 counts/recalls (including S2 recall **1.0**, FP **3448**, S3 FN **15**), explicitly mark thr=55 as diagnostic-only (matches best-F1 row in `docs/calibration_pr_curve.json`), disclaim CALIBRATED, and `config/scoring_config.yaml` still has `anomaly_threshold: 45.0`.

## Evidence gathered

### 1. Operating-point numbers match calibration_final_metrics.json

Compared `docs/phase2-s3-operating-point.md` vs `docs/calibration_final_metrics.json` → `calibrated` (source `alter_ego_calibrate_s31.db`, sweep S3.1, seed 42, config 2.2, `anomaly_threshold: 45.0`):

| Field | phase2 note | JSON |
|---|---|---|
| precision | 0.0191 | 0.019061… |
| recall | 0.8171 | 0.817073… |
| f1 | 0.0373 | 0.037253… |
| tp / fp / fn / tn | 67 / 3448 / 15 / 8969 | identical |
| drift_alerts | 46 | 46 |
| S1 / S2 / S4 recall | 1.0 | 1.0 |
| S2 tp/fn | 35 / 0 | 35 / 0 |
| S3 recall / tp / fn | 0.667 / 30 / 15 | 0.666… / 30 / 15 |

Rounding only; no material mismatch. PR-curve thr=45 row matches the same global P/R/F1/tp/fp/fn exactly.

### 2. progress.md Saved eval point no longer stale

`memory-bank/progress.md` § Saved eval point (lines 28–35):

- Current: thr=45 · P **0.019** · R **0.817** · F1 **0.037** · TP **67** · FP **3448** · FN **15**
- Per-scenario: S2 **1.0** (35/35), S3 **0.667** (15 FN)
- Grep for `≈0.42` / `R≈0.42` / stale “S2 recall 0.0 as current” in Saved eval point: **none**
- Phase 2 status row: Partial (Phase 2A); S1/S2/S4 recall 1.0; **not CALIBRATED**
- S3.4 task row: **done** with operating-point path + residuals

### 3. No CALIBRATED success claim; thr=55 diagnostic only

`docs/phase2-s3-operating-point.md`:

- Status: “**not CALIBRATED.**”
- Residual #3: thr=55 best-F1 documented “for diagnosis only. **Not applied** to YAML”
- “What this is not” → Not a CALIBRATED or audit-grade claim

`progress.md`: “**Not CALIBRATED.**”; thr=55 “diagnostic only, **not applied**”

thr=55 numbers vs `docs/calibration_pr_curve.json` threshold 55:

| Field | note | PR curve |
|---|---|---|
| P | 0.0405 | 0.040540… |
| R | 0.585 | 0.585365… |
| F1 | 0.076 | 0.075829… |
| FP / FN | 1136 / 34 | 1136 / 34 |

Python `max(pr_curve, key=f1)` → threshold **55** (best-F1 claim holds).

JSON note on metrics file also: “not a CALIBRATED claim.” (key name `calibrated` is structural, not a success assertion.)

### 4. YAML anomaly_threshold still 45

`config/scoring_config.yaml` line 2: `anomaly_threshold: 45.0` (read directly). No evidence in S3.4 deliverables of a threshold retune; implementer result states YAML out of scope / unchanged.

### 5. Implementer result consistency

`.workflow/.../results/S3.4-implementer-result.md`: documents same thr=45 metrics, S2 catch, residuals (3448 FP, S3 FN 15, thr=55 not applied), no CALIBRATED, no YAML/calibration JSON edits.

## Non-blocking observations (out of acceptance)

- Sprint table still shows S3.1 `ready` / S3.2 `todo` while S3.4 is `done` — process-table hygiene, not an acceptance failure for Saved eval point / residual note.
- S3.5/S3.6 gaps ignored per packet.

## Checks summary

| # | Check | Result |
|---|---|---|
| 1 | phase2 note numbers match calibration_final_metrics.json | pass |
| 2 | progress.md Saved eval point not stale R≈0.42 / S2 R=0.0 | pass |
| 3 | No CALIBRATED; thr=55 diagnostic only | pass |
| 4 | YAML anomaly_threshold still 45 | pass |
