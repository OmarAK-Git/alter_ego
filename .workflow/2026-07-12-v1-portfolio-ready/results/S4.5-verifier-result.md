verification_model: cursor-grok-4.5-high

# S4.5 verifier result

## Claim

S4.5 first-class `replay_run_id` on decisions/audit path is complete.

## Verdict

**survives**

## Strongest reason

Independent code reads plus fresh focused pytest (6 passed) and an unmocked `run_replay` smoke show `replay_run_id` is a real schema/ORM/API field and is written on emitted replay decisions — not only a `replay:{id}` flag.

## Acceptance criteria

| # | Criterion | Finding |
|---|---|---|
| 1 | Pydantic `DecisionRecord` optional/nullable `replay_run_id` | **pass** — `core/schemas/decisions.py:29` `Optional[str] = None` |
| 2 | ORM nullable `replay_run_id` | **pass** — `core/models.py:77` + migration `alembic/versions/f5a6b7c8d9e0_add_decisions_replay_run_id.py` (down_revision `e4f5a6b7c8d9` valid) |
| 3 | `run_replay` sets field on new decisions | **pass** — `batch/replay_runner.py:134,154`; test asserts column + flag; unmocked smoke emitted row with `replay_run_id='replay_unmocked'` |
| 4 | Originals null/absent | **pass** — `record_decision` omits field → DB null; tests assert null and immutability after replay |
| 5 | API detail includes field when present | **pass** — `web/api.py:109`; triage/suppressed also surface it (`:82`, `:199`); detail tests cover present + null |
| 6 | Focused tests + progress.md | **pass** — 6/6 pytest; `memory-bank/progress.md` S4.5 `done` |

## Adversarial probes (did not refute)

- **Mocked `score_event` in unit tests:** true, but unmocked smoke still emitted a decision with first-class `replay_run_id` and `replay:` flag.
- **`worker/recorder.py` does not map `replay_run_id`:** intentional/out of write scope; originals rely on nullable column default — matches AC4 and is covered by `test_original_decision_persists_null_replay_run_id`.
- **API list endpoints lack dedicated `replay_run_id` tests:** AC5 requires detail; detail tests are present; list payloads include the key in source.
- **`memory-bank/tasks.md` still unchecked for S4.5:** not an AC; progress.md is the required status surface.

## Commands run

```text
python -c "... pytest tests/batch/test_replay_runner.py
  tests/web/test_api.py::test_alert_detail_includes_replay_run_id_when_present
  tests/web/test_api.py::test_alert_detail_replay_run_id_null_for_original -v --tb=short ..."
# → 6 passed, EXIT=0 (wrote .pytest_s45_verify.txt)

python -c "... ruff check core/schemas/decisions.py core/models.py
  batch/replay_runner.py web/api.py
  alembic/versions/f5a6b7c8d9e0_add_decisions_replay_run_id.py
  tests/batch/test_replay_runner.py ..."
# → All checks passed!, EXIT=0

python -c "<unmocked run_replay smoke on sqlite memory>"
# → decisions_emitted=1, errors=[], row.replay_run_id='replay_unmocked'
```

## Evidence consulted

- Packet: `.workflow/.../packets/S4.5-verifier.md`, implementer packet/result (not trusted alone)
- Source: `core/schemas/decisions.py`, `core/models.py`, `batch/replay_runner.py`, `web/api.py`, `worker/recorder.py`, alembic `f5a6b7c8d9e0`
- Tests: `tests/batch/test_replay_runner.py`, `tests/web/test_api.py` (two alert-detail cases)
- Status: `memory-bank/progress.md` S4.5 done
