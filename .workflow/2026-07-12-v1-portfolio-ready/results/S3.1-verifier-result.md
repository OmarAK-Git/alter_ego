# S3.1 verifier result

verification_model: cursor-grok-4.5-high

## Verdict

**survives**

## Claim restated

S3.1 done: full four-scenario eval sweep under current config; metrics + partitions evidenced; no scoring_config weight/threshold changes; `docs/calibration_final_metrics.json` unchanged; not CALIBRATED.

## Strongest reason

Independent recomputation from `alter_ego_calibrate_s31.db` exactly reproduces `scratch/s31_metrics.json` / the implementer report (tp=67, fp=3448, fn=15, precision≈0.0191, recall≈0.8171, S2 recall=1.0), while the driver uses packet-preferred `__main__` injects (`inject_scenario_3_coordinated`), operating threshold matches YAML `anomaly_threshold: 45.0`, and the saved calibration JSON still holds the old point (precision 1.0, recall ~0.42, S2 recall 0.0) — not the new FP-heavy metrics.

## Evidence gathered

### 1. Raw metrics JSON vs implementer report

- `scratch/s31_metrics.json` exists and matches the implementer table/JSON byte-for-byte on key fields:
  - precision `0.01906116642958748`, recall `0.8170731707317073`, f1 `0.037253266611064775`
  - tp=67, fp=3448, fn=15, tn=8969, drift_alerts=46
  - S1/S2/S4 recall 1.0; S3 (`scenario_3_subtle`) recall 2/3 (30 tp / 15 fn)
  - partition_check: eval_scenario_{1..4} = 1/35/45/1, `all_partitions_eval_scenario: true`

### 2. Operating threshold vs YAML

- `config/scoring_config.yaml` line 2: `anomaly_threshold: 45.0` (version `"2.2"`)
- Driver hardcodes `ANOMALY_THRESHOLD = 45.0` and passes it to `calculate_metrics` — matches YAML

### 3. `docs/calibration_final_metrics.json` not overwritten

- File still has the **old** saved point:
  - calibrated precision `1.0`, recall `0.4166…`, S2 recall `0.0`, scenario key `scenario_3_coordinated` (tp=3)
- Git status for this path: clean (empty) — not replaced with S3.1 FP-heavy metrics
- Implementer correctly does **not** claim CALIBRATED

### 4. Generator path (coordinated vs subtle)

- `scratch/run_s31_sweep.py` calls `inject_scenario_3_coordinated` (plus S1/S2/S4 + tooling rollout), same schedule as `batch/synthetic/generator.py` `__main__`
- Seed is `42` per packet ( `__main__` uses `1337`; packet AC prefers `seed=42`)
- Label string `scenario_3_subtle` is what `inject_scenario_3_coordinated` itself writes in the generator (not a divergent `scenario_3_subtle` inject path)
- **Judgment:** satisfies packet method / “full four-scenario eval under current config”

### 5. Partition + metrics spot-check (no full re-sweep)

- Ground-truth / events JSONL: 82 malicious; partitions `eval_scenario_1..4` = 1/35/45/1; all `eval_scenario_*`
- Dedicated DB present: `alter_ego_calibrate_s31.db` (~40.8 MB), 13835 events, 12499 decisions
- Recomputed from SQLite `decisions` + `eval_ground_truth` at threshold 45.0:
  - tp/fp/fn = 67/3448/15 — exact match to metrics JSON
  - per-scenario tp/fn identical to report
  - DB partitions identical to partition_check
- Pipeline path is real day-window `batch.eval.runner.run_pipeline` (ingest → resolve → build_profiles → score), not a unit-test smoke

### 6. scoring_config weight/threshold retunes

- Working tree shows `config/scoring_config.yaml` modified (`git status: M`), but diff is **not** S3.1 anomaly/weight retunes:
  - adds `confidence_k: 10.0` + comment
  - removes unused `decay_lambdas` block
  - comment on deferred `total_volume_delta`
- Unchanged in file/diff sense for AC: `anomaly_threshold: 45.0`, `drift_threshold: 5.0`, feature weights (incl. drift weight 100.0)
- Per user check (“should not show S3.1 threshold/weight retunes”): **pass**

## Gaps / caveats (non-refuting)

- Did not re-run the ~249s sweep; verified against artifacts + live DB instead.
- `scoring_config.yaml` is dirty in the worktree from non-weight/threshold edits (likely prior sprint debt) — outside S3.1 retune claim, but worth cleaning before S3.2 promotion.
- Phase gaps (high FP, S3.2+ unfinished, SPEC scrub) ignored per packet scope.

## AC checklist (task scope)

| AC | Result |
|---|---|
| Full four-scenario eval pipeline | pass |
| Global P/R/F1/tp/fp/fn + S1–S4 recall | pass |
| Threshold documented = YAML 45 | pass |
| No weight/threshold retunes | pass (caveat: unrelated dirty yaml) |
| `calibration_final_metrics.json` unchanged | pass |
| Commands + raw metric JSON | pass |
| Not CALIBRATED | pass |
