verification_model: cursor-grok-4.5-high

# S5.12 Verifier Result — README + threat-model honesty polish (re-check after retry)

**Date:** 2026-07-13  
**Packet:** S5.12-verifier (re-check after S5.12-implementer-retry)  
**Claim tested:** README + threat-model aligned to S5 shipped/deferred reality; no false CALIBRATED claims. Prior refute gaps closed.

## Verdict: **survives**

**Strongest reason:** Independently re-checked: SPEC §14 now states debate transcripts are optional provenance and **not** shipped (`docs/SPEC.md:689` / byte-identical root `SPEC.md`), aligning with Phase 4 Deferred (`:655`) and README (`README.md:70`); files still absent (honest). Knob inventory lists `max_profile_build_block_days` as **wired** (`memory-bank/progress.md:210`) matching `batch/profile_builder/builder.py`. Prior ACs (README Partial / not CALIBRATED, Path B quals, portfolio pointers, root SPEC sync, no false CALIBRATED) still hold on fresh evidence.

---

## Method

Treat implementer transcript as unevidenced. Independently:

1. Re-test prior refute drivers (SPEC §14 transcript commitment; `progress.md` knob inventory)
2. Re-validate S5.12 ACs 1–5 against current tree
3. Byte-compare root `SPEC.md` vs `docs/SPEC.md`
4. Confirm portfolio doc paths exist; scan for positive CALIBRATED / “100% recall” / stale “missing 4-container” language

---

## Fresh verification

```text
$ python -c "… docs/SPEC.md vs SPEC.md …"
equal True
docs/root both 67179 bytes, sha256 prefix fde701166af48847

$ Path('docs/architecture-debate-v1.md').exists() → False
$ Path('docs/architecture-debate-v2.md').exists() → False

$ docs/SPEC.md:689
… Full debate transcripts (`docs/architecture-debate-v1.md`,
  `docs/architecture-debate-v2.md`) are optional provenance artifacts
  and are **not** shipped in the v1 portfolio repository (see §13 Deferred).

$ docs/SPEC.md:655 Deferred
- Architecture-debate transcripts under `docs/` (optional)

$ memory-bank/progress.md
210:| `max_profile_build_block_days` | 30 | wired | builder.py … (S5.6) |
(no inventory row with defer / “Not read” for this knob)

$ README
badge: Phase 0–4 Partial | not CALIBRATED
no: missing 4-container / CALIBRATED (Audit / 100% recall
pointers: deployment / pgvector / llm-determinism / counterfactual → present + files exist
llm artifact status: “Not executed” (docs/llm-determinism-check.md:3)

$ CALIBRATED hits (README + SPECs)
only “not CALIBRATED” / “not … CALIBRATED” — no positive audit-grade claim
```

---

## Prior refute gaps

| Gap | Result | Evidence |
|---|---|---|
| SPEC §14 claimed transcripts committed while files missing | **closed** | §14 rewritten to optional / **not** shipped; Phase 4 Deferred + README Deferred agree; files still missing (correct) |
| `progress.md` inventory `max_profile_build_block_days` defer/unread | **closed** | inventory status `wired`; S5.6 row `done`; builder reads knob |

---

## Acceptance checklist (adversarial)

| # | Criterion | Result | Evidence |
|---|---|---|---|
| 1 | README phase table honest on S5 shipped; Partial / not CALIBRATED preserved | **pass** | Badge (`README.md:10`); Phase 0 shipped compose/roles/audit/playbook, Open = empirical LLM run (`:66`); Phase 2 **not CALIBRATED** (`:56–68`); Phase 4 Partial, S5.11/K8s/debate deferred (`:70`) |
| 2 | Threat-model “Addressed via X” matches shipped; Path B deferred qualified | **pass** | §3.2 Path B quals for coordinated compromise + confidence-floor (`docs/SPEC.md:59,64`); S5.3–S5.6 shipped refs present (`:58–63`) |
| 3 | Pointers to deployment / pgvector / llm-determinism / counterfactual | **pass** | README Portfolio docs (`:119–126`); all four files exist |
| 4 | Root SPEC synced if docs/SPEC edited | **pass** | Byte-identical (`equal True`) |
| 5 | No false CALIBRATED / 100% recall claims | **pass** | No positive CALIBRATED; scenario recall 1.0 only where stated; global ~0.817 / not CALIBRATED |

---

## Residuals (not verdict drivers)

1. **§12 design-target body** still says asset maps “are committed” (`docs/SPEC.md:576`) immediately under an explicit v1 deferral banner (`:574`) stating no committed artifacts — same deferred-design prose pattern as other Path B sections; not a §14-class false shipped claim.
2. **“Scheduled” integrity:** §3.2/§9.2 say scheduled hash-chain check; job is callable (`batch/audit_integrity.py`) without compose cron — weak overclaim; S5.4 survived on callable semantics.
3. Soft wording “deferred to Phase 4” for Path B items that Phase 4 itself lists as Deferred — Path B qualifier present.

---

## Conclusion

Prior refute drivers are independently closed, and the original S5.12 honesty claim survives adversarial re-check on fresh evidence. Residual §12 design-body tension remains documentation noise, not a false portfolio CALIBRATED or shipped-transcript claim.
