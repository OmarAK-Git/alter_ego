# S3.2 verifier result

verification_model: cursor-grok-4.5-high

## Verdict

**survives**

## Claim restated

S3.2 done: both `docs/calibration_*.json` refreshed from the S3.1 DB; S2 recall ~1.0 at thr=45; no YAML retune; no CALIBRATED success claim.

## Strongest reason

`docs/calibration_final_metrics.json` `calibrated` block matches `scratch/s31_metrics.json` `metrics` field-for-field at thr=45 (including `scenario_2_slow_roll` recall **1.0**), and the thr=45 row in the 29-point PR curve matches those same global P/R/F1/tp/fp/fn exactly — while YAML `anomaly_threshold` remains **45.0** (thr=55 best-F1 not applied) and the JSON note explicitly disclaims CALIBRATED.

## Evidence gathered

### 1. Final metrics vs S3.1 raw JSON (thr=45)

Compared `docs/calibration_final_metrics.json` → `calibrated` vs `scratch/s31_metrics.json` → `metrics`:

| Field | final_metrics | s31_metrics |
|---|---|---|
| precision | 0.01906116642958748 | identical |
| recall | 0.8170731707317073 | identical |
| f1 | 0.037253266611064775 | identical |
| tp / fp / fn / tn | 67 / 3448 / 15 / 8969 | identical |
| drift_alerts | 46 | identical |
| scenario_2_slow_roll | recall 1.0, tp 35, fn 0 | identical |
| S1 / S4 | recall 1.0 | identical |
| S3 subtle | recall 0.666…, tp 30, fn 15 | identical |

Metadata: `source: alter_ego_calibrate_s31.db`, `sweep: S3.1`, `config_version: 2.2`, `generator_seed: 42`, `anomaly_threshold: 45.0`.

Contrast with S3.1 verifier (then): docs still held the **old** operating point (precision 1.0, S2 recall 0.0). Current docs are the post-S3.1 numbers — refresh happened.

### 2. PR curve multi-point + thr=45 consistency

`docs/calibration_pr_curve.json`:
- 29 points, thresholds 10–150 step 5 (matches `batch/eval/calibrate.py` `generate_pr_curves`)
- Per point fields: `threshold`, `precision`, `recall`, `f1`, `tp`, `fp`, `fn`
- thr=45 row: precision/recall/f1/tp/fp/fn **exact match** to final_metrics `calibrated`
- thr=55 is best F1 on curve (0.0758) but is only recorded in `scratch/s32_summary.json` `best_f1` — not promoted to YAML

### 3. YAML not retuned

`config/scoring_config.yaml` line 2: `anomaly_threshold: 45.0` (version `"2.2"`). Recommended thr=55 **not** applied.

### 4. No CALIBRATED success claim

- `docs/calibration_final_metrics.json` note: *“…not a CALIBRATED claim.”*
- Key name `"calibrated"` is the required schema slot for metrics-at-YAML-threshold (per S3.2 implementer packet), not a status badge
- No `Status: CALIBRATED` (or equivalent) in the new JSON artifacts
- `docs/calibration_pr_curve.json` has no CALIBRATED string

### 5. Helper points at S3.1 DB

`scratch/s32_refresh_calibration_docs.py`:
- `DB_PATH = REPO_ROOT / "alter_ego_calibrate_s31.db"`
- `ANOMALY_THRESHOLD = 45.0`
- Calls `generate_pr_curves(db, …)` + `calculate_metrics(db, threshold=45.0)`
- Writes both docs paths; exits if DB missing

`scratch/s32_summary.json` mirrors the same thr=45 metrics and reports `curve_points: 29`, `best_f1.threshold: 55`.

## Gaps / caveats (non-refuting)

- Could not re-query live SQLite this session (shell backend unavailable; `*.db` gitignored so Glob hid the file). Prescribed checks compare docs ↔ `s31_metrics.json` / PR curve / YAML / helper — all pass.
- Prior S3.1 verifier already independently recomputed the same numbers from `alter_ego_calibrate_s31.db`; S3.2 docs now match that verified point.

## AC checklist (task scope)

| AC | Result |
|---|---|
| Both docs JSON refreshed from S3.1 evidence | pass |
| S2 recall ~1.0 at thr=45 | pass |
| PR curve real multi-threshold sweep | pass |
| No scoring_config weight/threshold edits | pass (anomaly_threshold still 45) |
| No CALIBRATED claim | pass |

## Ignored (per packet)

S3.3–S3.6 / phase exit / high-FP governance trade-offs.
