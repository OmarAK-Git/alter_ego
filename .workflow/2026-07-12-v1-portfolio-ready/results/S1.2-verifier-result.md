# S1.2 Skeptic-Verifier Result

**Verdict: SURVIVES**

## Claim (restated precisely)

1. `batch/profile_builder/builder.py` builds geolocation histograms into profiles.
2. Geolocation is included in the drift KL computation.
3. The scorer can consume the geolocation histograms.
4. Tests pass.

Implementer treated as unevidenced; all evidence below gathered independently.

## Evidence gathered

### Test run (required_permissions: all)

```
python -m pytest tests/batch/test_profile_builder_geo.py tests/batch/profile_builder/builder.py -v --tb=short
```

Result: **3 passed, 4 warnings in 0.91s** (exit 0). Ran outside sandbox.
- `test_profile_builder_populates_geolocation_histogram` PASSED
- `test_drift_includes_geolocation_when_distributions_differ` PASSED
- `test_profile_builder_aggregates_correctly` PASSED
- (Only warning: pre-existing `datetime.utcnow()` deprecation at `builder.py:181` — unrelated.)

### (1) Geolocation histograms are built — CONFIRMED
- `builder.py:131` — `geolocation` extracted from `event_data` into the per-event JSONL.
- `builder.py:154,158,162` — `histogram(geolocation)` in parent/primary/global cohort queries → parsed into `"geolocations"` (lines 156,160,163).
- `builder.py:166,171` — `histogram(geolocation)` in per-entity historical and recent aggregation.
- `builder.py:258,309` — `"geolocations"` persisted into `features` on the profile artifact.

### (2) Geolocation in drift KL — CONFIRMED
- `builder.py:234` — `deltas` dict includes a `"geolocation"` key.
- `builder.py:237` — `deltas["geolocation"].append(compute_distribution_kl(rec_geolocations, prev.features.get("geolocations", {}), alpha=laplace_alpha))` — recent-vs-previous-profile KL.
- `builder.py:245` — `raw_drift = sum(avg_deltas[k] * drift_weights.get(k, 1.0) ...)` iterates all deltas including geolocation.

### (3) YAML weight key is used — CONFIRMED
- `config/scoring_config.yaml:61-63` defines `drift_weights.geolocation: 5.0`.
- `builder.py:61` loads `config.get("drift_weights", ...)` (YAML present, so real values used, not the inline default).
- `builder.py:245` applies `drift_weights.get("geolocation")` = 5.0. Key name matches.

### (4) Scorer consumes geolocations — CONFIRMED
- `worker/scorer.py:301` — `_get_cohort_histogram("geolocations", profile)`.
- `_get_cohort_histogram` (`scorer.py:184-194`) reads `features.get("geolocations")` and `cohort_data[...]["geolocations"]` — key names match what builder writes.
- `scorer.py:310,374` — geolocation rarity contribution emitted (`geolocation_rarity`).

## Adversarial checks (attempts to refute)

- **Does the drift test actually exercise geolocation, or pass vacuously?**
  In `test_drift_includes_geolocation_when_distributions_differ`, every event shares identical `endpoint_id` (`endpoint_a`), `process_name` (`bash`), hour (9:00), and empty `command_line`. Only `geolocation` differs between the recent window (`RU-Moscow`) and the baseline profile (`US-East`). Therefore `normalized_drift > 0` can only originate from the geolocation KL term. The assertion isolates the feature under test — not gamed.
- **Cohort normalization could zero out drift.** Checked: 4 `eng`-role entities; 3 stable have raw_drift≈0, drift_id has large raw_drift; `MIN_NORM_COHORT=3` is met so `cohort_median = median([0,0,0,large]) = 0`, leaving `normalized_drift = large > 0`. Assertion is robust.
- **Dead ternary noticed** at `test_profile_builder_geo.py:80` (`"US-East" if ... else "US-East"` — both branches identical). Confusing but harmless; it makes the baseline uniformly `US-East`, which is exactly what the test needs. Not a correctness/gaming issue.
- **Inline default drift_weights shadowing YAML?** No — YAML provides `drift_weights`, so the inline dict at `builder.py:62` is never used.

## Strongest reason it survives

The geolocation drift test is genuinely discriminating: all non-geo features are held constant, so its `normalized_drift > 0` assertion can only be satisfied by the geolocation KL contribution, and the YAML `drift_weights.geolocation` key is provably wired into `raw_drift`. Builder-written key `"geolocations"` matches the scorer's lookup key. All three tests pass on a clean, unsandboxed run.
