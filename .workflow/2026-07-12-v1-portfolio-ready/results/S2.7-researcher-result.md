# S2.7 Researcher Result — Lifecycle fork

**Recommendation: B — Defer with explicit SPEC downgrade**

## Evidence summary

- SPEC §5.4 defines five profile lifecycle states; Phase 1 gate does **not** list them; Phase 3 triage mentions lifecycle badges.
- Codebase: zero `lifecycle_state` / `dormant` / `reactivated_dormant` / `onboarding` / `role_transition` in core/worker/batch/web Python.
- UI `new`/`acknowledged` badges are **alert workflow**, not profile lifecycle.
- Partial substitutes only: cohort fallback / `cohort_unsupported`, `staleness_halt` (halt ≠ dormant penalty scoring).

## Why not A

Minimal honest ship needs schema + builder state machine + dormant retention + reactivation tracking + scorer penalties + tests — medium packet. Stubbing `lifecycle_state: "active"` everywhere would be a portfolio lie.

## Implementer brief (Path B)

Edit `docs/SPEC.md` (§5.2/§5.4/§6.2 flags/§11.1) with v1 defer note; update `memory-bank/progress.md` S2.7 done; optional one-liner in SPEC_V3. Do not stub lifecycle fields or fake flags.
