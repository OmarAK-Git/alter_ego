implementation_model: composer-2.5

# S5.12 Implementer Result — Threat-model + README honesty polish

**Date:** 2026-07-13  
**Packet:** S5.12-implementer (retry 1: S5.12-implementer-retry)

## Summary

Aligned README phase map, portfolio doc pointers, and SPEC threat-model / Phase 4 sequencing to S5.1–S5.11 shipped vs Path-B-deferred reality. Root `SPEC.md` synced from `docs/SPEC.md`. Did **not** edit `state.json`.

**Retry 1 (verifier refute):** Fixed SPEC §14 false transcript-committed claim; synced root SPEC; corrected `memory-bank/progress.md` knob inventory for `max_profile_build_block_days` (wired S5.6).

## Files changed

| File | Rationale |
|------|-----------|
| `README.md` | Phase 0/4 no longer claim missing compose/DB roles/playbook; badge → Phase 0–4 Partial / not CALIBRATED; quick-start four-container; new Portfolio docs table |
| `docs/SPEC.md` | §3.2 adversary bullets qualified with S5.3–S5.6/S5.4 shipped refs; §5.5–§5.7, §8.4–§8.5, §9.2, §10.5, §13 Phase 0/4, §13.1 deferrable list updated; §6.8 `max_profile_build_block_days` wired note; **retry:** §14 transcripts optional / not shipped |
| `SPEC.md` (root) | Full sync from `docs/SPEC.md` (including retry §14 fix) |
| `docs/pgvector-embedding-migration.md` | S5.9 startup check no longer marked future |
| `memory-bank/progress.md` | Phase 0/4 status, overstatements register, S5.3–S5.12 rows closed; **retry:** `max_profile_build_block_days` inventory → wired |

## AC coverage

| AC | Status |
|----|--------|
| 1. README phase table honest (S5 shipped; Partial / not CALIBRATED preserved) | PASS |
| 2. Threat-model "Addressed via X" matches shipped behavior; Path B deferred qualified | PASS — §3.2, §5.5–§5.7 |
| 3. Pointers to deployment, pgvector, llm-determinism, counterfactual docs | PASS — README Portfolio docs + SPEC cross-refs |
| 4. Root SPEC synced | PASS — byte sync via Write |
| 5. No false CALIBRATED / 100% recall claims | PASS — grep clean on README |

## Verification

```text
$ rg "missing 4-container|Postgres-only|CALIBRATED \(Audit|100% recall" README.md docs/SPEC.md SPEC.md
(no matches in README/SPEC threat sections; README retains honest "not CALIBRATED")

$ rg "shipped S5\.(3|4|5|6|7|8|9|10)" docs/SPEC.md | wc -l
(multiple hits — threat model + Phase 4 table)

$ rg "Phase 4.*Open" README.md memory-bank/progress.md
(no matches — Phase 4 now Partial)
```

## Retry 1 verification

```text
$ rg "committed as .docs/architecture-debate" docs/SPEC.md SPEC.md
(no matches)

$ rg "not.*shipped" docs/SPEC.md -n | rg "689"
689:... optional provenance artifacts and are **not** shipped ...

$ rg "max_profile_build_block_days.*defer" memory-bank/progress.md
(no matches)

$ rg "max_profile_build_block_days" memory-bank/progress.md
171:| S5.6 | `max_profile_build_block_days` supervisor escalation | ...
210:| `max_profile_build_block_days` | 30 | wired | `batch/profile_builder/builder.py` ...
```

## Unresolved

1. ~~Verifier should confirm debate transcripts remain optional (not claimed shipped).~~ **Resolved retry 1:** §14 now states transcripts optional / not shipped.
2. ~~`memory-bank/progress.md` knob inventory still lists `max_profile_build_block_days` as defer~~ **Resolved retry 1:** inventory row → wired (S5.6).
3. Empirical LLM determinism run still requires operator API keys (`docs/llm-determinism-check.md` status: not executed).
