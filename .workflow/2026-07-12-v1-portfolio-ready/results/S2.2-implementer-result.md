# S2.2 implementer result

## Summary

Added evidence-binding reconstruction tests. No scorer changes required — `score_event` already guarantees `raw_total = Σ contribution_score_i`, and `decision.score` equals that sum unless low-confidence damping applies.

**Verifier fix (refuted → addressed):** Damping test now uses `laplace_alpha=0.0001`, boosted rarity weights, and `anomaly_threshold=45` so `Σ contribution_score` (≈100) strictly exceeds `threshold-5` (40). Asserts `decision.score == 40 < sum` with damping flag set. Undamped fixture now asserts hour/geo/endpoint rarity features are clearly non-zero (not just process+drift).

## Formula tested

**Code invariant (not SPEC §7 literal):**

```
raw_total = Σ contribution_score_i
```

Each `contribution_score` already folds in feature weight, Laplace rarity centering, `contribution_scale_max` capping, and cohort-novelty suppression. `confidence_weight` on `FeatureContribution` is metadata for decision confidence, **not** multiplied into the score sum.

**Decision score semantics:**

| Path | Condition | `decision.score` |
|---|---|---|
| Undamped | `decision.confidence >= confidence_floor` | `== raw_total` |
| Damped | `decision.confidence < confidence_floor` | `== min(anomaly_threshold - 5, raw_total)` + flag `low_confidence_damping_applied` |

SPEC text (`raw_score = Σ (contribution_i × weight_i × confidence_i)`) describes the conceptual model; the implementation pre-multiplies weight into `contribution_score` and does not fold per-feature `confidence_weight` into the aggregate.

Tolerance: `1e-6` absolute.

## Files changed

| File | Rationale |
|---|---|
| `tests/worker/test_evidence_binding.py` | Evidence-binding reconstruction tests (undamped + damped paths) |

`worker/scorer.py` — **not modified**. Existing aggregation at lines 367 and 400–406 is consistent with contributions.

## Verification

```text
$ python -m pytest -k "evidence_binding or reconstruct or raw_score" -v --tb=short

tests/worker/test_evidence_binding.py::test_evidence_binding_reconstructs_raw_score_from_contributions PASSED
tests/worker/test_evidence_binding.py::test_evidence_binding_damped_score_differs_from_contribution_sum PASSED

============================== 2 passed in 1.21s ==============================
```

Damping probe (post-fix): `Σ contribution_score=100.0`, `anomaly_threshold-5=40.0`, `decision.score=40.0`, flag `low_confidence_damping_applied` set.

## Acceptance checklist

- [x] Test loads real `DecisionRecord` from `score_event` (not hand-waved)
- [x] Reconstructs score from contributions using documented aggregation rule (module docstring)
- [x] Asserts reconstructed value equals `decision.score` (undamped) or damped formula within `1e-6`
- [x] Covers multiple non-zero contributions (hour, geo, endpoint, process, drift — all verified non-zero)
- [x] Covers damping path where `decision.score < Σ contribution_score` (sum≈100, cap=40)

## Unresolved

None.
