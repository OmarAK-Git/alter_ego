# S4.6 Researcher Result — Calendar dual-score / telemetry gap two-tier

**Packet:** S4.6 — defer-or-ship (SPEC §6.5 / §6.6)  
**Date:** 2026-07-13

## Decision: **B defer**

Calendar dual-score (§6.6) and telemetry-gap two-tier policy (§6.5) should be **deferred with explicit SPEC downgrade**, following the S2.7 Path B pattern. Plan preference is "defer unless cheap"; neither feature is cheap to ship honestly. §6.8 already lists the YAML knobs as deferred; Phase 4 §13 schedules the sensor-health / gap path; §13.1 deferrable list covers "elaborate sensor-health workflows beyond basic staleness circuit breaker." A minimal stub (reading config with no calendar data or gap detector) would violate the constraint: *must not silently leave SPEC claiming shipped behavior that is absent.*

---

## Evidence

### Production code — zero implementation

| Capability | Expected (SPEC) | Verified in repo | Status |
|---|---|---|---|
| Read `max_calendar_adjustment` | Scorer bounds calendar factor | `rg max_calendar_adjustment --glob '*.py'` → **0 hits** | **Absent** |
| Read `gap_correlation_window` | Localized gap → entity risk | `rg gap_correlation_window --glob '*.py'` → **0 hits** | **Absent** |
| Read `investigation_context_window` | Detail timeline window | `rg investigation_context_window --glob '*.py'` → **0 hits** | **Absent** |
| `context_adjusted_score` | Dual score alongside `raw_score` | `rg context_adjusted_score` → **0 hits** in `*.py` / web | **Absent** |
| `calendar_context_active` flag | Triage icon | `rg calendar_context` → **0 hits** in Python | **Absent** |
| Calendar entry store | Maintenance-window lookup | No model/table; `core/models.py` has events/profiles/decisions only | **Absent** |
| Gap detection / correlation | Per-entity vs source-wide gaps | `rg 'gap\|sensor.*health\|telemetry.*gap' --glob '*.py'` → no pipeline code | **Absent** |
| Sensor-health incidents | Collector-wide gap path (§6.5) | Listed Phase 4 only (`docs/SPEC.md:623`) | **Absent** |

### YAML — keys present, unwired (S2.8 inventory confirmed)

| Knob | Value | Location | Reader |
|---|---|---|---|
| `max_calendar_adjustment` | 0.3 | `config/scoring_config.yaml:29` | **None** |
| `gap_windows.gap_correlation_window` | 60 | `config/scoring_config.yaml:43` | **None** |
| `gap_windows.investigation_context_window` | 14 | `config/scoring_config.yaml:44` | **None** |

Inventory status **defer → S4.6**: `memory-bank/progress.md:205,232-233`; S2.8 verifier: `.workflow/.../S2.8-verifier-result.md:42`.

### Scorer / schema / API — single score, no calendar layer

- `DecisionRecord` schema (`core/schemas/decisions.py:14-29`): field is `score` only; no `raw_score`, `context_adjusted_score`, or calendar metadata.
- `DecisionRecordModel` (`core/models.py:61-77`): same — single `score` column.
- Scorer returns `DecisionRecord` with `score=total_score` (`worker/scorer.py:462-478`); no post-aggregation calendar adjustment; no `calendar_adjustment` synthetic contribution.
- Flags actually emitted today: `low_resolution_confidence`, `staleness_halt`, novelty/cap hits, `volume_delta_deferred`, `low_confidence_damping_applied`, `simulated_containment_queued` (`worker/scorer.py:262-453`) — not `calendar_context_active` or `infrastructure_volatile`.
- `GET /api/alerts` and `GET /api/alerts/{id}` expose `score` only (`web/api.py:75-109`); no `context_adjusted_score`, calendar entry, or entity timeline.

### Web UI — SPEC §11 claims not met

- Triage renders `alert.score` only (`web/static/app.js:88`); **no flag icons** (including `calendar_context_active`).
- Detail renders `data.decision.score` + contributions (`web/static/app.js:139-150`); **no** `context_adjusted_score`, calendar entry, or `investigation_context_window` timeline.
- `index.html` has no timeline/calendar/flag elements (`rg` on `web/static` → no matches).

### SPEC honesty gap (needs downgrade on defer path)

§6.8 (`docs/SPEC.md:288`) already defers the knobs, but other sections still read as shipped:

| Passage | Overclaim |
|---|---|
| §6.2 (`:215`) | Lists `context_adjusted_score` as decision-record field |
| §6.2 (`:222`) | Lists `calendar_context_active` in shipped flags |
| §6.2 (`:233`) | "calendar adjustment (§6.6) are layered on after this baseline" |
| §11.1 (`:519`) | `calendar_context_active` triage icon |
| §11.2 (`:525-530`) | Detail view requires `context_adjusted_score`, calendar entry, investigation timeline |
| Phase 3 (`:615`) | "Suppressed-decisions view … with aging escalation" (separate S4.3 defer) |

Phase 4 authority for gap work: `docs/SPEC.md:623` — "Sensor-health incident path (telemetry gap two-tier, audit chain integrity)."  
§13.1 deferrable: `docs/SPEC.md:653` — "Elaborate sensor-health workflows beyond basic staleness circuit breaker."

### Cost estimate

| Path | Scope | Honest? | Relative cost |
|---|---|---|---|
| **B defer (docs)** | SPEC downgrade §6.5/§6.6 + §6.2/§11 + progress; keep YAML placeholders | Yes | **Low** — S2.7 precedent (~5 files, docs-only) |
| **A minimal ship — calendar only** | Calendar ORM + ingest/API + scorer dual-score + schema/DB + detail UI + tests + calibration note | Needs real calendar data source | **High** — multi-layer, no existing calendar surface |
| **A minimal ship — gap only** | Gap detector in ingest/worker + correlation logic + sensor-health incidents + optional timeline API | Partial without incident store | **High** — Phase 4 item |
| **A stub ship** | Read YAML, no-op or fake calendar | **No** — SPEC describes behavioral semantics, not config presence | Rejected |

Cheapest *honest* partial (investigation timeline only, no scoring) still requires: entity event query endpoint, window from config, detail UI section, tests — materially more than defer and does not satisfy §6.5 scoring tier or §6.6 dual-score.

---

## Implementer write scope (defer path — Path B)

**In scope:**

1. **`docs/SPEC.md`** (+ sync `SPEC.md` root per S2.7/S2.8 pattern)
   - **§6.5:** Add v1 deferral banner (S4.6 / Path B): two-tier gap policy is design target; no gap detector, correlation scorer, or sensor-health incident path in v1; `gap_windows.*` YAML placeholders only.
   - **§6.6:** Add v1 deferral banner: no scheduled-change calendar store; no `context_adjusted_score` computation; `max_calendar_adjustment` unwired; containment never auto-suppressed by calendar (retain as design invariant).
   - **§6.2:** Qualify `context_adjusted_score` as deferred with §6.6; remove `calendar_context_active` from shipped flags list (or mark ▲ deferred with §6.6). Clarify v1 `score` field is the operational anomaly score (equivalent to SPEC `raw_score` intent).
   - **§6.2 / §6 scoring narrative (`:233`):** Change "calendar adjustment … layered on" → deferred post-v1 per §6.6.
   - **§11.1:** Defer `calendar_context_active` icon (and `infrastructure_volatile` if still unwired — verify; not in scorer flags today).
   - **§11.2:** Defer `context_adjusted_score`, calendar entry display, and `investigation_context_window` timeline to Phase 4 / future packet; list what detail view **does** ship (score, contributions, explanation).
   - **§13.1 Deferrable:** Add explicit bullets for §6.5 telemetry-gap two-tier and §6.6 calendar dual-score (alongside existing sensor-health deferral language).

2. **`memory-bank/progress.md`**
   - Close S4.6 row: **done, Path B defer** with rationale.
   - Keep `max_calendar_adjustment`, `gap_correlation_window`, `investigation_context_window` as **defer** (unchanged values); note owner → Phase 4 or post-v1 packet.

3. **`config/scoring_config.yaml`** (optional, S4.3 pattern)
   - Inline comments on `max_calendar_adjustment` and `gap_windows.*`: deferred S4.6, no production reader.

4. **`OPS.md`** (one line)
   - Calendar dual-score and telemetry-gap two-tier deferred; v1 uses single `score` and staleness halt only for profile freshness.

**Out of scope (explicit defer):**

- Calendar / maintenance-window data model, ingest, or admin API.
- Scorer calendar adjustment, `context_adjusted_score`, `calendar_adjustment` contribution.
- Gap detection, localized vs source-wide correlation, entity risk inflation from gaps.
- Sensor-health incident table/workflow.
- Detail UI timeline, calendar icons, dual-score display.
- DB migration for new decision fields.
- Calibration sweep for `max_calendar_adjustment`.

**Not touched:** `state.json`, scorer weights/thresholds, Python runtime (docs-only packet).

---

## Acceptance criteria (implementer)

1. `docs/SPEC.md` and root `SPEC.md` no longer imply v1 ships §6.5 gap scoring or §6.6 calendar dual-score without ▲ deferral qualification.
2. §6.2 / §11.1 / §11.2 aligned: shipped vs deferred fields/icons/views are explicit.
3. §13.1 deferrable list mentions calendar dual-score and telemetry-gap two-tier (or cross-refs §6.5/§6.6 defer banners).
4. `memory-bank/progress.md` S4.6 closed Path B; inventory rows for the three knobs remain **defer** with Phase 4 / post-v1 owner.
5. `rg 'max_calendar_adjustment|gap_correlation_window|investigation_context_window' --glob '*.py'` still **0 hits** (no fake wiring).
6. Docs-only: `pytest -q` unit/integration unchanged (no regressions from markdown/YAML comments).

---

## Open questions

1. **`infrastructure_volatile` flag:** Listed in SPEC §6.2/§11.1 but no Python emitter found — include in same defer scrub as calendar flags, or separate inventory ticket?
2. **`raw_score` naming:** v1 uses `score` in schema/API; defer packet should state equivalence rather than rename column (migration cost).
3. **Phase 4 bundling:** Calendar (§6.6) and gap/sensor-health (§6.5) may ship as one Phase 4 packet or split — not decided; defer docs should point to Phase 4 §13 without over-committing sequencing.

---

## If shipped later (reference only)

Minimum additional scope beyond v1:

- **§6.6:** `calendar_entries` store; lookup at score time; `context_adjusted_score` + `calendar_adjustment` contribution; detail API/UI; triage `calendar_context_active` icon; tests; calibration governance for `max_calendar_adjustment`.
- **§6.5:** Inter-event gap analysis; entity-local vs cohort/source correlation using `gap_correlation_window`; sensor-health incident path for collector-wide gaps; detail timeline query over `investigation_context_window`; tests.

Estimated **not cheap** — touches ingest/worker, schema, API, UI, incident model, and eval discipline.
