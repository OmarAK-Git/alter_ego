# S3.2 implementer result

implementation_model: composer-2.5

## Summary

Refreshed `docs/calibration_final_metrics.json` and `docs/calibration_pr_curve.json` from the verified S3.1 sweep DB (`alter_ego_calibrate_s31.db`). Operating point is **YAML `anomaly_threshold=45`** — not auto-promoted from PR-curve F1. **Not CALIBRATED.**

## Written metrics (`calibration_final_metrics.json` @ thr=45)

| Metric | Value |
|---|---|
| Precision | 0.0191 |
| Recall | 0.8171 |
| F1 | 0.0373 |
| TP | 67 |
| FP | 3448 |
| FN | 15 |
| TN | 8969 |
| Drift alerts | 46 |

### Per-scenario recall

| Scenario | Recall | TP | FN |
|---|---|---|---|
| scenario_1_sharp_misuse | **1.000** | 1 | 0 |
| scenario_2_slow_roll | **1.000** | 35 | 0 |
| scenario_3_subtle | 0.667 | 30 | 15 |
| scenario_4_service_abuse | **1.000** | 1 | 0 |

**scenario_2_slow_roll** recall **1.0** — matches S3.1 evidence.

### Metadata

- `source`: `alter_ego_calibrate_s31.db`
- `sweep`: `S3.1`
- `config_version`: `2.2`
- `generator_seed`: `42`
- `anomaly_threshold`: `45.0`

### Baseline (heuristic novelty/rarity ≥2)

| Metric | Value |
|---|---|
| Precision | 0.0 |
| Recall | 0.0 |
| TP / FP / FN | 0 / 0 / 82 |

Note: `batch.eval.calibrate.run_baseline` raises `KeyError` on drift-only contribution dicts in the S3.1 DB; `scratch/s32_refresh_calibration_docs.py` uses a tolerant local baseline helper.

## PR curve (`calibration_pr_curve.json`)

- 29 threshold points (10–150, step 5)
- Fields per point: `threshold`, `precision`, `recall`, `f1`, `tp`, `fp`, `fn`
- thr=45 point matches calibrated metrics exactly

### Recommended alternate operating point (for S3.4 / S3.6 only — **not applied to YAML**)

| Threshold | Precision | Recall | F1 | TP | FP | FN |
|---|---|---|---|---|---|---|
| **55** (best F1 on curve) | 0.0405 | 0.585 | 0.0758 | 48 | 1136 | 34 |

At thr=55: lower recall (58.5% vs 81.7%) but ~3× precision and ~67% fewer FPs vs thr=45. Trade-off for governance review — no `scoring_config.yaml` change in this packet.

## Commands run

```powershell
cd C:\Users\oalan\alter_ego
$env:PYTHONPATH="."
$env:DATABASE_URL="sqlite:///alter_ego_calibrate_s31.db"
python scratch/s32_refresh_calibration_docs.py
```

Exit code: **0**

## Files changed

| File | Rationale |
|---|---|
| `docs/calibration_final_metrics.json` | S3.1 sweep metrics @ thr=45 + metadata + baseline |
| `docs/calibration_pr_curve.json` | Full threshold sweep from S3.1 DB |
| `scratch/s32_refresh_calibration_docs.py` | One-shot refresh helper (baseline-safe) |
| `scratch/s32_summary.json` | Local run summary incl. best-F1 point |

## Acceptance

- [x] Both docs JSON artifacts refreshed from S3.1 DB (not hand-copied stale narrative)
- [x] `scenario_2_slow_roll` recall = 1.0
- [x] PR curve present with threshold / P / R / F1 / tp / fp / fn
- [x] No `scoring_config.yaml` edits
- [x] No CALIBRATED claim in JSON or this result

## Unresolved

None.
