implementation_model: composer-2.5

# S4.2 implementer result

## Summary

Implemented SPEC §8.6 explainer queue-depth limit with deterministic template fallback under load. Added `explainer_queue_depth: 8` to scoring config; `generate_explanation` tracks in-flight LLM jobs with a thread-safe counter and skips the provider when at capacity, logging overflow and persisting audit-visible `validation_notes` on the explanation record. Decisions are never dropped — overflow paths still produce and save a template explanation. API `/explain` now returns `validation_status` in the response.

## Files changed

| File | Rationale |
|------|-----------|
| `config/scoring_config.yaml` | Added `explainer_queue_depth: 8` with SPEC §8.6 comment (no weight/threshold changes) |
| `worker/explainer.py` | Queue-depth gate, slot acquire/release, overflow logging + template fallback, `reset_explainer_queue_state()` for tests |
| `web/api.py` | Return `validation_status` from explain endpoint |
| `tests/worker/test_explainer.py` | Config key test, overflow → template without LLM, under-capacity LLM path |

## Config key

- **Key:** `explainer_queue_depth`
- **Default:** `8` (YAML + code fallback via `EXPLAINER_QUEUE_DEPTH_DEFAULT`)

## Verification

```text
python -m pytest tests/worker/test_explainer.py -v --tb=short
```

```
============================= test session starts =============================
collected 8 items

tests/worker/test_explainer.py::test_low_trust_slot_escapes_injection_attempt PASSED
tests/worker/test_explainer.py::test_low_trust_slot_length_cap PASSED
tests/worker/test_explainer.py::test_high_trust_fields_remain_structured_json PASSED
tests/worker/test_explainer.py::test_template_fallback_on_invalid_json PASSED
tests/worker/test_explainer.py::test_prohibited_content_validation PASSED
tests/worker/test_explainer.py::test_scoring_config_includes_explainer_queue_depth PASSED
tests/worker/test_explainer.py::test_queue_overflow_uses_template_without_llm PASSED
tests/worker/test_explainer.py::test_under_capacity_calls_llm_provider PASSED

======================= 8 passed, 32 warnings in 0.65s ========================
```

## Acceptance criteria

1. ✅ Configurable max in-flight LLM jobs via `explainer_queue_depth` (default 8)
2. ✅ At/over depth → template fallback without `provider.generate`
3. ✅ Overflow logged (`logger.warning`) + `validation_notes` on `ExplanationRecord`
4. ✅ Under capacity → existing LLM path preserved
5. ✅ Tests prove overflow vs under-capacity behavior
6. ✅ Focused pytest passes (8/8)

## Unresolved

- None. `state.json` not modified per packet constraints.
