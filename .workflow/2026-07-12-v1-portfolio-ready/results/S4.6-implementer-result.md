implementation_model: composer-2.5

# S4.6 Implementer Result — Path B (defer)

**Decision:** B — Defer calendar dual-score (§6.6) and telemetry-gap two-tier (§6.5) with explicit SPEC downgrade.

**Date:** 2026-07-13

## Files changed

| File | Rationale |
|---|---|
| `docs/SPEC.md` | Deferral banners on §6.5/§6.6; qualify §6.2/§11.1/§11.2; add §13.1 deferrable bullets |
| `SPEC.md` (repo root) | Full UTF-8 sync from `docs/SPEC.md` |
| `config/scoring_config.yaml` | Inline defer comments on `max_calendar_adjustment` and `gap_windows.*` (no numeric changes) |
| `memory-bank/progress.md` | Close S4.6 Path B; inventory knobs → Phase 4 owner |
| `OPS.md` | One-line note: single `score`, staleness halt only for freshness |
| `.workflow/.../S4.6-implementer-result.md` | This report |

**Not touched:** state.json, scorer/runtime code, scoring weights/thresholds, Python readers for deferred knobs.

## SPEC.md passages changed

### §6.2 Decision Record Fields
- `raw_score` clarified: v1 schema/API uses `score` (single operational anomaly score); `context_adjusted_score` deferred with §6.6.
- Shipped flags list trimmed; `calendar_context_active` deferred with §6.6; `infrastructure_volatile` deferred with §12.
- Scoring narrative: drift (§6.4) ships; calendar adjustment (§6.6) deferred post-v1.

### §6.5 Telemetry Gap Handling
- Added **v1 deferral (S4.6 / Path B)** banner: no gap detector, correlation scorer, or sensor-health incident path; `gap_windows.*` placeholders only; staleness circuit breaker is v1 substitute.

### §6.6 Calendar Context
- Added **v1 deferral (S4.6 / Path B)** banner: no calendar store, no dual-score, no `max_calendar_adjustment` reader; single `score` in API/UI; containment-never-auto-suppressed invariant retained as design target.

### §11.1 Triage View
- `score` API field noted; `calendar_context_active` and `infrastructure_volatile` icons deferred.

### §11.2 Detail View
- Reordered to list v1-shipped content (score, contributions, explanation, lineage).
- Deferred: `context_adjusted_score`, calendar entry, `investigation_context_window` timeline → Phase 4.

### §13.1 Deferrable
- Added explicit bullets for §6.5 telemetry-gap two-tier and §6.6 calendar dual-score.

## Verification

```
python -c "import shutil, pathlib; ..."  # SPEC sync byte-identical — OK
rg "max_calendar_adjustment|gap_correlation_window|investigation_context_window" --glob "*.py"
```

**Python reader check:** 0 hits (no fake wiring).

```
python -m pytest -q --tb=line
```

**Result:** 85 passed, 8 failed — all failures in `tests/live/*` (server not running; pre-existing, unrelated to docs-only change). No test regressions from this packet.

## Acceptance checklist

1. SPEC no longer implies v1 ships §6.5/§6.6 without ▲ deferral qualification — **pass**
2. §6.2 / §11.1 / §11.2 aligned: shipped vs deferred explicit — **pass**
3. §13.1 deferrable list mentions calendar dual-score and telemetry-gap two-tier — **pass**
4. progress.md S4.6 closed Path B; three knobs remain defer → Phase 4 — **pass**
5. No Python readers for deferred knobs — **pass**
6. No scoring weight changes — **pass**
7. Root `SPEC.md` byte-identical to `docs/SPEC.md` — **pass**

## Unresolved

- `infrastructure_volatile` flag defer cross-ref to §12 left for S4.7 packet (not in S4.6 write scope beyond SPEC scrub).
- Phase 4 may bundle §6.5 and §6.6 in one packet or split — defer docs point to §13 without sequencing commitment.
