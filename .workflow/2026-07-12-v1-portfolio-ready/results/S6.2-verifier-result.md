# S6.2 verifier result

verification_model: cursor-grok-4.5-high

## Verdict

**survives**

## Claim tested

S6.2 is complete: residual-risk + open drift hypotheses doc exists under `docs/` with evidence-backed residuals (including embedding normalizer trap) and open research hypotheses; no scoring knobs changed.

## Acceptance criteria

| AC | Result | Evidence |
|---|---|---|
| 1. Concrete residual risks + evidence pointers | **PASS** | `docs/residual-risk-drift-hypotheses.md` §2.1–2.5 cite metrics JSON, operating-point, S5-EXIT-GATE |
| 2. Open drift hypotheses (not closed) | **PASS** | §3 H1–H10 framed as open questions; §3 header: “not solved or recommended changes” |
| 3. Stale `core/` normalizer default trap | **PASS** | §2.4; confirmed `DEFAULT_EMBEDDING_INPUT_NORMALIZER_VERSION = "1.0"` (`core/schemas/profiles.py:8`) vs `NORMALIZER_VERSION = "1.0-char-3gram-hash-128"` (`worker/vectorizer.py:6`); ORM mirrors default (`core/models.py:58-59`) |
| 4. Honest: not CALIBRATED; no governance-skipping knob recs | **PASS** | Status line + §5; thr=55 labeled diagnostic; governance pointers to `scoring-config-governance-s3.md` / S6.3 |
| 5. `state.json` not marked done by implementer | **PASS** | S6.2 `status: "verifying"` (`.workflow/.../state.json` ~1113–1125), not `done` |

## Spot-checks (cited numbers)

| Citation in doc | Source check | Match? |
|---|---|---|
| FP **3448**, P **0.019**, R 0.817 | `docs/calibration_final_metrics.json` `fp: 3448`, `precision: 0.01906…`, `recall: 0.81707…`; `phase2-s3-operating-point.md` | Yes |
| scenario_3 recall **0.667**, TP 30 / FN **15** | metrics JSON `scenarios.scenario_3_subtle` | Yes |
| thr=55 diagnostic P≈0.041, R 0.585, FP 1136, FN 34, F1 0.076 | `docs/calibration_pr_curve.json` threshold 55: P 0.04054…, R 0.585…, FP 1136, FN 34, F1 0.0758… | Yes (honest rounding) |
| 46 drift alerts; S2 recall 1.0 (35/35) | metrics JSON `drift_alerts: 46`, `scenario_2_slow_roll` | Yes |
| Drift knobs (threshold 5.0, half-life 7, hist count 5, weights emb 40 / process 20 / others 5, alert weight 100, cap 50) | `config/scoring_config.yaml` | Yes |
| Drift mechanism (recent 3d vs last N promoted profiles; MIN_NORM_COHORT=3) | `batch/profile_builder/builder.py` ~322–380 | Yes (H1 observation accurate vs code) |

## Scoring knobs / product code

- Conversation-start `git status` snapshot: `config/scoring_config.yaml` **not** listed dirty.
- File content still v2.2 / `anomaly_threshold: 45.0` / `drift_threshold: 5.0` with thr=55 “diagnostic only” comment — consistent with “untouched for S6.2.”
- Doc-only artifact under `docs/`; implementer also updated `memory-bank/progress.md` (in write scope). No product-code requirement for this packet.

## Adversarial attempts (did not refute)

1. **Broad “complete” vs state still verifying** — AC5 *requires* implementer not mark `done`; verifying is correct.
2. **thr=55 P 0.041 vs operating-point 0.0405** — both consistent with JSON; not a false claim.
3. **Hidden knob recommendations** — grep of doc shows governance prohibitions and “not a threshold recommendation”; hypotheses defer YAML changes to sweeps.
4. **Normalizer trap overstated as production bug** — doc correctly says production safe via builder; fail-closed halt path exists in `worker/scorer.py` metadata check — matches S5-EXIT-GATE residual.

## Strongest reason

All five ACs hold on independently checked artifacts: the handoff doc exists with spot-checked metrics, open H1–H10, the confirmed `"1.0"` vs `"1.0-char-3gram-hash-128"` engineering residual, explicit not-CALIBRATED / no-governance-skip posture, and `state.json` S6.2 still `verifying` (not `done`).

## Commands / reads

- Read: `packets/S6.2-verifier.md`, `packets/S6.2-implementer.md`, `results/S6.2-implementer-result.md`, `docs/residual-risk-drift-hypotheses.md`, `docs/calibration_final_metrics.json`, `docs/calibration_pr_curve.json` (thr 45/55), `docs/phase2-s3-operating-point.md` (grep), `core/schemas/profiles.py`, `core/models.py`, `worker/vectorizer.py`, `worker/scorer.py`, `config/scoring_config.yaml`, `batch/profile_builder/builder.py` (drift path), `results/S5-EXIT-GATE-verifier-result.md` (residual), `state.json` S6.2 block, `memory-bank/progress.md` S6.2 row
- Grep: `DEFAULT_EMBEDDING_INPUT_NORMALIZER_VERSION`, CALIBRATED/recommend language, metrics keys
