verification_model: cursor-grok-4.5-high

# S4.1 Verifier Result — Explainer slot-isolated low-trust fields

## Claim restated

S4.1 is complete: low-trust explainer fields are slot-isolated, escaped, and length-capped; the prompt forbids treating slot contents as instructions; high-trust fields stay structured bindings; tests cover injection escape + length cap; fallback/prohibited tests still pass; `pytest tests/worker/test_explainer.py` passes.

## Verdict

**survives**

## Strongest reason

Fresh `pytest tests/worker/test_explainer.py -v --tb=short` → **5 passed**; code in `worker/explainer.py` places command_line / file_path / url / free_text in labeled delimited slots with HTML-entity escape + 512-char cap, keeps high-trust JSON in `<high_trust_context>`, and includes `SLOT_ISOLATION_INSTRUCTION` — matching all six acceptance criteria.

## Evidence gathered (independent of implementer transcript)

### Code reads

| Check | Evidence |
|---|---|
| AC1 slots | `format_low_trust_slot` / `_build_low_trust_slots` → `<command_line>`, `<file_path>`, `<url>`, `<free_text>` inside `<low_trust_slots>` (`worker/explainer.py` ~162–248, 273–296) |
| AC2 escape + cap | `escape_slot_content` (`&`, `<`, `>`), `cap_slot_content` / `LOW_TRUST_SLOT_MAX_LENGTH = 512` (~27–28, 147–159) |
| AC3 instruction | `SLOT_ISOLATION_INSTRUCTION` embedded in `build_prompt` (~42–46, 282) |
| AC4 high-trust | Named JSON bindings in `<high_trust_context>`; string raw_values stripped via `_sanitize_contributions` / `_sanitize_for_high_trust` (~189–271) |
| AC5 tests | `test_low_trust_slot_escapes_injection_attempt`, `test_low_trust_slot_length_cap`, plus retained `test_template_fallback_on_invalid_json`, `test_prohibited_content_validation` |

### Adversarial probes (readonly Python)

- Profile `description` / event `message` → free_text slots; not in high-trust JSON.
- Contribution string `raw_value` with `feature_name=command_line` → low-trust slot, absent from high-trust block.
- Cap-then-escape: 512× `<` becomes 2048 chars after escape (pathological expansion). Residual quality note only — AC2 requires escape + length-cap applied; SPEC does not require post-escape ≤512; tests assert cap on typical content.

### Commands run

```text
python -m pytest tests/worker/test_explainer.py -v --tb=short
# 5 passed, 22 warnings in 0.64s

python -m ruff check worker/explainer.py tests/worker/test_explainer.py
# All checks passed!

python -c "..."  # probes: free_text routing, raw_value isolation, cap-then-escape length
```

## Attempted refutations (did not hold)

1. **Weak line in injection test** (`IGNORE ALL RULES` only checked before `<high_trust_context>`): stronger asserts still hold (`malicious not in prompt`, escaped `</command_line>`).
2. **Cap-then-escape length expansion**: does not violate stated AC/SPEC wording; not grounds to refute completion.
3. **Unmapped free-text keys** (e.g. `notes`): outside mapped low-trust sources; AC requires at least command lines, paths, URLs, free-text feature values — covered via field map + contribution string path.

## Residual (non-blocking)

- Prefer capping after escape (or re-capping escaped output) if prompt-size bounds must be strict under adversarial angle-bracket floods.
