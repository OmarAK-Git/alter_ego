verification_model: cursor-grok-4.5-high

# S5.8 Verifier Result — pgvector / embedding migration playbook

**Date:** 2026-07-13  
**Packet:** S5.8-verifier  
**Claim tested:** Full pgvector/embedding migration playbook shipped under `docs/`.

## Verdict: **survives**

**Strongest reason:** Independent reads confirm `docs/pgvector-embedding-migration.md` exists with lock table (`alter-ego-ngram-v1` / 128-d), why dim breaks pgvector, offline migration phases, rollback, and verification — and it labels `nomic-embed-text` historical-only; cross-links land in `docs/SPEC.md` §4.3 and `docs/deployment.md`.

---

## Method

Treat implementer transcript as unevidenced. Independently:

1. Full-read `docs/pgvector-embedding-migration.md`
2. Read §4.3 in both `SPEC.md` and `docs/SPEC.md`; diff the two files
3. Read cross-link sites in `docs/deployment.md` and Related docs / SPEC_V3 §6.8 / §9
4. Cross-check lock claims against `worker/vectorizer.py`, `core/schemas/profiles.py`, `core/models.py`, Alembic `a2b3c4d5e6f7` / `e4f5a6b7c8d9`, and `docker-compose.yml` service names
5. Grep `docs/` + `core/` for nomic / playbook pointers

---

## Acceptance checklist

| # | Criterion | Result | Evidence |
|---|---|---|---|
| 1 | Playbook exists under `docs/` | **pass** | `docs/pgvector-embedding-migration.md` present; title + SPEC §4.3 / V3 §6.8 / §9 framing |
| 2 | Covers lock / why dim breaks / steps / rollback / verify | **pass** | §§ Current lock (`:9-22`), Why dimensionality changes (`:26-38`), Pre-migration (`:42-92`), Migration procedure phases 0–5 (`:96-191`), Rollback (`:195-208`), Post-migration verification (`:212-228`) |
| 3 | Does not claim nomic is runtime | **pass** | Historical note headed **(not runtime)**; “No external per-event model runs in the hot path today”; runtime table lists `alter-ego-ngram-v1` / 128 (`:15-20`). No “nomic ships” / “nomic scores” language |
| 4 | Cross-links present | **pass** | `docs/SPEC.md:128` links `[pgvector embedding migration playbook](pgvector-embedding-migration.md)`; `docs/deployment.md:102` links same; playbook Related documents point back to SPEC / SPEC_V3 / deployment (`:251-256`) |
| 5 | If `docs/SPEC.md` changed → root `SPEC.md` sync | **fail (residual)** | Diff is **exactly one line** (§4.3): root still says “documented in the schema migration playbook” with **no path/link**; `docs/SPEC.md` has the markdown link. Lengths 61816 vs 61863; files not equal |

---

## Runtime-truth spot checks (attack surface)

| Claim in playbook | Independent check | Outcome |
|---|---|---|
| Model `alter-ego-ngram-v1`, dim 128 | `core/schemas/profiles.py` defaults; `core/models.py` `Vector(128)`; builder writes those + `NORMALIZER_VERSION` | Matches |
| Vectorizer char 3-gram SHA-256, unit-norm | `worker/vectorizer.py` (`dim=128`, SHA-256 3-grams, L2 norm) | Matches |
| nomic not in hot path | `rg nomic-embed-text core/` → **no matches**; only Alembic history + playbook historical note | Matches AC3 |
| `a2b3c4d5e6f7` → 128-d column | `alembic/versions/a2b3c4d5e6f7_phase1_closeout.py:37-40` drop/re-add `Vector(dim=128)` | Matches |
| `e4f5a6b7c8d9` aligns model default | `e4f5a6b7c8d9_align_embedding_model_default.py` sets server_default `alter-ego-ngram-v1` | Matches |
| Compose `worker` / `batch` / owner DSN | `docker-compose.yml` has `worker`, `batch`, `web`; postgres user `user`/`password` | Matches operator commands |

---

## Attack attempts (did not flip verdict)

1. **Root SPEC desync** — Real. Packet required the sync *check*; root §4.3 still lacks the hyperlink. Does **not** erase playbook existence, required sections, nomic honesty, or the two required cross-link surfaces (`docs/SPEC` + deployment). Tracked as residual, not packet AC4 failure (AC4 is OR of SPEC §4.3 *or* deployment — both docs/ paths satisfied).
2. **SPEC_V3 still says playbook “is required”** (`docs/SPEC_V3.md:219`, §9 `:333`) with no back-link — stale portfolio prose; implementer AC did not require SPEC_V3 edit; playbook still cites §6.8/§9.
3. **Broken `#68-embedding-lock` anchor** — SPEC_V3 `6.8 Embedding Lock` is not a `#` heading; GitHub-style fragment may not resolve. Navigation hygiene only.
4. **Normalizer string split** — Playbook lock uses `1.0-char-3gram-hash-128` (matches `worker/vectorizer.py` / builder write path); `DEFAULT_EMBEDDING_INPUT_NORMALIZER_VERSION` in schemas is still `"1.0"`. Pre-migration SQL omits normalizer. Does not restore a nomic-runtime claim or omit required sections.
5. **“S1.4 debt … remaining schema defaults”** — Overstates residual (ORM/schema defaults already ngram; no `nomic` in `core/`). Direction is still “not runtime.”

---

## Residual risks (do not flip verdict)

- Sync root `SPEC.md` §4.3 to the `docs/SPEC.md` playbook link (one-line).
- Optionally point SPEC_V3 §6.8 / §9 at `pgvector-embedding-migration.md` so the portfolio gate reads as satisfied rather than still-open.
- Align schema default normalizer string with `NORMALIZER_VERSION` (pre-existing S1.4 hygiene; outside S5.8 write scope).

---

## Verdict summary

Packet-scoped claim **survives**. The playbook is on disk under `docs/`, covers the required operator content, does not present nomic as runtime, and is linked from `docs/SPEC.md` §4.3 and `docs/deployment.md`. Root `SPEC.md` remains one line behind `docs/SPEC.md` on that pointer.
