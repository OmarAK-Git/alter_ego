# S2.8 implementer result

**Packet:** S2.8 — Apply S0.4 inventory: wire cheap knobs or strip from calibrated claims  
**Date:** 2026-07-13

## Before / after inventory counts

| Label | Before (S0.4) | After (S2.8) | Δ |
|---|---|---|---|
| **wired** | 25 | **28** | +3 |
| **wire** | 5 | **0** | −5 |
| **defer** | 5 | **7** | +2 |
| **delete** | 2 | **0** | −2 |

**YAML tunables:** 37 → **35** (removed 2 orphan `decay_lambdas` keys). No code-only inventory gaps remain.

## Actions taken per knob

| Knob | Prior status | Action | Rationale |
|---|---|---|---|
| `confidence_k` | wire (code default; absent YAML at S0.4) | **Promote → wired** (no code change) | Already in YAML + `worker/scorer.py:428` since S2.4; inventory only |
| `containment_threshold` | wire | **Promote → wired** (no code change) | `worker/scorer.py:448-450` gates flag + queue write since S1.3 |
| `drift_weights.geolocation` | wire | **Promote → wired** (no code change) | `batch/profile_builder/builder.py:242,250` geo KL in `deltas` since S1.2 |
| `geolocation_rarity` weight | wired (partial notes) | **Notes upgraded → wired** | Builder builds `geolocations` histograms; scorer reads via `_get_cohort_histogram` |
| `min_clean_observation_count` | wire | **Reclassify → defer** | Zero production readers; only `scratch/test_cohort_gate.py` |
| `total_volume_delta` | wire/defer | **Confirm defer** | Scorer always `score_vol=0.0` + `volume_delta_deferred` (S2.6) |
| `decay_lambdas.staleness` | delete | **Stripped from YAML** | No `.py` reader; staleness is binary halt |
| `decay_lambdas.drift` | delete | **Stripped from YAML** | Superseded by `drift_half_life_days` |
| `max_calendar_adjustment` | defer | **Unchanged defer** | S4.6 |
| `gap_windows.*` | defer | **Unchanged defer** | S4.6 |
| `max_profile_build_block_days` | defer | **Unchanged defer** | S5.6 |
| `age_jitter_hours` | defer | **Unchanged defer** | S4.3 |

## Changes

| File | Rationale |
|---|---|
| `config/scoring_config.yaml` | Removed orphan `decay_lambdas` block (no numeric retune) |
| `docs/SPEC.md` §6.8 | Dropped fake-calibrated `decay_lambdas`; added `confidence_k`, containment note, defer list |
| `SPEC.md` | Full UTF-8 copy from `docs/SPEC.md` (S2.8 sync fix; same as S2.7 pattern) |
| `memory-bank/progress.md` | Full inventory refresh (2026-07-13); overstatements fixed for S1.2/S1.3; S2.4/S2.8 done |
| `OPS.md` | One-liner inventory truth: 28/0/7/0; root/docs SPEC sync note |

**Not touched:** application code, weights/thresholds, `state.json`.

## Code reader verification (grep, 2026-07-13)

| Knob | Production reader | Evidence |
|---|---|---|
| `containment_threshold` | yes | `worker/scorer.py:448` `config.get("containment_threshold", 85.0)` |
| `confidence_k` | yes | `worker/scorer.py:428` `config.get("confidence_k", 10.0)` |
| geo histograms + drift KL | yes | `batch/profile_builder/builder.py:157-242` histogram + KL; `worker/scorer.py:334-343` rarity |
| `min_clean_observation_count` | **no** | YAML only; `scratch/test_cohort_gate.py:85` |
| `decay_lambdas.*` | **no** | Zero hits in `*.py` / `*.yaml` after strip |

## Verification

| Check | Result |
|---|---|
| `grep decay_lambdas` (py/yaml) | **0 matches** |
| `grep decay_lambdas` root `SPEC.md` | **1 hit — legacy note only** (`replaces legacy decay_lambdas.drift`; removed S2.8; not live calibrated) |
| Root `SPEC.md` == `docs/SPEC.md` (byte compare) | **identical** |
| `pytest tests/worker/test_confidence.py tests/worker/test_containment_queue.py -v` | **11 passed** |
| Numeric diff on weights/thresholds | **None** (only removed unused section) |

## Inventory summary (post-S2.8)

- **28 wired** — all production-scoring and profile-build knobs that affect runtime behavior today.
- **0 wire** — no remaining near-term unwired knobs from S0.4 list.
- **7 defer** — explicit placeholders: `min_clean_observation_count`, `total_volume_delta`, `max_calendar_adjustment`, `max_profile_build_block_days`, `age_jitter_hours`, `gap_correlation_window`, `investigation_context_window`.
- **0 delete** — orphans stripped; no silent delete-candidates left in YAML.

## Unresolved

- `state.json` S2.8 status not updated (packet write scope excludes it).
- `min_clean_observation_count` wiring deferred to **S5.11** (requires cohort gate design, not a cheap wire).
