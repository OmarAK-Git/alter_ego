# S2.7 Implementer Result — Path B (defer)

**Decision:** B — Defer profile lifecycle state machine with explicit SPEC downgrade.

**Date:** 2026-07-13

## Files changed

| File | Rationale |
|---|---|
| `docs/SPEC.md` | Downgrade §5.2/§5.4/§6.2/§11.1/§13.1 so v1 no longer implies full lifecycle machine ships |
| `SPEC.md` (repo root) | Full sync from `docs/SPEC.md` — refutation fix; root copy had retained unqualified lifecycle claims |
| `memory-bank/progress.md` | Close S2.7 with Path B rationale |
| `docs/SPEC_V3.md` | One-sentence deferral under §6.2 (point-in-time activation) |
| `OPS.md` | One-line operational note: lifecycle deferred; alert workflow ≠ profile lifecycle |

**Not touched:** state.json, scoring weights, Python code, lifecycle stubs/flags.

## SPEC.md passages changed

### §5.2 Profile Composition
- Lifecycle bullet relabeled **deferred post-v1**; notes v1 artifacts lack `lifecycle_state`; points to §5.4.

### §5.4 Lifecycle States
- Added **v1 deferral (S2.7 / Path B)** banner: design target only; no `lifecycle_state` field; scorer does not branch on lifecycle.
- Explicit distinction: **alert workflow states** (§11.5) ≠ profile lifecycle.
- Partial substitutes documented: `cohort_unsupported` + cohort fallback (§6.3); `stale_profile` + staleness halt (§5.7) — halt ≠ dormant penalty.
- State definitions retained as design target for Phase 4.

### §6.2 Decision Record Fields
- `reactivated_dormant` removed from shipped flags list; marked **deferred with §5.4**.
- `cohort_unsupported` added to explicit shipped flags list.

### §11.1 Triage View
- "lifecycle state badge" → **alert workflow state badge** (`new`, `acknowledged`, `investigating` per §11.5); profile lifecycle deferred.
- `reactivated_dormant` icon deferred with §5.4.

### Phase 3 (§13, triage bullet)
- "lifecycle/confidence flags" → confidence/scoring flags; alert workflow per §11.5; profile lifecycle §5.4 deferred.

### §13.1 Schedule Cut Priority
- Added profile lifecycle state machine to **Deferrable** list with partial-substitute note.

## Verification

```
python -m pytest -q --tb=line
```

**Result:** 69 passed, 8 failed — all failures in `tests/live/*` (server not running; pre-existing, unrelated to docs-only change). No test regressions from this packet.

## Acceptance checklist

1. SPEC.md no longer implies v1 ships full §5.4 lifecycle machine without qualification — **pass**
2. Explicit deferral + alert workflow ≠ profile lifecycle — **pass**
3. progress.md S2.7 closed with Path B rationale — **pass**
4. Docs-only; existing pytest remains green (unit/integration; live smoke excluded) — **pass**

## Unresolved

None. Full lifecycle implementation remains future work (Phase 4 / S5.x).

## Refutation fix (2026-07-13)

Verifier refuted initial delivery because repo-root `SPEC.md` still had unqualified lifecycle claims (`lifecycle state badge`, `lifecycle/confidence flags`) while `docs/SPEC.md` was updated.

**Fix:** Full content sync — root `SPEC.md` copied from `docs/SPEC.md` (UTF-8).

**Grep confirmation (root `SPEC.md`):**
- `S2.7 / Path B` — present (§5.4 deferral banner)
- `deferred post-v1` — present (§5.2)
- `lifecycle state badge` — absent (replaced with alert workflow state badge)
