verification_model: cursor-grok-4.5-high

# S5.5 Verifier Result — Staleness + active-alert mandatory escalation

**Date:** 2026-07-13  
**Packet:** S5.5-verifier  
**Claim tested:** S5.5 complete — staleness halt + active-alert mandatory escalation (flags, API queue, extend-halt, tests).

## Verdict: **survives**

**Strongest reason:** Fresh `pytest` on both scoped suites passed **13/13** (exit 0), and independent code reads show the halt path emits `staleness_halt` / score 0 / gated `staleness_active_alert_escalation`, the mandatory queue lists non-anomaly halt decisions with linked uncleared alert IDs, clear/extend-halt suppress the queue with persisted justification, and no `.py` file wires `max_profile_build_block_days`.

---

## Method

Treat implementer transcript as unevidenced. Independently read `worker/scorer.py`, `web/api.py`, `core/models.py`, `core/schemas/workflow.py`, Alembic migration, and both test files; re-ran scoped pytest this session; confirmed no Python usage of `max_profile_build_block_days`.

---

## Fresh verification command

```text
$ PYTHONPATH=. python -m pytest tests/worker/test_staleness_escalation.py tests/web/test_mandatory_escalations.py -v --tb=short
tests/worker/test_staleness_escalation.py::test_staleness_halt_preserves_score_zero PASSED
tests/worker/test_staleness_escalation.py::test_staleness_halt_without_active_alert_no_escalation_flag PASSED
tests/worker/test_staleness_escalation.py::test_staleness_halt_with_active_alert_adds_escalation_flags[new] PASSED
tests/worker/test_staleness_escalation.py::test_staleness_halt_with_active_alert_adds_escalation_flags[acknowledged] PASSED
tests/worker/test_staleness_escalation.py::test_staleness_halt_with_active_alert_adds_escalation_flags[investigating] PASSED
tests/worker/test_staleness_escalation.py::test_cleared_alert_does_not_trigger_escalation_flag PASSED
tests/worker/test_staleness_escalation.py::test_entity_has_active_uncleared_alert_helper PASSED
tests/web/test_mandatory_escalations.py::test_mandatory_escalations_lists_staleness_with_active_alert PASSED
tests/web/test_mandatory_escalations.py::test_mandatory_escalations_no_anomaly_required_for_halt_decision PASSED
tests/web/test_mandatory_escalations.py::test_cleared_alert_removes_from_mandatory_queue PASSED
tests/web/test_mandatory_escalations.py::test_extend_halt_requires_justification PASSED
tests/web/test_mandatory_escalations.py::test_extend_halt_persists_and_removes_from_queue PASSED
tests/web/test_mandatory_escalations.py::test_expired_extend_halt_returns_entity_to_queue PASSED
======================= 13 passed, 26 warnings in 1.05s =======================
PYTEST_EXIT=0
```

`rg max_profile_build_block_days --glob "*.py"` → **no matches** (exit 1).

---

## Acceptance checklist

| # | Criterion | Result | Evidence |
|---|---|---|---|
| 1 | Halt preserved: `staleness_halt`, score 0 | **pass** | `worker/scorer.py:311-339` early return `score=0.0`, `flags` include `staleness_halt`; `tests/worker/test_staleness_escalation.py:106-112` |
| 2 | Halt + uncleared alert → `staleness_active_alert_escalation` (+ `sensor_health_staleness` OK) | **pass** | Escalation gated on `entity_has_active_uncleared_alert` (`scorer.py:314-315`); active states `new\|acknowledged\|investigating` (`:37`); parametrized test (`test_staleness_escalation.py:123-131`) |
| 3 | Halt without alert → no escalation flag | **pass** | Test asserts flag absent (`:115-119`); cleared alert also absent (`:134-140`) |
| 4 | `GET /api/mandatory-escalations` without `is_anomaly` on halt item | **pass** | `_build_mandatory_escalations` filters by escalation flag only (`web/api.py:109-134`); halt fixture `is_anomaly=False` still listed (`test_mandatory_escalations.py:102-120`) |
| 5 | Queue links uncleared alert `decision_id`s | **pass** | `alert_decision_ids` from `get_active_alert_decision_ids` (`api.py:123-129`; `scorer.py:217-236`); asserted (`test_mandatory_escalations.py:98`) |
| 6 | Clear removes from queue | **pass** | Cleared workflow state excluded from active IDs (`scorer.py:232-235`); queue empty for cleared entity (`test_mandatory_escalations.py:123-150`). PUT `/api/alerts/{id}/workflow` persists `cleared` (`api.py:213-235`) — same state field the queue reads |
| 7 | Extend-halt with persisted justification | **pass** | `ExtendHaltRequest.justification` min_length=1 (`workflow.py:34-35`); model + Alembic table (`models.py:256-262`; `h7i8j9k0l1m2_...py`); POST persists + suppresses queue (`api.py:142-162`; test `:158-194`) |
| 8 | Fresh pytest passes | **pass** | Re-run this session: **13 passed**, exit 0 |
| 9 | `max_profile_build_block_days` NOT wired | **pass** | No `.py` matches for the key; remains YAML/docs/S5.6 only |

---

## Attack attempts (did not refute claim)

1. **Clear test seeds DB instead of calling PUT workflow** — AC6 requires clear via existing workflow removes from queue. PUT writes the same `AlertWorkflowStateModel.state` the queue reads; logic path is shared. Gap is test coverage, not missing behavior.
2. **Queue uses “latest escalation-flagged decision,” not “latest decision overall”** — Implementer prose said “latest decision with … escalation”; code keeps an entity if *any* (most recent flagged) escalation decision exists while alerts remain uncleared. Researcher Path A wording matches the code; packet ACs do not require the stricter semantics.
3. **Active-alert helper requires `is_anomaly=True`** — Correct for *linked alerts*; AC4 only forbids requiring anomaly on the *halt/queue listing* decision. Tested.
4. **No confidence_floor on mandatory queue alerts** — Triage list filters floor; escalation links any uncleared anomaly. Packet does not require floor parity.
5. **`max_profile_build_block_days` still in YAML** — Present as deferred config is expected; AC9 is “not wired.” Confirmed unwired in Python.

---

## Residual risks (do not flip verdict)

- Live Postgres needs `alembic upgrade head` for `staleness_halt_extensions` (SQLite tests use `Base.metadata.create_all`).
- Queue may retain entities after a newer non-escalation decision if an older flagged halt row remains and alerts are still uncleared.
- `GET /api/mandatory-escalations` loads all decisions then filters in Python (scalability, not AC).
- `datetime.utcnow()` deprecation warnings in API/tests (noise only).

---

## Conclusion

Task-scoped acceptance criteria are met with fresh pytest evidence and independent static verification. Claim **survives**.
