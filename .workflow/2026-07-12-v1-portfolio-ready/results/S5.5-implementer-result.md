implementation_model: composer-2.5

# S5.5 Implementer Result — Staleness + active-alert mandatory escalation

**Date:** 2026-07-13  
**Packet:** S5.5-implementer

## Summary

Shipped staleness halt escalation flags in the scorer, mandatory escalation queue API, extend-halt persistence with required justification, Alembic migration, and TDD tests. Did **not** implement `max_profile_build_block_days` (S5.6). Did **not** edit `state.json`.

## Files changed

| File | Rationale |
|------|-----------|
| `worker/scorer.py` | Active-alert detection helpers; `sensor_health_staleness` + `staleness_active_alert_escalation` flags on halt path |
| `web/api.py` | `GET /api/mandatory-escalations`, `POST /api/mandatory-escalations/{entity_id}/extend-halt` |
| `core/models.py` | `StalenessHaltExtensionModel` for extend-halt persistence |
| `core/schemas/workflow.py` | `ExtendHaltRequest` with required `justification` |
| `alembic/versions/h7i8j9k0l1m2_add_staleness_halt_extensions.py` | DB table for extend-halt rows |
| `tests/worker/test_staleness_escalation.py` | Scorer combo: halt preserved, escalation flag gating by alert state |
| `tests/web/test_mandatory_escalations.py` | API queue listing, clear removes, extend-halt persists + suppresses queue |
| `results/S5.5-researcher-result.md` | Path A research conclusions (packet summary) |

## Design choices

- **Active alert:** `is_anomaly=True` decision with workflow state in `{new, acknowledged, investigating}` (default `new` when no row).
- **Mandatory queue:** query-time — entity has latest decision with `staleness_active_alert_escalation`, active uncleared alert(s), and no non-expired extend-halt.
- **SLA:** hardcoded `MANDATORY_ESCALATION_SLA_HOURS = 24` (config weights out of scope).
- **Clear path:** existing `PUT /api/alerts/{decision_id}/workflow` with `cleared` removes entity from queue (no active alerts remain).

## Verification

```text
$ PYTHONPATH=. pytest tests/worker/test_staleness_escalation.py tests/web/test_mandatory_escalations.py -v --tb=short
13 passed

$ ruff check worker/scorer.py web/api.py core/models.py core/schemas/workflow.py tests/worker/test_staleness_escalation.py tests/web/test_mandatory_escalations.py
All checks passed!
```

## Unresolved / verifier follow-ups

1. Run `alembic upgrade head` on live Postgres after deploy.
2. Verifier should confirm no `max_profile_build_block_days` wiring (S5.6 scope).
