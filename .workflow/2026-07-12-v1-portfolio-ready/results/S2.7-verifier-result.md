# S2.7 Verifier Result — Path B (defer) re-verify after root SPEC sync

**Verdict:** survives

**Claim under test:** After root `SPEC.md` sync from `docs/SPEC.md`, both SPECs carry S2.7 Path B deferral; no `lifecycle_state` stubs; `progress.md` updated.

**Prior refute:** Root `SPEC.md` still implied lifecycle shipped while `docs/SPEC.md` was updated.

**Date:** 2026-07-13

## Evidence gathered (fresh)

### 1. Root ↔ docs SPEC identity

```
python -c "…sha256 compare SPEC.md vs docs/SPEC.md…"
root ee18db88e694ad4d4d070bc7f785a1830abc0f2e7f57a110c70dc0e0a9f77c2d 57622
docs ee18db88e694ad4d4d070bc7f785a1830abc0f2e7f57a110c70dc0e0a9f77c2d 57622
identical
```

Byte-identical — prior root/docs drift is gone.

### 2. Path B deferral present in both SPECs

| Location | Finding |
|---|---|
| `SPEC.md` / `docs/SPEC.md` L162 | Lifecycle labeled **deferred post-v1**; v1 artifacts lack `lifecycle_state` |
| L174 | Explicit **v1 deferral (S2.7 / Path B)** banner; design target only; alert workflow ≠ profile lifecycle; partial substitutes noted |
| L222 | `reactivated_dormant` marked deferred with §5.4 |
| L516 | **alert workflow state badge** (not lifecycle badge); profile lifecycle deferred |
| L611 | Phase 3 triage: confidence/scoring flags; profile lifecycle §5.4 deferred |
| L647 | Profile lifecycle on **Deferrable** list |

Absent (grep): `lifecycle state badge`, `lifecycle/confidence` — prior refute strings gone from root.

### 3. No `lifecycle_state` stubs

| Scope | `lifecycle_state` / `reactivated_dormant` |
|---|---|
| `core/` (incl. `models.py`, `schemas/profiles.py`) | zero matches |
| `worker/` | zero matches |
| `batch/` | zero matches |
| `web/` | zero matches |
| `tests/` | zero matches |

### 4. `progress.md`

`memory-bank/progress.md` L100: S2.7 **done**, Path B rationale — SPEC §5.2/§5.4/§6.2/§11.1/§13.1 downgraded; no `lifecycle_state`; alert workflow ≠ profile lifecycle; partial substitutes noted.

### 5. Optional docs (packet-allowed)

- `OPS.md` L23: lifecycle deferred; alert workflow ≠ profile lifecycle
- `docs/SPEC_V3.md` L133: deferred post-v1 (S2.7)

## Acceptance vs packet

| Criterion | Result |
|---|---|
| SPEC no longer implies v1 ships full §5.4 without qualification | survives (banner + deferred labels; identical root/docs) |
| Explicit deferral + alert workflow ≠ profile lifecycle | survives |
| progress.md S2.7 closed Path B | survives |
| No `lifecycle_state` stubs | survives |
| Docs-only (no scoring/code stubs) | survives |

## Residual quibble (not a refute)

§5.4 still retains present-tense design-target state bullets under the deferral banner. Packet acceptance required qualification, not deletion of the design target. Banner at L174 is unambiguous.

## Strongest reason

**survives** — root and `docs/SPEC.md` are byte-identical and both carry the S2.7 Path B deferral banner; prior refute (root still claiming shipped lifecycle) no longer holds; no runtime `lifecycle_state` stubs; progress.md closed Path B.
