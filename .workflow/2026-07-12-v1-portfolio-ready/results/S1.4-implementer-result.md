# S1.4 implementer result

**Packet:** S1.4 — Align ProfileArtifact / DB defaults / builder metadata to shipping embedding (`alter-ego-ngram-v1` / 128-d)

## Summary

Replaced `nomic-embed-text` as the claimed-runtime default across Pydantic schema, SQLAlchemy ORM, and profile builder promote path. Introduced shared constants in `core/schemas/profiles.py` so schema, ORM, and builder agree on shipping metadata. Added an additive Alembic revision to update the PostgreSQL `server_default` without rewriting the historical `a2b3c4d5e6f7` migration.

## Files changed

| File | Rationale |
|------|-----------|
| `core/schemas/profiles.py` | Added `DEFAULT_EMBEDDING_*` constants; `ProfileArtifact` defaults now `alter-ego-ngram-v1` / 128 |
| `core/models.py` | ORM `ProfileArtifactModel` column defaults import shared constants |
| `batch/profile_builder/builder.py` | Promote path uses shared constants (was already ngram; now DRY) |
| `alembic/versions/e4f5a6b7c8d9_align_embedding_model_default.py` | Additive revision: `profiles.embedding_model_id` server_default → `alter-ego-ngram-v1` |
| `tests/test_embedding_defaults.py` | **New** — TDD tests for schema, ORM, and builder promote metadata |
| `tests/test_spec_alignment.py` | Removed explicit `nomic-embed-text` fixture overrides; rely on defaults |
| `tests/worker/test_scorer.py` | Removed explicit `nomic-embed-text` fixture overrides; rely on defaults |

## Acceptance criteria

| # | Criterion | Status |
|---|-----------|--------|
| 1 | New profiles / schema defaults use ngram model id and dim 128 | **PASS** — `DEFAULT_EMBEDDING_MODEL_ID = "alter-ego-ngram-v1"`, `DEFAULT_EMBEDDING_DIMENSIONALITY = 128` |
| 2 | Builder metadata on promote matches | **PASS** — builder imports constants; test asserts `NORMALIZER_VERSION` from vectorizer |
| 3 | Tests updated; pytest evidence | **PASS** — see below |
| 4 | Alembic additive revision for server_default | **PASS** — `e4f5a6b7c8d9` revises `d32eb29d44c1` |

## Alembic notes

- **Revision:** `e4f5a6b7c8d9_align_embedding_model_default.py`
- **Upgrade:** `op.alter_column("profiles", "embedding_model_id", server_default="alter-ego-ngram-v1")`
- **Downgrade:** restores `server_default="nomic-embed-text"`
- **Rollback:** `alembic downgrade -1` reverts server default only; existing row values are untouched (non-destructive).
- **Historical migration** `a2b3c4d5e6f7_phase1_closeout.py` left intact (already applied in dev/prod).

## TDD evidence

### Red (before implementation)

```
tests/test_embedding_defaults.py::test_profile_artifact_schema_defaults_to_ngram_model FAILED
  AssertionError: assert 'nomic-embed-text' == 'alter-ego-ngram-v1'
tests/test_embedding_defaults.py::test_profile_artifact_model_orm_defaults_to_ngram_model FAILED
  AssertionError: assert 'nomic-embed-text' == 'alter-ego-ngram-v1'
tests/test_embedding_defaults.py::test_builder_promote_writes_ngram_embedding_metadata PASSED
```

### Green (after implementation)

```
pytest tests/test_embedding_defaults.py -v --tb=short
======================== 3 passed, 2 warnings in 0.78s ========================
```

```
pytest -v --tb=short --ignore=tests/live
======================= 50 passed, 40 warnings in 2.85s =======================
```

Live smoke tests (`tests/live/`, 8) require a running API server and were not run as part of this packet.

## Unresolved / out of scope

- `embedding_input_normalizer_version` ORM column exists but has no Alembic migration (pre-existing); builder writes `NORMALIZER_VERSION` from `worker/vectorizer.py` explicitly.
- Pydantic schema default for `embedding_input_normalizer_version` remains `"1.0"` while runtime uses `"1.0-char-3gram-hash-128"` — builder always sets the vectorizer value on promote.
- Docs (`CLAUDE.md`, `OPS.md`, `SPEC_V3.md`) still mention nomic as historical debt; doc refresh not in S1.4 write scope.
- `state.json` not edited per packet instructions.
