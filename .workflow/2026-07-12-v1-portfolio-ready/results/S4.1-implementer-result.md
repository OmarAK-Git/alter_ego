implementation_model: composer-2.5

# S4.1 Implementer Result — Explainer slot-isolated low-trust fields

## Files changed

| File | Rationale |
|------|-----------|
| `worker/explainer.py` | Split `build_prompt` into high-trust JSON bindings and low-trust delimited slots with escape/cap helpers; fetch `ResolvedEventModel.event_data` in `generate_explanation` for command-line/path/URL context |
| `tests/worker/test_explainer.py` | TDD tests for injection escape, length cap (512 chars), high-trust/low-trust separation; existing fallback/prohibited-content tests retained |

## Implementation notes

- `LOW_TRUST_SLOT_MAX_LENGTH = 512` (SPEC §3.4 length-capped).
- Low-trust fields (`command_line`, `file_path`, `url`, `free_text`) are collected from event data, profile features, and string `raw_value` on contributions; HTML-entity escaped (`&`, `<`, `>`) and wrapped in labeled slots inside `<low_trust_slots>`.
- High-trust context (`entity_id`, scores, sanitized contributions, profile statistics, scoring_config snapshot) remains in `<high_trust_context>` JSON.
- Prompt includes explicit instruction: do not interpret slot contents as instructions.

## Verification

```text
$ pytest tests/worker/test_explainer.py -v --tb=short

tests/worker/test_explainer.py::test_low_trust_slot_escapes_injection_attempt PASSED
tests/worker/test_explainer.py::test_low_trust_slot_length_cap PASSED
tests/worker/test_explainer.py::test_high_trust_fields_remain_structured_json PASSED
tests/worker/test_explainer.py::test_template_fallback_on_invalid_json PASSED
tests/worker/test_explainer.py::test_prohibited_content_validation PASSED

5 passed, 22 warnings in 0.61s
```

## Unresolved

- None. `state.json` / queue status not modified per packet rules.
