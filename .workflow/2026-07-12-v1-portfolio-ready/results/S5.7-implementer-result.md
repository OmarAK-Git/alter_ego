implementation_model: composer-2.5

# S5.7 Implementer Result — empirical LLM determinism check

**Date:** 2026-07-13  
**Packet:** S5.7-implementer

## Summary

Replaced the mock `scripts/llm_determinism_check.py` (injected variance on run 10) with a real provider path via `worker.explainer.RealLLMProvider`. The script uses pinned non-alias model IDs (`claude-3-haiku-20240307` or `gpt-4o-mini`), temperature=0, and 10 identical prompts. **No live API run was performed** — `ANTHROPIC_API_KEY` and `OPENAI_API_KEY` are unset in this environment. Wrote an honest not-executed artifact to `docs/llm-determinism-check.md` that does not claim empirical results. Did **not** edit `state.json`.

## Files changed

| File | Rationale |
|------|-----------|
| `scripts/llm_determinism_check.py` | Real provider path via `RealLLMProvider`; SHA-256 hashing; honest not-executed vs executed doc writers; exit code 1 when credentials missing |
| `docs/llm-determinism-check.md` | Regenerated as **Not executed -- awaiting credentials** with re-run instructions and lineage architecture note |

## Design choices

- **Provider reuse:** Same HTTP client and model pinning as `worker/explainer.py` (Anthropic preferred, then OpenAI).
- **No mock default:** Removed `mock_llm_call` and artificial variance; script only writes empirical conclusions after live calls.
- **Missing credentials:** Exit code 1, stderr message, and artifact explicitly states **No empirical conclusion**.
- **Docs:** Include how to re-run and section 8.4 lineage note (authoritative record is immutable lineage, not provider reproducibility).

## Verification

```text
$ python -c "import os; print('ANTHROPIC:', 'set' if os.getenv('ANTHROPIC_API_KEY') else 'unset'); print('OPENAI:', 'set' if os.getenv('OPENAI_API_KEY') else 'unset')"
ANTHROPIC: unset
OPENAI: unset

$ python scripts/llm_determinism_check.py (via subprocess)
returncode 1
stderr: ERROR: No LLM API key configured. Set ANTHROPIC_API_KEY or OPENAI_API_KEY.
        Wrote honest not-executed artifact to docs/llm-determinism-check.md

$ ruff check scripts/llm_determinism_check.py
All checks passed!
```

## approval_gates

None required for this session (no API keys present; no live provider calls attempted).

To execute empirically when credentials are available:

```bash
# Set ANTHROPIC_API_KEY or OPENAI_API_KEY, then:
python scripts/llm_determinism_check.py
```

This performs 10 external API calls (network approval may be required in gated environments).

## Unresolved / verifier follow-ups

1. Empirical hash table and deterministic/non-deterministic conclusion pending a live run with credentials.
2. Verifier should confirm old mock conclusion (2 unique hashes from injected variance) is no longer present in `docs/llm-determinism-check.md`.
