# S0.1 implementer result

## Files changed

| File | Rationale |
|---|---|
| `docs/SPEC.md` | Demoted header from overstated "CALIBRATED (Audit Grade)" to honest Phase 2A / residual language with metrics pointers |
| `memory-bank/progress.md` | Updated S0.1 Notes column only (Status left `ready` for controller) |

## Exact new status lines (SPEC.md header)

```
**Status:** Phase 2A — Partial (residual risk; S2 recall 0.0)
**Scope:** Portfolio project, single-operator deployment, 8–10 week build
**Last updated:** July 2026
**Version:** 2.2 — interim operating point at Threshold = 45.0 (Phase 2 not closed)

**Metrics authority:** `memory-bank/progress.md` (phase tracker) and `docs/calibration_final_metrics.json` (saved sweep). At thr=45: global recall ~0.42; S1/S3/S4 recall 1.0; S2 (slow-roll) recall 0.0.
```

## Verification notes

- **Header grep:** No `CALIBRATED` in lines 1–10 of `docs/SPEC.md` (header/status area clean).
- **Residual body text:** §6.8 and other sections still use "calibrated" in historical/parameter context; per packet, residual mentions elsewhere are OK.
- **progress.md:** Phase 2 row still documents S2 recall 0.0 as the reason for demotion (line 24).
- **metrics JSON:** `docs/calibration_final_metrics.json` confirms global recall 0.417 (~0.42), S2 recall 0.0, S1/S3/S4 recall 1.0 @ thr=45.
- **state.json:** Not touched (per instructions).
- **Commit:** Not performed (per instructions).

## Unresolved

- §6.8 "Calibrated Parameters (Audit Grade)" section (line ~271) still overstates calibration closure; out of S0.1 write scope (header-only demotion). Deferred to S3.5 / follow-on doc hygiene packets.
