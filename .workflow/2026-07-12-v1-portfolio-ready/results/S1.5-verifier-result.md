# S1.5 Skeptic-Verifier Result

**Verdict: `survives`**

## Claim under test

> Benign / correlated-benign smokes exist and pass; attack partitions remain
> `eval_scenario_*`; no new precision/recall (P/R) claims.

## Evidence gathered (fresh, this session)

### 1. Smokes exist and pass — verified by re-running

```
python -m pytest tests/test_generator.py -v --tb=short
=> 7 passed in 1.55s
```

The two claimed smokes are present in `tests/test_generator.py` and passed:

- `test_benign_only_baseline_smoke_production_partition` (L48–57) — generates
  benign-only baseline, asserts `labels == []` and every event partition is
  exactly `{"production"}`.
- `test_correlated_benign_rollout_stays_production` (L60–79) — injects the
  correlated-benign tooling rollout, asserts 45 rollout events all tagged
  `production` and none `eval_scenario_*`.

### 2. Attack partitions remain `eval_scenario_*` — verified against source

`test_attack_partitions_remain_eval_scenario_after_s1_1` (L82–126) passes and
asserts, for a full mixed run, that all labeled attack events start with
`eval_scenario_` and none are `production`, benign events stay `production`, and
each scenario maps to its expected `eval_scenario_N`.

Cross-checked the assertions are non-vacuous against the real generator
(`batch/synthetic/generator.py`): baseline emits `simulation_partition="production"`
(L100, L114); `inject_tooling_rollout` emits `"production"` (L144, benign);
scenarios 1–4 emit `eval_scenario_1`…`eval_scenario_4` (L169, L201, L232, L261).
The tests exercise live generator code paths, not fixtures or stubs — attempts to
game these would require changing the generator itself, which still emits the
correct partitions.

### 3. No new P/R claims — verified

- `S1.5-implementer-result.md` contains no precision/recall/AUC numbers; it
  explicitly states "No precision/recall metrics were computed or claimed."
- `docs/calibration_final_metrics.json` was not modified (absent from the repo's
  changed/untracked file set); no calibration sweep artifact was refreshed.
- The added tests assert only partition/label structure, never detection quality.

## Attempts to refute (all failed)

- Weakened/vacuous assertions? No — each smoke asserts a concrete count/partition
  set and would fail if partitions were wrong (e.g. `== {"production"}` rejects
  any leaked `eval_scenario_*`).
- Tests not exercising new code? No — they call the real `EventGenerator`.
- Stale results? No — pytest was re-run this session (7 passed).
- Hidden P/R claim smuggled into metrics? No — metrics JSON untouched.

## Single strongest reason it survives

The two named smokes pass against the live generator, and the generator source
independently confirms benign→`production` / attack→`eval_scenario_N`; no P/R
metric was produced or altered. The claim is fully backed by fresh evidence.
