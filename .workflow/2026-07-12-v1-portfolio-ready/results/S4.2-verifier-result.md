verification_model: cursor-grok-4.5-high

# S4.2 Verifier Result — Explainer queue-depth + template fallback

## Claim restated

S4.2 is complete: configurable max in-flight LLM explanation jobs via scoring_config; at/over depth uses deterministic template fallback without calling the LLM provider; overflow is logged for audit and decisions/explanations are not dropped; under-capacity still uses the LLM path; tests prove overflow vs under-capacity; focused explainer pytest passes. Only new config key allowed (no anomaly_threshold / feature-weight retune).

## Verdict

**survives**

## Strongest reason

Fresh `pytest tests/worker/test_explainer.py -v --tb=short` → **8 passed**; `generate_explanation` gates on `explainer_queue_depth` with a thread-safe in-flight counter (`worker/explainer.py` ~23–51, 413–424), skips `provider.generate` on overflow while still persisting a `template_fallback` record with audit-visible `validation_notes` + `logger.warning`, and under-capacity still calls the provider — matching all six acceptance criteria. Config retains `anomaly_threshold: 45.0`, `drift_threshold: 5.0`, `features.drift_alert.weight: 100.0` with only `explainer_queue_depth: 8` added.

## Evidence gathered (independent of implementer transcript)

### Code reads

| Check | Evidence |
|---|---|
| AC1 config key | `config/scoring_config.yaml` L25–26: `explainer_queue_depth: 8`; loader `_get_explainer_queue_depth` / default `EXPLAINER_QUEUE_DEPTH_DEFAULT = 8` (`worker/explainer.py` L23–36) |
| AC2 overflow → no LLM | Gate before provider (`L413–424`); on acquire fail → `generate_template_explanation` only |
| AC3 audit + not dropped | `logger.warning(...)` L415–418; `validation_notes` names queue-depth overflow L421–423; path still `db.merge` / `commit` L496–511 |
| AC4 under capacity | Else branch acquires slot, calls `provider.generate`, releases in `finally` L426–494 |
| AC5 tests | `test_queue_overflow_uses_template_without_llm` (asserts `llm_called==0`, `template_fallback`, notes contain "queue depth"); `test_under_capacity_calls_llm_provider` (asserts `llm_called==1`, `passed`); `test_scoring_config_includes_explainer_queue_depth` |
| Weights untouched | `anomaly_threshold: 45.0`, `drift_threshold: 5.0`, feature weights incl. `drift_alert: 100.0` / rarity weights unchanged vs S3 calibrated point; new key only |

### Adversarial probes

- Overflow test synthesizes capacity via `monkeypatch` of `_inflight_llm_count` rather than real multi-thread load — still exercises the real acquire gate and provider-skip path; not a letter/intent miss for AC5.
- No `AuditLogModel` insert on overflow — AC3 explicitly allows app logger **and/or** ExplanationRecord notes; both present.
- Counter is process-local (not distributed) — out of packet scope; SPEC §8.6 asks for configurable depth + immediate template on overflow.

### Commands run

```text
python -m pytest tests/worker/test_explainer.py -v --tb=short
# 8 passed, 32 warnings in 0.64s

python -m ruff check worker/explainer.py web/api.py tests/worker/test_explainer.py
# All checks passed!
```

## Residual notes (non-refuting)

- Overflow concurrency is unit-simulated, not stress-tested under threads.
- `POST /api/alerts/{decision_id}/explain` now returns `validation_status` (`web/api.py` L126) — helpful but not required to refute/survive AC list.
