implementation_model: composer-2.5

# S5.3 Implementer Result — Postgres INSERT-only DB roles (Path A)

**Date:** 2026-07-13  
**Packet:** S5.3-implementer  
**Decision source:** S5.3-researcher-result.md (Path A)

## Summary

Shipped Alembic migration `g6h7i8j9k0l1_add_app_db_roles`, compose DSN split (owner migrations / `alter_ego_app` runtime), and honest doc updates for profile lifecycle-column UPDATE exception. Did **not** mark `state.json` done; verifier pending.

## Files changed

| File | Rationale |
|---|---|
| `alembic/versions/g6h7i8j9k0l1_add_app_db_roles.py` | Creates `alter_ego_app` role; grants INSERT-only on audit/commit tables; column-level `UPDATE (promoted_at, superseded_at)` on `profiles`; operational table grants; sequence privileges |
| `docker-compose.yml` | `web` runs `alembic upgrade head` as owner `user` (inline `DATABASE_URL` override); `web`/`worker`/`batch` runtime `DATABASE_URL` uses `alter_ego_app`; `ALTER_EGO_APP_PASSWORD` env (default `alter_ego_app_dev`) |
| `docs/SPEC.md` | §9.2 mechanism marked shipped at DB-role layer; §14 debate note resolved |
| `docs/SPEC_V3.md` | §6.3 lifecycle-column exception; §7 acceptance wording for column grants |
| `docs/deployment.md` | Role matrix, DSN split, manual INSERT-only verification recipe; removed S5.3 from "not shipped" |
| `SPEC.md` (root) | Synced §9.2 + §14 from `docs/SPEC.md` |
| `memory-bank/progress.md` | S5.3 row `doing`; overstatements register updated; Phase 0 note |

## Migration

- **Name:** `g6h7i8j9k0l1_add_app_db_roles`
- **Revises:** `f5a6b7c8d9e0`
- **Role:** `alter_ego_app` (password from `ALTER_EGO_APP_PASSWORD`, default `alter_ego_app_dev`)
- **Append-only (SELECT, INSERT; REVOKE UPDATE, DELETE):** `audit_logs`, `decisions`, `explanations`, `scoring_configs`
- **Profiles:** SELECT, INSERT; `UPDATE (promoted_at, superseded_at)` only; REVOKE DELETE — **not** table-level INSERT-only
- **Operational (SELECT, INSERT; REVOKE UPDATE, DELETE):** `events`, `resolved_events`, `containment_queue`, `eval_ground_truth`
- **Workflow:** `alert_workflow_state` — SELECT, INSERT, UPDATE; REVOKE DELETE
- **Sequences:** `audit_logs_log_id_seq`, `containment_queue_queue_id_seq` — USAGE, SELECT

## Compose changes

- `web` command prefix: `DATABASE_URL=postgresql://user:password@postgres:5432/alter_ego alembic upgrade head &&`
- Runtime `DATABASE_URL` on `web`, `worker`, `batch`: `postgresql://alter_ego_app:${ALTER_EGO_APP_PASSWORD:-alter_ego_app_dev}@postgres:5432/alter_ego`
- `ALTER_EGO_APP_PASSWORD` exposed on all three app services (for migration password sync)

## Doc banners / honesty

- **SPEC §9.2:** Describes shipped `alter_ego_app` grants; profiles explicitly lifecycle-column exception
- **SPEC §14:** Debate item resolved — shipped S5.3
- **SPEC_V3 §6.3:** Payload immutability + column-level lifecycle UPDATE only (no false table INSERT-only claim)
- **SPEC_V3 §7:** Column-grant wording for acceptance criterion
- **deployment.md:** Full grant matrix; eval harness `clear_db()` documented as requiring owner DSN

## Verification attempted

| Check | Result |
|---|---|
| `ruff check alembic/versions/g6h7i8j9k0l1_add_app_db_roles.py` | **PASS** |
| `pytest -q --ignore=tests/live` | **85 passed** (SQLite suite unaffected) |
| Full `pytest -q` | 8 failed — `tests/live/*` only (no local server on :8000; environment hits Splunk 404) — pre-existing live-smoke dependency |
| `docker compose config` | **Not run** — `docker` not available in implementer shell |
| Live `alembic upgrade` / `docker compose up` | **Skipped** per approval gate — no external/shared DB |

## Unresolved / verifier follow-ups

1. Verifier should run compose + manual psql recipe in `docs/deployment.md` when Docker/Postgres available.
2. Confirm `alter_ego_app` can connect after fresh `docker compose up -d --build` (role created on first migration).
3. Eval sweeps: document confirms owner DSN override for `batch/eval/runner.py` `clear_db()` DELETE paths — no separate `alter_ego_batch` role shipped.
