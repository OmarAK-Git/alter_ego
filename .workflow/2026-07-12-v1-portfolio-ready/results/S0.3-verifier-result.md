# S0.3 Verifier Result

**Verdict: survives**

## Claim (restated)

In write-scope files (`docs/SPEC_V3.md`, `README.md`, `CLAUDE.md`, `OPS.md`), the shipping embedding narrative is `alter-ego-ngram-v1` / 128-d char 3-gram SHA-256 hashing; `nomic` (768-d) appears only as deferred/abandoned debt, never claimed as current runtime.

## Evidence gathered (implementer report treated as unevidenced)

### Ground truth — `worker/vectorizer.py` (read directly)
- `NORMALIZER_VERSION = "1.0-char-3gram-hash-128"` (L6).
- `vectorize_command_line(cmd, dim=128)` (L32): sliding-window 3-grams (L43), `hashlib.sha256` (L46), `idx = int(h,16) % dim` (L47), L2 normalization (L50-53).
- No `nomic`, `768`, or BERT/neural embedder in the shipping code path. Confirms shipping = deterministic char 3-gram SHA-256 → 128-d unit-norm.

### Grep for `nomic|768|BERT|bert` in write scope — every hit inspected

- **`docs/SPEC_V3.md:97`** — Shipping = "128-d vectors (`alter-ego-ngram-v1`)"; "not semantic BERT similarity"; nomic labeled "**Deferred debt**", "768-d ... explored early, abandoned", with explicit "do not describe nomic as current runtime."
- **`docs/SPEC_V3.md:213`** — "**Deferred / abandoned:** `nomic-embed-text` (768-d) ... it is not what scores events today."
- **`README.md:37`** — "128-d unit-norm vector (`alter-ego-ngram-v1`; not BERT / nomic)."
- **`CLAUDE.md:61`** — "128-dim unit-norm vector (model id `alter-ego-ngram-v1`) ... Alembic/ORM still default `nomic-embed-text` — deferred S1.4 debt, not shipping runtime."
- **`OPS.md:21`** — "128-d unit-norm vectors (`alter-ego-ngram-v1`) ... not 768-d BERT / nomic; Alembic `nomic-embed-text` default is S1.4 debt."

No occurrence in any write-scope file presents nomic/768/BERT as the current runtime embedder. Every occurrence is either a negation ("not BERT/nomic") or an explicit deferred/abandoned-debt qualifier.

## Attempts to refute
- Searched for any nomic/768/BERT mention lacking a debt qualifier: none found.
- Checked shipping code independently against the docs' 128-d char-3gram claim: matches.
- Checked for a broad claim on narrow evidence: the doc statements are scoped and consistent across all four files.

## Strongest reason it survives
Direct read of `worker/vectorizer.py` independently confirms the 128-d char-3gram SHA-256 shipping path, and every `nomic`/`768`/`BERT` token in write scope is bounded by a negation or explicit deferred/abandoned-debt label.
