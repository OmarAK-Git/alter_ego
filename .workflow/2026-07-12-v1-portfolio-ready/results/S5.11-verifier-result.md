verification_model: cursor-grok-4.5-high

# S5.11 Verifier Result — Path B defer (advanced cohort gates)

**Date:** 2026-07-13  
**Packet:** S5.11-verifier  
**Claim tested:** Path B defer of advanced cohort gates + independent artifacts with honest SPEC banners.

## Verdict: **survives**

**Strongest reason:** Independently confirmed `docs/SPEC.md` §7.3 carries an explicit **v1 deferral (S5.11 / Path B)** banner (`docs/SPEC.md:317`), root `SPEC.md` is **byte-identical** to `docs/SPEC.md` (SHA-256 prefix `729c59e4a2d84a64`), production Python has **zero** `min_clean_observation_count` readers and **zero** `cohort_builder` / `CohortPrior` modules, while scoring-time novelty (`max_changed_fraction`, `cohort_gate_window_days`) is present in `worker/scorer.py` — matching the Path B ship/defer split.

---

## Method

Treat implementer/researcher transcripts as unevidenced. Independently:

1. Re-read researcher Path B ACs 1–5 and implementer write scope
2. Read SPEC §7.3 + cross-refs (§3.2 / §10.3 / §13.1), SPEC_V3 §6.6/§6.7, progress/OPS/YAML
3. Byte-compare root vs docs SPEC
4. Attack for hollow stubs, production `min_clean` readers, and dishonest “ships” claims
5. Confirm novelty gate exists in scorer without requiring scorer edits for this packet

---

## Fresh verification

```text
$ python -c "… Path('SPEC.md').read_bytes() == Path('docs/SPEC.md').read_bytes() …"
identical
root 729c59e4a2d84a64
docs 729c59e4a2d84a64

$ production *.py under worker/batch/core/web/scripts for min_clean_observation_count
NONE

$ Glob **/cohort_builder/**
0 files

$ Grep min_clean_observation_count in *.py / *.yaml
config/scoring_config.yaml:42  (comment: Deferred S5.11)
scratch/test_cohort_gate.py:85  (non-production)

$ Grep max_changed_fraction / cohort_gate_window_days in worker/scorer.py
present (novelty gate ~441–496)
```

---

## Acceptance checklist (Path B ACs)

| # | Criterion | Result | Evidence |
|---|---|---|---|
| 1 | SPEC §7.3 ▲ v1 deferral (S5.11 / Path B) banner; root SPEC synced | **pass** | Banner `docs/SPEC.md:317`; root≡docs bytes identical |
| 2 | §3.2 / Scenario 3 / §13.1 cross-refs honest | **pass** | §3.2 `:59`; Scenario 3 `:496` “v1 partial”; §13.1 `:637`/`:666` list prior-update / independent artifacts / `min_clean` as deferred |
| 3 | SPEC_V3 aligned (novelty ships; min_clean + independence deferred) | **pass** | §6.6 `:195` Path B; §6.7 `:201` novelty required / min_clean deferred |
| 4 | progress.md S5.11 done Path B; OPS one-liner; YAML comment | **pass** | `memory-bank/progress.md:175`; `OPS.md:22`; `config/scoring_config.yaml:41-42` |
| 5 | No new Python cohort infrastructure; no production `min_clean` reader | **pass** | No `cohort_builder`; no production py hits; scorer untouched for Path B |

---

## Attack attempts (did not flip verdict)

1. **Design-target bullets under §7.3 still describe prior-update rejection** (`docs/SPEC.md:319-325`) — Same Path B pattern as S2.7/S4.6: banner marks design target; bullets retained. Banner text is explicit about what does / does not ship. Not a refute.
2. **SPEC_V3 still says “deferred to §9 / S5.11”** (`:201`, `:267`) — Slightly stale pointer after Path B close, but `:195` adds “(S5.11 / Path B)” and content remains “deferred, not shipped.” Soft residual only.
3. **Hollow `cohort_builder` / fake `CohortPrior`** — Glob/grep found none.
4. **Secret production `min_clean` reader** — Only YAML + `scratch/test_cohort_gate.py`; worker/batch/core/web/scripts clean.
5. **False “novelty ships” claim** — `worker/scorer.py` reads `max_changed_fraction` / `cohort_gate_window_days` and applies novelty suppression (~441–496). Blocked-entity cohort exclusion exists in `batch/profile_builder/builder.py` (~242–260).
6. **Implementer edited scorer contrary to Path B** — Packet forbade scorer edits; no need for scorer change for docs defer; novelty already present.

---

## Residual risks (do not flip verdict)

- SPEC deferred-knob list (`docs/SPEC.md:293`) still includes `max_profile_build_block_days` as unwired despite S5.6 — **out of S5.11 write scope**; honesty debt for a later packet, not a Path B cohort-gate lie.
- SPEC_V3 §9 / S5.11 cross-refs could be tightened to “Phase 4 (S5.11 Path B closed)” for clarity.
- §7.3 body still narrates cohort recomputation gates in present tense under the banner; readers who skip the ▲ banner could over-read.

---

## Verdict summary

Packet-scoped Path B claim **survives**. Honest §7.3 deferral banner with synced root SPEC, aligned SPEC_V3 / progress / OPS / YAML comment, and codebase absence of advanced cohort infrastructure / `min_clean` production readers, while scoring-time novelty remains the shipped substitute.
