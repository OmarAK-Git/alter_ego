# S2.3 verifier result

## Verdict

**survives**

## Claim under test

`decision_id` canonical serialization includes `scorer_algorithm_version` and `embedding_model_version`; stable for same inputs; changes when either version changes.

## Evidence gathered (independent)

### Commands

```text
$ python -m pytest \
    tests/worker/test_scorer.py::test_idempotent_decision_id \
    tests/worker/test_scorer.py::test_decision_determinism \
    tests/worker/test_scorer.py::test_decision_id_matches_canonical_formula \
    tests/worker/test_scorer.py::test_decision_id_sensitive_to_algorithm_version \
    tests/worker/test_scorer.py::test_decision_id_sensitive_to_embedding_model_version \
    -v --tb=short
# 5 passed in 0.66s (fresh)

$ python -c "<independent score_event vs raw JSON SHA-256 probe>"
# SCORER_ALGORITHM_VERSION 1.0
# canonical {"embedding_model_version":"1.0","event_id":"evt_123",
#   "profile_version":"prof_v1","scorer_algorithm_version":"1.0",
#   "scoring_config_version":"1.0"}
# match True; helper True; emb_diff True
```

### Reads

| Location | Finding |
|---|---|
| `worker/scorer.py:28-51` | `SCORER_ALGORITHM_VERSION = "1.0"`; `compute_decision_id` builds sorted-key JSON with both version fields + optional `halt`, then SHA-256 |
| `worker/scorer.py:275-280` | Staleness-halt path calls `compute_decision_id(..., embedding_model_version=profile.embedding_model_version, halt=True)` |
| `worker/scorer.py:455-460` | Normal scoring path calls same helper with `profile.embedding_model_version` (no alternate ad-hoc concat) |
| `worker/scorer.py:521` | `process_unscored_events` goes through `score_event` only |
| `tests/worker/test_scorer.py:119-173` | Formula test: `score_event` id == helper == independent JSON hash; algorithm sensitivity monkeypatches module constant then re-calls `score_event`; embedding sensitivity rebuilds `ProfileArtifact` and re-calls `score_event` |

### Reward-hack checklist

| Hack | Present? |
|---|---|
| Tests call helper only; `score_event` still uses old concat | **No** — both halt + normal paths call `compute_decision_id`; formula test asserts `score_event` id |
| Monkeypatch `compute_decision_id` / fake versions without production read | **No** — algorithm test patches `SCORER_ALGORITHM_VERSION`; helper reads that global at call time |
| Embedding version in helper but not passed from `score_event` | **No** — both paths pass `profile.embedding_model_version` |
| Sensitivity asserts only on helper, not `score_event` | **No** — both sensitivity tests go through `score_event` |
| Hand-built `DecisionRecord.decision_id` | **No** |

### Non-blocking notes (do not refute packet claim)

- SPEC_V3 §6.1 also mentions normalized UTC datetimes / stable decimal floats in canonical serialization; current payload is strings (+ optional `halt` bool) only. Packet AC and this claim do not require those fields.
- Sensitivity tests assert inequality only (not recomputed expected hash after bump); covered indirectly by formula + production wiring.

## What holds

- Canonical sorted-key JSON includes `scorer_algorithm_version` (pinned module constant) and `embedding_model_version`.
- Same inputs → same `decision_id` (helper + `score_event` determinism).
- Changing algorithm constant or profile `embedding_model_version` changes `decision_id` via real `score_event` path.
- Fresh focused pytest: 5/5 passed; independent probe matched raw SHA-256 of documented canonical string.

## Strongest reason

Production `score_event` (normal + halt) hashes sorted-key JSON that includes both version fields via `compute_decision_id`; independent probe and five focused tests confirm stability and sensitivity without bypassing that path.
