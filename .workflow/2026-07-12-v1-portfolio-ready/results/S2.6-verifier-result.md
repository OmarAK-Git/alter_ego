# S2.6 skeptic-verifier result

## Claim under test

`total_volume_delta` honestly deferred/stripped from active feature claims; scorer still emits zero with explicit deferred flag; README no longer lists it as an active calibrated feature; tests prove deferred behavior.

**Packet:** `.workflow/2026-07-12-v1-portfolio-ready/packets/S2.6-implementer.md`  
**Implementer report reviewed (not treated as evidence):** `results/S2.6-implementer-result.md`

## Verdict

**survives**

## Strongest reason

Fresh `pytest -k volume` passes (`test_total_volume_delta_deferred`), and live code unconditionally hard-codes `score_vol = 0.0` / `v_delta = 0.0` with `volume_delta_deferred` and no YAML weight read (`worker/scorer.py:377-380`, `:411`). README’s feature table no longer lists volume as calibrated — only an explicit deferred footnote (`README.md:35-41`).

---

## Claim restated (precise)

1. Portfolio-facing **active** feature claims no longer present `total_volume_delta` / “Hourly volume spike” as a working calibrated scorer feature.
2. Scorer still emits a `total_volume_delta` contribution with score/raw **0** and flag **`volume_delta_deferred`** (not a silent stub).
3. README feature table excludes it; deferred status is explicit.
4. Tests encode and (claimed) pass that deferred contract.

---

## Evidence gathered

### 1. Required pytest (fresh)

```text
$ python -m pytest tests/worker/test_scorer.py -k volume -v --tb=short

tests/worker/test_scorer.py::test_total_volume_delta_deferred PASSED

====================== 1 passed, 19 deselected in 0.56s =======================
```

Exit code 0. Test source `tests/worker/test_scorer.py:119-129` asserts score/raw `0.0` and `"volume_delta_deferred" in decision.flags`. Production path is unconditional (no non-zero volume branch).

### 2. Scorer volume section

| Check | Location | Finding |
|---|---|---|
| Deferred comment | `worker/scorer.py:376-378` | Explicit S2.6 deferred note |
| Score hard-zero | `worker/scorer.py:378-379` | `score_vol = 0.0`; `v_delta = 0.0` |
| Explicit flag | `worker/scorer.py:380` | `flags.append("volume_delta_deferred")` |
| Contribution row | `worker/scorer.py:411` | Still emitted with `score_vol` / `v_delta` |
| Weight unused | grep `worker/` for `total_volume_delta` weight read | **No** `features_config.get("total_volume_delta"…)` / `weight_vol` — only hard-zero + contribution emit |
| Old flag gone | grep `volume_delta_suppressed_no_current_count` | **Absent** from live code (only historical workflow results) |

### 3. README feature table

| Check | Location | Finding |
|---|---|---|
| Active table | `README.md:35-39` | login/geo/endpoint/process, embedding, drift, periodicity only — **no** volume row |
| Deferred note | `README.md:41` | Explicit: not in calibrated path; weight reserved; contribution `0` + `volume_delta_deferred` |
| “Hourly volume spike” as active title | repo grep | **No matches** for exact `Hourly volume spike` |

### 4. Config / progress alignment

| Check | Location | Finding |
|---|---|---|
| YAML reserved | `config/scoring_config.yaml:60-62` | Comment: deferred; weight `1.0` kept unused |
| Knob inventory | `memory-bank/progress.md:241` | `defer`; always `score_vol=0.0` + `volume_delta_deferred` |
| OPS | `OPS.md:37` | Lists `total_volume_delta` weight as unwired (S2.6) |

### 5. SPEC six-feature contract (no volume overclaim)

`docs/SPEC.md:222-229` and `docs/SPEC_V3.md:143-157` list the six features as rarity×4 + embedding + service-account periodicity — **not** `total_volume_delta`. README “six-feature path” (`README.md:66`) matches that contract, not a seventh volume feature.

---

## Attacks considered

| Attack | Outcome |
|---|---|
| README still implies active volume | **Failed** — table excludes it; deferred footnote present |
| Flag rename only (behavior still stubbed silently) | **Failed** — claim allows emit-zero + explicit deferred flag; that is what code does |
| YAML weight still present ⇒ “not stripped” | **Failed** — packet allows keep weight + document deferred; comment + progress/OPS agree |
| Test is weak (single fixture) / gamed | **Weakened, not fatal** — test is narrow, but production path is unconditional; no alternate code path can score volume |
| Stale docs still show non-zero volume | **Soft residual** — `docs/phase2-self-audit.md:81` sample JSON shows `total_volume_delta` score `7.1` (historical audit narrative). Outside S2.6 write scope; does not restore README/active-table claim |
| `docs/phase1-audit-results.md:9` “No more stubs” | **Soft residual** — historical closed-sprint note; not an active README feature row |
| Fresh pytest required for “tests prove” | **Failed to refute** — 1 passed (`test_total_volume_delta_deferred`) |

---

## Acceptance mapping

| Packet / claim limb | Result |
|---|---|
| Strip/defer path (not implement) | Met |
| Stop listing as active calibrated feature in README | Met |
| Keep YAML weight **or** remove; document deferred | Met (kept + comment) |
| Contribution may remain zero with explicit flag | Met (`volume_delta_deferred`) |
| Tests prove deferred behavior | Met (fresh pytest) |

---

## Bottom line

**survives** — strongest reason: fresh volume test pass plus unconditional scorer deferral (`score_vol=0` + `volume_delta_deferred`, weight unread) and README demotion from the active feature table. Soft residual only: historical `phase2-self-audit` sample still shows a non-zero volume contribution (out of write scope).
