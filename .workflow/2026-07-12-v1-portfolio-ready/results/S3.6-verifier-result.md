# S3.6 Verifier Result

verification_model: cursor-grok-4.5-high

## Verdict: survives

## Claim (restated)

S3.6 done: written governance attestation that `scoring_config` v2.2 @ thr=45 was unchanged through S3; thr=55 not applied; standing rule restated; YAML numeric knobs unchanged (no weight retunes); `progress.md` S3.6 done; no CALIBRATED claim.

## Evidence gathered (independent; implementer transcript not trusted)

| Check | Result | Evidence |
|---|---|---|
| Written attestation + S3.1/S3.2 pointers | PASS | `docs/scoring-config-governance-s3.md` — knob table, thr=55 not-applied section, pointers to `docs/calibration_final_metrics.json`, `results/S3.1-implementer-result.md`, `results/S3.2-implementer-result.md` |
| Standing rule restated | PASS | `OPS.md` L25 standing rule; L26 S3 attestation one-liner; also restated in governance doc §Standing rule |
| YAML version / thr / weights | PASS | Parsed `config/scoring_config.yaml`: `version=2.2`, `anomaly_threshold=45.0`, `drift_threshold=5.0`, `drift_alert.weight=100.0`; feature + drift_weights match v2.2 layout. No `anomaly_threshold: 55` under `config/`. Header comments only (S3 governance note). |
| thr=55 not applied | PASS | YAML still 45.0; PR-curve thr=55 exists only as diagnostic row in `docs/calibration_pr_curve.json` (P≈0.041, R≈0.585, FP=1136); governance + operating-point docs say not applied; no ConfigStore thr=55 write in tree |
| progress.md S3.6 done | PASS | `memory-bank/progress.md` S3.6 row: `done` — `docs/scoring-config-governance-s3.md` — v2.2 @ thr=45 unchanged; thr=55 not applied |
| No CALIBRATED claim | PASS | Governance status: **Not CALIBRATED.** OPS/progress/operating-point/S3.1–S3.2 results disclaim CALIBRATED. Metrics JSON note: "not a CALIBRATED claim." |

Cross-checks vs prior verified baselines:

- S0.4 verifier: same numeric knobs (`45.0` / `5.0` / `drift_alert` 100.0 / v2.2)
- S3.2 verifier: YAML remained 45.0; thr=55 best-F1 not applied
- `docs/calibration_final_metrics.json`: `config_version: "2.2"`, `anomaly_threshold: 45.0`, `sweep: "S3.1"`

## Attempted refutations (failed)

1. **thr=55 silently promoted** — Grep `config/` for anomaly_threshold 55: no matches; live parse shows 45.0.
2. **Weight retune under cover of comments** — All feature/drift weights present and match attested v2.2 values; comment-only header added above `version`.
3. **CALIBRATED smuggled in** — Governance/OPS/progress/phase2 docs explicitly say not CALIBRATED. (JSON key `"calibrated"` is a metrics block name from S3.2, with explicit disclaimer in `note` — not an S3.6 status claim.)
4. **Missing evidence pointers** — S3.1/S3.2 implementer results and calibration JSON exist and cite thr=45 / v2.2.

Non-blocking observation (out of S3.6 acceptance scope): `progress.md` still shows S3.1=`ready` and S3.2=`todo` while later S3 rows are done — does not refute S3.6 done row.

## Strongest reason

Live YAML is still v2.2 @ `anomaly_threshold=45.0` with unchanged feature/drift weights, backed by a written attestation + OPS standing rule that explicitly records thr=55 as diagnostic-only and disclaims CALIBRATED.
