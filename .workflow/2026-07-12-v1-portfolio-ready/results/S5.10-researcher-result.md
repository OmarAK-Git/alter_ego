# S5.10 Researcher Result — Counterfactual consistency corpus + harness

**Decision: A — ship minimal corpus + pytest harness**

Counterfactuals already ship deterministically from top-3 contributions in `worker/explainer.py` (LLM never generates them). Missing: extracted helper, JSON corpus, pytest harness, docs artifact. Path B downgrade inappropriate — feature already exposed in API/UI; V3 §9 lists corpus+harness as binding; §13.1 does not defer it.

## Implementer ACs

1. Extract `build_top_k_counterfactuals(contributions, k=3)` in `worker/explainer.py`; wire into `generate_explanation`.
2. Corpus `tests/fixtures/counterfactual_corpus.json` ≥3 cases (normal, <K, empty).
3. Harness `tests/worker/test_counterfactual_consistency.py` asserts ID order, score_delta, text binding.
4. Integration test via `generate_explanation` + mock LLM.
5. `docs/counterfactual-consistency.md` documents metric + rerun command.
6. SPEC_V3 §9 mark satisfied (S5.10).
7. `pytest tests/worker/test_counterfactual_consistency.py tests/worker/test_explainer.py -v` + ruff clean.

Write scope: `worker/explainer.py`, `tests/`, `docs/`.
