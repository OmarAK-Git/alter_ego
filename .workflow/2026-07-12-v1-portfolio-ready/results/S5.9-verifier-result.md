verification_model: cursor-grok-4.5-high

# S5.9 Verifier Result — embedding/schema mismatch detection

**Date:** 2026-07-13  
**Packet:** S5.9-verifier  
**Claim tested:** Embedding/schema mismatch detection at scorer; halt on mismatch; tests pass.

## Verdict: **survives**

**Strongest reason:** Fresh focused pytest is **7 passed** (exit 0), and `score_event` early-returns a structured halt (`score=0`, empty `contributions`, `embedding_metadata_mismatch_halt`) at `worker/scorer.py:407-438` before feature scoring whenever `check_profile_embedding_metadata` reports any field mismatch against the runtime contract.

---

## Method

Treat implementer transcript as unevidenced. Independently:

1. Re-read implementer packet ACs and SPEC §5.6 / V3 §9 framing
2. Read `worker/scorer.py` gate (`check_profile_embedding_metadata`, `find_active_profiles_with_embedding_mismatch`, `score_event` halt, `process_unscored_events` audit)
3. Read `tests/worker/test_embedding_metadata_mismatch.py` + `tests/worker/conftest.py`
4. Cross-check runtime constants vs `worker/vectorizer.py` / `core/schemas/profiles.py` / builder write path
5. Re-run focused pytest (not reuse implementer log)

---

## Fresh verification

```text
$ PYTHONPATH=. python -m pytest tests/worker/test_embedding_metadata_mismatch.py -v --tb=short
============================= test session starts =============================
collected 7 items
... 7 passed in 0.49s ==============================
EXIT 0
```

---

## Acceptance checklist (packet ACs)

| # | Criterion | Result | Evidence |
|---|---|---|---|
| 1 | Detectable check: runtime vs profile embedding metadata (and/or schema version if present) | **pass** | `check_profile_embedding_metadata` (`worker/scorer.py:69-104`) compares model_id, model_version, dimensionality, normalizer, optional vector length. No `schema_version` field exists in `core/` (grep empty) — AC allows embedding-only when schema version absent. |
| 2 | Mismatch → no silent scoring; raise or structured halt | **pass** | Halt path `score_event` `:407-438` returns `DecisionRecord` with `EMBEDDING_METADATA_MISMATCH_FLAG`, per-field detail flags, `score=0.0`, `contributions=[]`. Tests: `test_model_id_mismatch_halts_scoring`, `test_normalizer_version_mismatch_halts_scoring`. |
| 3 | Matching metadata allows scoring as today | **pass** | `test_matching_metadata_allows_scoring` — halt flag absent, `score >= 0`. Gate does not fire on compatible metadata. |
| 4 | Tests cover match vs mismatch | **pass** | Pure check match/mismatch + score_event match/halt + `find_active_profiles_with_embedding_mismatch` entity filter |
| 5 | Focused pytest passes | **pass** | Fresh re-run: 7 passed, exit 0 |

---

## Attack attempts (did not flip verdict)

1. **“Schema-version” missing** — True: no `profile_schema_version` / `schema_version` in `core/`. Packet AC1 is OR (“and/or … if present”); SPEC §5.6 treats embedding metadata as part of schema versioning. Residual, not AC fail.
2. **“Startup” is warn-only** — `process_unscored_events` (`:668-677`) only logs; enforcement is per-entity in `score_event`. AC2 allows affected-entity halt; documented in implementer result. Not silent scoring.
3. **Weak match assertion** — Match test does not assert non-empty contributions (empty `features` config can yield score 0). Distinguisher is absence of halt flag vs mismatch tests that require the flag + empty contributions. Sufficient for “gate does not block.”
4. **Runtime model_id not sourced from vectorizer** — `RUNTIME_*` for id/version/dim alias `DEFAULT_*` from profiles; only normalizer comes from `NORMALIZER_VERSION`. Builder writes the same defaults + `NORMALIZER_VERSION` (`batch/profile_builder/builder.py`). Contract is coherent for shipping ngram v1; drift risk if vectorizer identity gains separate constants later — residual hygiene.
5. **Pydantic default normalizer `"1.0"` ≠ runtime** — Intentional halt for schema-default profiles until rebuild (matches implementer note). Fixture helper `COMPATIBLE_EMBEDDING_PROFILE_FIELDS` prevents unrelated tests from false-failing; does not game S5.9 assertions.
6. **Dimensionality halt not separately integration-tested** — Unit check covers dim mismatch; halt path is shared. Coverage gap only.

---

## Residual risks (do not flip verdict)

- No dedicated profile schema-version field (SPEC prose still says profiles “carry a schema version”).
- Batch startup audit warns but does not globally abort `process_unscored_events`.
- Align `DEFAULT_EMBEDDING_INPUT_NORMALIZER_VERSION` (`"1.0"`) with `NORMALIZER_VERSION` to reduce accidental incompatible defaults.
- Optionally export embedding model id/dim from `vectorizer.py` so runtime contract cannot diverge from the hashing implementation.

---

## Verdict summary

Packet-scoped claim **survives**. Embedding-metadata mismatch is detected, scoring is structurally halted (not silent), match path is unblocked, and focused pytest freshly passes 7/7.
