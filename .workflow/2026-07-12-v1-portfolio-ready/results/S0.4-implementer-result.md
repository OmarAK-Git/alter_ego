# S0.4 implementer result

**Packet:** S0.4 — Inventory `config/scoring_config.yaml` knobs → wired / wire / defer / delete  
**Date:** 2026-07-12

## Changes

| File | Rationale |
|---|---|
| `memory-bank/progress.md` | Full knob inventory table (40 YAML tunables + 1 code-only `confidence_k`); S0.4 row marked done; working-config pointer updated |
| `OPS.md` | Short operational pointer to inventory + top unwired keys (file stays under 60 lines) |

**Not touched:** `config/scoring_config.yaml` (no numeric or structural edits), `state.json`, application code.

## Inventory summary

Grep scope: `worker/`, `batch/`, `core/` (production paths); `scratch/` / `tests/` for negative evidence only.

| Label | Count | Items |
|---|---|---|
| **wired** | 25 | All core scoring thresholds, windows, cohort gate (except `min_clean_observation_count`), feature weights (read path), four of five `drift_weights`, `version` stamp |
| **wire** | 5 | `containment_threshold` → S1.3; `total_volume_delta` → S2.6; `drift_weights.geolocation` → S1.2; `min_clean_observation_count` → S2.4; `confidence_k` (code default, missing from YAML) → S2.4 |
| **defer** | 5 | `max_calendar_adjustment`, `gap_windows.*` → S4.6; `age_jitter_hours` → S4.3; `max_profile_build_block_days` → S5.6 |
| **delete** | 2 | `decay_lambdas.staleness`, `decay_lambdas.drift` — superseded/unused; strip in S2.8 |

### Notable dual-use / partial wiring

- **`drift_threshold`:** scorer uses as drift-score divisor; builder uses as accumulator alert threshold (`drift_alert` flag).
- **`geolocation_rarity` weight:** read by scorer, but profiles do not build geo histograms — effective contribution near zero until S1.2.
- **`total_volume_delta`:** contribution row emitted but score hard-coded 0 with suppression flag; YAML weight unused.

## Acceptance criteria

1. **Every tunable classified** — Done (40 YAML keys + `confidence_k` gap documented).
2. **Inventory dated 2026-07-12 in progress.md / OPS.md** — Done.
3. **No numeric values changed** — Done (YAML untouched).
4. **Deferred items mapped to later packets** — Done in inventory tables.

## Verification

| Check | Result |
|---|---|
| Read full `config/scoring_config.yaml` | 40 tunables across 8 sections |
| Grep production paths for each top-level key | See inventory; unwired keys have zero hits in `worker/`/`batch/`/`core/` |
| Grep `decay_lambdas`, `gap_windows`, `containment_threshold`, `max_profile_build_block_days`, `age_jitter_hours`, `max_calendar_adjustment` | YAML + docs only (no production reader) |
| Grep `drift_weights`, feature names, `laplace_alpha`, `cohort_gating_constants` | Confirmed readers in `worker/scorer.py` and/or `batch/profile_builder/builder.py` |
| Numeric diff on `scoring_config.yaml` | None (file not modified) |

Shell not used (per packet); verification via Read/Grep only.

## Unresolved

- `state.json` S0.4 status not updated (packet write scope excludes it).
- `core/schemas/config.py` `ScoringConfig` Pydantic model does not mirror live YAML shape — governance/schema alignment is out of scope (S3.6 / separate debt).
