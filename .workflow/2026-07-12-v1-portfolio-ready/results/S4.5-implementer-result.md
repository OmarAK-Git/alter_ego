implementation_model: composer-2.5

# S4.5 implementer result

## Summary

Added first-class `replay_run_id` across the decision schema, ORM, replay runner, and API per SPEC §6.2 / §9.3. Original decisions remain insert-only with `replay_run_id` null; replays emit new records linked by `replay_run_id` (plus legacy `replay:{id}` flag).

## Files changed

| File | Rationale |
|---|---|
| `core/schemas/decisions.py` | `DecisionRecord.replay_run_id: Optional[str] = None` |
| `core/models.py` | Nullable indexed `replay_run_id` column on `DecisionRecordModel` |
| `alembic/versions/f5a6b7c8d9e0_add_decisions_replay_run_id.py` | Migration file only (not applied to shared/prod) |
| `batch/replay_runner.py` | Set `replay_run_id` on emitted decisions; fix `score_event` call; ORM→schema helpers |
| `web/api.py` | Surface `replay_run_id` on triage, suppressed, and alert-detail decision payloads |
| `tests/batch/test_replay_runner.py` | Schema default, persistence, replay tagging, immutability of originals |
| `tests/web/test_api.py` | Alert detail returns `replay_run_id` when present / null for originals |
| `memory-bank/progress.md` | S4.5 marked done |

## Verification

```text
pytest tests/batch/test_replay_runner.py \
  tests/web/test_api.py::test_alert_detail_includes_replay_run_id_when_present \
  tests/web/test_api.py::test_alert_detail_replay_run_id_null_for_original -v --tb=short
```

**Result:** 6 passed in 0.97s (EXIT=0)

## Acceptance criteria

| # | Criterion | Status |
|---|---|---|
| 1 | `DecisionRecord` has optional `replay_run_id` | pass |
| 2 | ORM stores nullable `replay_run_id` | pass |
| 3 | `run_replay` sets `replay_run_id` on new decisions | pass |
| 4 | Original decisions have `replay_run_id` null | pass |
| 5 | API decision detail includes `replay_run_id` | pass |
| 6 | Focused pytest passes | pass |
| 7 | progress.md S4.5 done | pass |

## Notes / unresolved

- `worker/recorder.py` not in write scope; originals persist with DB-default null `replay_run_id` (verified in test).
- Alembic migration created but not executed (per packet constraint).
- `state.json` not modified (per packet constraint).
