# S1.4 Skeptic-Verifier Result

**Verdict: `survives`**

## Claim (restated precisely)
ProfileArtifact schema, `ProfileArtifactModel` ORM, and the builder promote path all default the shipping embedding metadata to `alter-ego-ngram-v1` / dimensionality `128`; an *additive* Alembic revision updates the Postgres `server_default` (without rewriting history); and `tests/test_embedding_defaults.py` passes.

## Independent evidence gathered

### 1. Pydantic schema default — CONFIRMED
`core/schemas/profiles.py:5-7,25,27`
- `DEFAULT_EMBEDDING_MODEL_ID = "alter-ego-ngram-v1"`, `DEFAULT_EMBEDDING_DIMENSIONALITY = 128`
- `ProfileArtifact.embedding_model_id` / `embedding_dimensionality` default to those constants. No `nomic` present.

### 2. ORM default — CONFIRMED
`core/models.py:5-10,54,56` imports the shared constants and applies them as column defaults on `ProfileArtifactModel`. No `nomic` present. Vector column is `Vector(128)`.

### 3. Builder promote path — CONFIRMED (real write path, not shadowed)
`batch/profile_builder/builder.py:16-23` imports `DEFAULT_EMBEDDING_*` from `core.schemas.profiles` and `NORMALIZER_VERSION` from `worker.vectorizer`. The actual `ProfileArtifactModel(...)` construction at `builder.py:334-350` (the promote/insert path, `db_session.add(db_profile)`) sets `embedding_model_id/version/dimensionality` from those constants. No local shadowing of the constants.

### 4. Alembic additive revision — CONFIRMED
`alembic/versions/e4f5a6b7c8d9_align_embedding_model_default.py`
- `revision = "e4f5a6b7c8d9"`, `down_revision = "d32eb29d44c1"`.
- `upgrade()` → `op.alter_column("profiles","embedding_model_id", server_default="alter-ego-ngram-v1", ...)`; `downgrade()` restores `nomic-embed-text`.
- Chain is a single linear head: `607152f795d1 → a2b3c4d5e6f7 → b3c4d5e6f7g8 → d32eb29d44c1 → e4f5a6b7c8d9`. `d32eb29d44c1` was the prior sole head; nothing references `e4f5a6b7c8d9` as a down_revision → it is the new sole head. History was **not** rewritten; the original `a2b3c4d5e6f7:33` (`server_default='nomic-embed-text'`) is left intact and layered over. Additive claim holds.

### 5. Grep for remaining `nomic` defaults — CLEAN in product code
`nomic` (case-insensitive) appears in **zero** of the three target product paths (`profiles.py`, `models.py`, builder promote). The only `.py` occurrences are expected/correct:
- `alembic/versions/a2b3c4d5e6f7_phase1_closeout.py:33` — the historical migration the additive revision is designed to update (must remain).
- `alembic/versions/e4f5a6b7c8d9_...py:21` — `LEGACY_MODEL_ID` used solely by `downgrade()`.
Remaining hits are all in docs/workflow reports framing nomic as historical/deferred debt.

### 6. Test run — PASS (executed independently, permissions: all)
```
$env:PYTHONPATH="."; python -m pytest tests/test_embedding_defaults.py -v --tb=short
```
Result: `3 passed, 2 warnings in 0.63s` (Python 3.13.12, pytest 9.0.3)
- `test_profile_artifact_schema_defaults_to_ngram_model` PASSED
- `test_profile_artifact_model_orm_defaults_to_ngram_model` PASSED
- `test_builder_promote_writes_ngram_embedding_metadata` PASSED

Assertions genuinely exercise the claim: they check `== "alter-ego-ngram-v1"` and `== 128` against a fresh `ProfileArtifact`, a committed+refreshed ORM row (real SQLite default resolution), and a profile produced by `build_profiles(...)`. Not gamed.

## Attempts to refute (all failed)
- **Constants shadowed in builder?** No — imported directly from `core.schemas.profiles`; the write-path construction uses them verbatim.
- **Test exercises a shadow/dead path?** No — the builder test triggers the non-shadow promote branch and reads back the persisted row via query.
- **Alembic history rewritten instead of additive?** No — original migration untouched; new head added.
- **Lingering nomic default sneaking through?** No — only historical migration + downgrade constant remain, both intentional.

## Caveats (non-refuting)
- The Alembic `server_default` change is verified by **inspection only**; it was not applied against a live Postgres (tests use SQLite ORM-side defaults, which don't exercise the SQL `server_default`). The revision content and chain are correct, but no runtime `alembic upgrade head` evidence was collected.
- Minor unrelated inconsistency: schema/ORM default `embedding_input_normalizer_version = "1.0"` while the builder writes `NORMALIZER_VERSION = "1.0-char-3gram-hash-128"`. Outside the stated claim (model_id/128) and not a defect in scope.

**Conclusion: the claim survives.**
