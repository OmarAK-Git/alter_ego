# S2.5 Verifier Result — Path B (SPEC_V3 Downgrade)

**Date:** 2026-07-13  
**Verdict:** **survives**  
**Claim under test:** SPEC_V3 and `progress.md` honestly downgrade v1 to profile-embedded `cohort_data`; independent frozen cohort-prior artifacts deferred; no fake `cohort_builder` shipped; three-step fallback still described as implemented.

**Packet:** `.workflow/2026-07-12-v1-portfolio-ready/packets/S2.5-implementer.md`  
**Inputs reviewed:** `S2.5-researcher-result.md`, `S2.5-implementer-result.md` (transcripts not treated as evidence)

---

## Claim restated (precise)

1. `docs/SPEC_V3.md` no longer presents independent frozen cohort-prior artifacts / independent cadence as an unqualified Phase-1/v1 requirement.
2. Docs explicitly state v1 = profile-embedded `features["cohort_data"]`, with independent versioned artifacts deferred.
3. No new `batch/cohort_builder` module or `CohortPrior` ORM/model was added.
4. `memory-bank/progress.md` closes S2.5 with an explicit Path B / downgrade note.
5. Three-step fallback + unsupported terminus remain described as **implemented** (not deferred away).

---

## Evidence gathered

### 1. SPEC_V3 — no remaining unqualified independence claims

Searched `docs/SPEC_V3.md` for: `frozen cohort`, `cohort-prior`, `cohort builder`, `independent cadence`, `CohortPrior`, `cohort_data`, three-step / unsupported terminus.

**Pre-downgrade phrases absent** (no matches):
- `Cohort builder writes…`
- `independent cadence`
- `Frozen cohort-prior snapshots exist.` (unqualified exit bullet)

**Current language (qualified / embedded):**

| Location | Content |
|---|---|
| §3 ~39, ~57 | Cold path / batch table: profile builder **embeds** cohort priors; no `cohort builder` module listed |
| §4 ~79 | Explicit: embeds `features["cohort_data"]`; **v1 does not ship** separate cohort-builder or independent versioned artifacts |
| §4 ~81 | Scorer reads embedded `cohort_data`; independent selection **deferred** to Phase 4 / §9 / S5.11 |
| §6.6 ~191–193 | Embedded-only + no cohort table / no `cohort_version`; independence **deferred post-v1**; fallback/novelty **implemented** |
| §7 ~263–265 | Exit: three-step + unsupported terminus **implemented**; profile-embedded priors exist; independent frozen snapshots **deferred** |

**Soft residual (does not refute):** deferral cross-refs cite `§9 / S5.11`, but §9’s portfolio-gate bullet list does **not** enumerate independent cohort artifacts. Deferral target S5.11 lives in `progress.md:170`. Cross-ref is imprecise, not a reintroduction of an unqualified v1 claim.

### 2. No fake independent cohort machinery

| Check | Result |
|---|---|
| Glob `**/cohort_builder/**` | **0 files** |
| `batch/` modules (glob) | `profile_builder`, `synthetic`, `eval`, `replay_runner` only |
| Grep `class CohortPrior` / `CohortPriorModel` / `cohort_builder` in `*.py` | **No production matches** (only workflow result docs mentioning “do not create”) |
| Grep `CohortPrior` / `cohort_prior` / `cohort_builder` in `core/` and `alembic/` | **No matches** |

Consistent with Path B: docs-only downgrade, no stub independence.

### 3. `progress.md` Path B

- Overstatements register `progress.md:55`: gap marked **done (S2.5)** — “Cohort data embedded in profiles only; SPEC_V3 downgraded (S2.5)”
- S2.5 row `progress.md:98`: **done** — `2026-07-13: Path B — SPEC_V3 downgraded; v1 = profile-embedded cohort_data; independent versioned cohort artifacts deferred to S5.11`
- S5.11 `progress.md:170`: receives the deferred independence work; note cites S2.5

### 4. Three-step fallback still described as implemented

SPEC_V3:
- §6.6 ~179–183: `entity-local -> role cohort -> unsupported terminus`
- §6.6 ~187: unsupported terminus sets `cohort_unsupported`
- §6.6 ~193: “The three-step fallback, unsupported terminus … are implemented.”
- §7 ~263: “Three-step cohort fallback and unsupported terminus are implemented.”

Runtime spot-check (supports honesty of “implemented,” not required by packet docs-only scope):
- `worker/scorer.py:209-219` — local (≥20) → role_cohort (≥100) → terminus from `profile.features["cohort_data"]`
- `worker/scorer.py:476` — `cohort_unsupported=(worst_level == "terminus")`

### 5. OPS.md (optional packet scope)

`OPS.md:22` — one-line note: profile-embedded only in v1; independence deferred (S2.5 / S5.11). Aligns with Path B.

---

## Attacks considered (failed to refute)

| Attack | Outcome |
|---|---|
| Leftover unqualified “frozen cohort-prior” / independent cadence as v1 exit | **Failed** — old phrases gone; remaining “frozen” uses are deferred or about Pydantic immutability (§4 profile models) |
| Silent progress close without Path B | **Failed** — Path B named in S2.5 notes |
| Stub `cohort_builder` / empty `CohortPrior` table shipped | **Failed** — absent |
| Downgrade accidentally deferred the three-step fallback | **Failed** — still asserted as implemented in §6.6 and §7 |
| §9 missing S5.11 bullet makes deferral dishonest | **Insufficient** — v1 exit text is clear; S5.11 tracked in progress; imprecise pointer ≠ contradictory claim |

---

## Acceptance vs packet

| # | Packet criterion | Verdict |
|---|---|---|
| 1 | No unqualified independent frozen artifacts as v1 exit | Pass |
| 2 | Explicit embedded `cohort_data` + deferral | Pass |
| 3 | Three-step + unsupported terminus still described as implemented | Pass |
| 4 | progress.md S2.5 closed with downgrade rationale | Pass |
| 5 | No new modules pretending independence | Pass |

---

## Strongest reason

**Survives:** Independent verification of `docs/SPEC_V3.md` (§3/§4/§6.6/§7), `memory-bank/progress.md` (S2.5 Path B + S5.11 deferral), and codebase absence of `batch/cohort_builder` / `CohortPrior` shows an honest docs-only downgrade with the three-step fallback still claimed (and present in `worker/scorer.py`) as implemented.

**Residual (non-blocking):** SPEC_V3 deferral pointers to “§9 / S5.11” are slightly loose because §9 does not list cohort independence; track via S5.11 in `progress.md`.
