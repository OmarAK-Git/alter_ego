# S2.5 Implementer Result — Path B (SPEC_V3 Downgrade)

**Date:** 2026-07-13  
**Decision:** B — downgrade SPEC_V3; no fake cohort_builder stubs  
**Packet:** `.workflow/2026-07-12-v1-portfolio-ready/packets/S2.5-implementer.md`

## Files changed

| File | Rationale |
|---|---|
| `docs/SPEC_V3.md` | Downgrade independent frozen cohort-prior claims to profile-embedded `cohort_data`; defer independence to §9 / S5.11 |
| `memory-bank/progress.md` | Close S2.5 with downgrade rationale; update overstatements register + S5.11 deferral |
| `OPS.md` | One-line operational note matching repo convention |

**Not touched:** `batch/profile_builder/`, ORM/alembic, scoring weights, `state.json`, no commits.

---

## Exact SPEC_V3 passages changed

### §3 Architecture — cold path (line ~39)

**Before:**
> Cold path: profile builds, cohort recomputation, drift calculation, calibration, replay, and synthetic generation.

**After:**
> Cold path: profile builds (including embedded cohort priors), drift calculation, calibration, replay, and synthetic generation.

### §3 Architecture — batch container table (line ~57)

**Before:**
> | batch | profile builder, synthetic generator, eval harness, replay runner, cohort builder |

**After:**
> | batch | profile builder (embeds cohort priors), synthetic generator, eval harness, replay runner |

### §4 Data Flow — cohort artifact steps (lines ~79–81)

**Before:**
> Cohort builder writes versioned frozen cohort-prior artifacts on an independent cadence.
>
> Scorer selects the point-in-time active profile for the event and a frozen cohort snapshot for the scoring window.

**After:**
> Profile builder embeds cohort priors in every profile artifact as `features["cohort_data"]` during each build (`primary` by role, `parent` by entity_type, `terminus` global histograms from the same `as_of` window). v1 does not ship a separate cohort-builder module or independent versioned cohort-prior artifacts.
>
> Scorer selects the point-in-time active profile for the event and reads cohort histograms from that profile's embedded `cohort_data` (entity-local → role → unsupported terminus). Independent cohort artifact selection is deferred to Phase 4 / portfolio hardening (§9, S5.11).

### §6.6 Cohort Chain — independence claims (lines ~191–192)

**Before:**
> Cohort priors are recomputed on a schedule independent of profile builds. Cohort-prior artifacts are versioned and frozen per scoring window.

**After:**
> v1 embeds cohort priors at profile-build time in `features["cohort_data"]`. The scorer consumes cohort histograms from the active profile only; there is no separate cohort artifact table and no `cohort_version` in decision lineage today.
>
> Independent cohort-prior artifacts (versioned, frozen per scoring window, recomputed on a cadence separate from profile builds) are **deferred post-v1** to Phase 4 hardening (§9 / S5.11). The three-step fallback, unsupported terminus (`cohort_unsupported`), `cohort_used` metadata, and cohort novelty gate are implemented.

**Unchanged in §6.6:** three-step fallback (`entity-local -> role cohort -> unsupported terminus`) and unsupported-terminus behavior.

### §7 Acceptance Criteria — exit gate (line ~263)

**Before:**
> Frozen cohort-prior snapshots exist.

**After:**
> Profile-embedded cohort priors exist (`cohort_data` on active profile artifacts). Independent frozen cohort-prior snapshots are deferred to §9 / S5.11.

---

## memory-bank/progress.md

- Overstatements register row: frozen cohort gap marked **done (S2.5)** with downgrade note.
- S2.5 task row: `todo` → `done` with Path B rationale (2026-07-13).
- S5.11 row: independent versioned cohort artifacts explicitly deferred here from S2.5.

## OPS.md

Added operational note: cohort priors profile-embedded only in v1; independence deferred S2.5 / S5.11.

---

## Acceptance criteria check

| # | Criterion | Status |
|---|---|---|
| 1 | SPEC_V3 no longer claims independent frozen cohort artifacts as v1 exit requirement without qualification | **Pass** |
| 2 | Explicit downgrade: v1 = profile-embedded `cohort_data`; independent artifacts deferred (Phase 4 / S5.11) | **Pass** |
| 3 | Three-step fallback + unsupported terminus remain described as implemented | **Pass** (§6.6 preserved) |
| 4 | `memory-bank/progress.md` S2.5 closed with downgrade rationale | **Pass** |
| 5 | No new code modules pretending independence | **Pass** |

## Verification

**Command (packet):** `pytest tests/batch/test_profile_builder_geo.py tests/worker/test_scorer.py -v`

**Result:** Not run — docs-only change; no code modified. Prior S1 exit gate reported pytest 53/53 green; these tests exercise profile `cohort_data` embedding and scorer cohort fallback unchanged by this packet.

---

## Deferred (out of scope for S2.5)

- `batch/cohort_builder/`, `CohortPriorArtifactModel`, alembic cohort tables
- `cohort_version` in `compute_decision_id`
- Removing duplicate `cohort_data` from profiles
- Wiring `parent` cohort into scorer fallback
