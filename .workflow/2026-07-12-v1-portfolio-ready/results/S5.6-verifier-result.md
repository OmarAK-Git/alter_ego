verification_model: cursor-grok-4.5-high

# S5.6 Verifier Result — max_profile_build_block_days supervisor escalation

**Date:** 2026-07-13  
**Packet:** S5.6-verifier  
**Claim tested:** max_profile_build_block_days supervisor escalation wired (config read; over/under threshold; default unchanged; focused pytest).

## Verdict: **survives**

**Strongest reason:** Fresh focused pytest passed **3/3** (exit 0), and independent reads show the builder loads `max_profile_build_block_days` from scoring config (YAML value still 30), emits a testable `DecisionRecord` with `profile_build_block_supervisor_escalation` only when `block_days >` threshold, and under-threshold / no-events paths behave as claimed — independent of S5.5 staleness queue wiring.

---

## Method

Treat implementer transcript as unevidenced. Independently read `batch/profile_builder/builder.py`, `config/scoring_config.yaml`, and `tests/batch/test_profile_build_block_escalation.py`; confirmed `/api/mandatory-escalations` remains staleness-only (`web/api.py`); re-ran scoped pytest this session (via shell agent after parent Shell backend failure).

---

## Fresh verification command

```text
$ $env:PYTHONPATH='.'; python -m pytest tests/batch/test_profile_build_block_escalation.py -v --tb=short
tests/batch/test_profile_build_block_escalation.py::test_build_block_under_threshold_no_supervisor_escalation PASSED
tests/batch/test_profile_build_block_escalation.py::test_build_block_over_threshold_emits_supervisor_escalation PASSED
tests/batch/test_profile_build_block_escalation.py::test_build_block_escalation_without_events_when_over_threshold PASSED
======================== 3 passed, 3 warnings in 0.99s ========================
EXIT=0
```

---

## Acceptance checklist

| # | Criterion | Result | Evidence |
|---|---|---|---|
| 1 | Builder reads `max_profile_build_block_days` from scoring config | **pass** | `builder.py:144-155` loads YAML then `config.get("max_profile_build_block_days", 30)`; value passed into `_emit_build_block_supervisor_escalations` (`:200-204`, `:478-482`) |
| 2 | Over-threshold blocked entity emits testable supervisor-escalation signal | **pass** | Emit path writes `DecisionRecord` with `BUILD_BLOCK_SUPERVISOR_ESCALATION_FLAG`, `block_days`, threshold, `sla_hours=24` (`builder.py:72-131`); over-threshold test asserts one flagged decision (`test_profile_build_block_escalation.py:131-149`) |
| 3 | Under-threshold does not escalate | **pass** | Guard `if block_days <= max_block_days: continue` (`builder.py:88-89`); under-threshold test expects empty escalations (`:108-120`) |
| 4 | Tests cover over vs under threshold | **pass** | 15-day under / 35-day over (+ no-events over) in focused suite |
| 5 | Numeric default unchanged | **pass** | `config/scoring_config.yaml:33` still `max_profile_build_block_days: 30` (comment-only change) |
| 6 | Focused pytest passes | **pass** | Re-run this session: **3 passed**, exit 0 |

---

## Attack attempts (did not refute claim)

1. **SPEC §5.5 wants a supervisor review queue; API is staleness-only** — `/api/mandatory-escalations` filters only `STALENESS_ESCALATION_FLAG` (`web/api.py:109-134`). Packet AC2 explicitly allows flag/log/returned list/audit; implementer chose auditable `DecisionRecord`. SPEC queue gap is residual, not a packet-AC miss.
2. **Tests do not mutate YAML to prove the key is read** — Could pass with a hard-coded 30. Code inspection shows real `config.get(...)` from loaded YAML; AC1 is satisfied by code + default-unchanged check.
3. **No exact-boundary test at 30 days** — `<=` means equality does not escalate; SPEC “when exceeded” matches. Coverage gap only.
4. **No cleared-alert non-escalation test** — Cleared states are outside `ACTIVE_ALERT_STATES` (`builder.py:28`), so cleared entities are not in `blocked_alert_rows`. Untested but consistent with existing block filter.
5. **Repeated builds re-emit escalations (no dedup)** — Each `build_profiles` run can insert another decision. Packet does not require idempotency; observability noise, not AC failure.
6. **Coupling to S5.5** — Build-block path lives in profile builder with a distinct flag; scorer/mandatory-queue paths untouched. Independence holds.

---

## Residual risks (do not flip verdict)

- No consumer surfaces build-block escalations in a supervisor queue UI/API (SPEC intent broader than packet ACs).
- Escalation records can accumulate on every profile build while the alert remains uncleared.
- `datetime.utcnow()` deprecation warnings in builder (`:199`, `:279`).
- Docs/SPEC deferred-knob inventory may still list the key as unread (stale docs, not runtime).

---

## Verdict summary

Packet-scoped claim **survives**. Do not treat SPEC review-queue wording as satisfied beyond the auditable DecisionRecord signal documented by the implementer.
