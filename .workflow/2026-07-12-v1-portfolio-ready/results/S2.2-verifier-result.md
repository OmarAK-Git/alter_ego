# S2.2 verifier result

## Verdict

**survives**

## Claim under test

Evidence-binding reconstruction of `raw_score` from contributions passes within tolerance; uses real `score_event` `DecisionRecord`; covers multiple non-zero contributions (including hour/geo/endpoint) and a damping path where `decision.score` actually diverges from `Σ contribution_score`.

## Evidence gathered (independent, post-fix)

### Commands

```text
$ python -m pytest tests/worker/test_evidence_binding.py -v --tb=short
# 2 passed in 0.61s (fresh)

$ python -c "<probe score_event fixtures from test module>"
# UNDAMPED (total_events=200, anomaly_threshold=75):
#   non_zero: login_hour_rarity=20, geolocation_rarity=20, endpoint_set_rarity=20,
#             process_name_rarity=20, drift_alert=20
#   sum=100.0 == decision.score=100.0; no low_confidence_damping_applied
#   hour/geo/endpoint subset present: True
# DAMPED (total_events=5, anomaly_threshold=45):
#   sum=100.0, cap=40.0, decision.score=40.0
#   sum > cap: True; score == cap: True; score != sum: True
#   flag low_confidence_damping_applied present; confidence≈0.333 < floor 0.6
#   DIVERGENCE CONFIRMED: True
```

### Reads

| Location | Finding |
|---|---|
| `tests/worker/test_evidence_binding.py:121-160` | Both tests call `score_event`; undamped asserts hour/geo/endpoint ⊆ firing and `sum == score`; damped asserts `sum > cap`, flag set, `score == cap`, `\|score - sum\| > TOLERANCE` |
| `tests/worker/test_evidence_binding.py:57-73` | Fixture uses `laplace_alpha=0.0001` and rarity weights 10.0 so rarity contributions clear centering and hit scale max |
| `worker/scorer.py:367,400-406` | `raw_total = Σ feature scores`; damping sets `total_score = min(threshold - 5, raw_total)` + flag |

### Reward-hack checklist

| Hack | Present? |
|---|---|
| Hand-built `DecisionRecord` (not from `score_event`) | **No** |
| Loosened tolerance (> 1e-6) | **No** — `TOLERANCE = 1e-6` |
| Single-contribution only / vacuous multi-nonzero | **No** — five non-zero features including hour/geo/endpoint |
| Damping flag without score divergence | **No** — live probe: score 40 ≠ sum 100 |
| Equality escape hatch on damping assert | **No** — strict `abs(score - reconstructed) > TOLERANCE` |

## Prior refutation status

Previous verdict **refuted** because shipped damping fixture had `sum ≈ 20 < cap 40`, so `min(cap, sum) == sum` and score never diverged. Post-fix fixture produces `sum=100 > cap=40`; that failure mode is gone.

## What holds

- Undamped: `Σ contribution_score == decision.score` within `1e-6` via real `score_event`.
- Multiple clearly non-zero contributions including hour, geo, and endpoint (each 20.0 after contribution scale max).
- Damped: flag set, `decision.score == anomaly_threshold - 5 == 40 < Σ contribution_score == 100`.
- Documented invariant matches scorer aggregation (weight folded into `contribution_score`; `confidence_weight` not multiplied into sum).

## Strongest reason

Independent probe confirms the previously vacuous damping path now diverges: `Σ=100`, `cap=40`, `score=40` with `low_confidence_damping_applied`, and undamped hour/geo/endpoint contributions are non-zero; pytest 2/2 pass on fresh run.
