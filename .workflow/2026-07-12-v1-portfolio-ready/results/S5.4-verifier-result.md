verification_model: cursor-grok-4.5-high

# S5.4 Verifier Result — Audit hash-chain integrity check

**Date:** 2026-07-13  
**Packet:** S5.4-verifier  
**Claim tested:** S5.4 complete — scheduled/callable audit hash-chain integrity check with tests.

## Verdict: **survives**

**Strongest reason:** Fresh `pytest tests/test_audit.py -v --tb=short` passed (4/4), and static reads show `verify_audit_log_chain` + `batch.audit_integrity` implement ordered chain linkage checks, structured pass/fail, broken-chain detection, optional decision-count mismatch, CLI exit-1 / raise failure signals, and healthy/broken tests — matching all task-scoped ACs.

---

## Method

Treat implementer transcript as unevidenced. Independently read `core/models.py`, `batch/audit_integrity.py`, `tests/test_audit.py`; re-ran pytest this session. Live `python -m batch.audit_integrity` against Postgres not executed (not required by AC6; CLI path is present in code and the module is imported by the passing tests).

---

## Fresh verification command

```text
$ PYTHONPATH=. python -m pytest tests/test_audit.py -v --tb=short
tests/test_audit.py::test_decision_record_is_insert_only PASSED
tests/test_audit.py::test_audit_chain_integrity_healthy_chain_passes PASSED
tests/test_audit.py::test_audit_chain_integrity_broken_chain_fails PASSED
tests/test_audit.py::test_audit_chain_integrity_decision_count_mismatch_fails PASSED
4 passed, 7 warnings in 0.57s
PYTEST_EXIT=0
```

---

## Acceptance checklist

| # | Criterion | Result | Evidence |
|---|---|---|---|
| 1 | Walks audit_logs; verifies `previous_log_hash` linkage; structured pass/fail | **pass** | `core/models.py:162-198` — ordered walk, compare to prior `compute_hash()`, `AuditIntegrityResult` / `to_dict()`; batch loads ordered by `log_id` (`batch/audit_integrity.py:40-42`) |
| 2 | Detects broken chain; optional decision-count mismatch | **pass** | Broken: tests tamper `previous_log_hash` → `ok is False` + break reason (`tests/test_audit.py:86-104`). Count mismatch when `RECORD_DECISION` present and decisions=0 (`tests/test_audit.py:107-114`; `models.py:183-187`) |
| 3 | Runnable as `python -m batch.audit_integrity` (or equivalent) | **pass** | `batch/audit_integrity.py:56-70` — `main()` + `sys.exit`; `pyproject.toml:39-40` includes `batch*`; tests import `batch.audit_integrity` successfully |
| 4 | Non-silent failure (exit code / raise) | **pass** | Exit 1 when `not result.ok` (`audit_integrity.py:62-63`); `AuditIntegrityError` when `raise_on_failure=True` (tested `:102-104`); ERROR log + JSON stdout |
| 5 | Tests: healthy pass, broken fail | **pass** | `:70-83` healthy; `:86-104` broken |
| 6 | Fresh `pytest tests/test_audit.py` passes | **pass** | Re-run this session: 4 passed, exit 0 |

---

## Attack attempts (did not refute claim)

1. **Tests only exercise helpers, not CLI exit path** — AC4 allows raise *or* exit; raise path is asserted; `main()` exit-1 is present. Not an AC miss.
2. **No top-level `batch/__init__.py`** — setuptools `batch*` + successful pytest import of `batch.audit_integrity` refute “module not package.”
3. **Count check skipped when no decision-audit rows** — intentional (`count_check_skipped` when `decision_audit_count == 0`); AC2 marks count mismatch optional.
4. **SPEC §9.2 “sensor-health incident” wording** — packet AC4 softens to exit/raise/structured signal; no sensor-health store exists in v1 (§6.5 deferral). Packet-scoped claim still holds; residual honesty gap vs full SPEC prose.
5. **Last-row content-only tamper** — classic hash-chain limit (nothing points at last hash). AC1 requires linkage verification, which is implemented; SPEC already scopes superuser/full WORM out.

---

## Residual risks (do not flip verdict)

- Live Postgres CLI / DSN wiring not re-run here.
- No cron/compose schedule wiring (callable + suitable for cron only).
- Production path may skip count check until decision recording emits `RECORD_DECISION` / `DECISION_RECORDED` audit rows.
- SPEC still says “sensor-health incident” without a dedicated incident type — ops signal is exit/log/JSON.

---

## Conclusion

Task-scoped acceptance criteria are met with fresh pytest evidence. Claim **survives**.
