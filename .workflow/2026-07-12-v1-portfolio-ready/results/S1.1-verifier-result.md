# S1.1 Verifier Result

**Verdict: SURVIVES**

## Claim
S2/S3 attack injections use `eval_scenario_2` / `eval_scenario_3` (not `production`); tests prove it.

## Evidence gathered (independent)

### 1. Injector source (`batch/synthetic/generator.py`)
- `inject_scenario_2_slow_roll` (L185): every injected event sets `simulation_partition="eval_scenario_2"` (L201). Emits 7 days × 5 bursts = **35** events, all labeled malicious.
- `inject_scenario_3_coordinated` (L217): every injected event sets `simulation_partition="eval_scenario_3"` (L232). Emits 3 targets × 3 steps × 5 bursts = **45** events, all labeled malicious.
- No `production` partition appears anywhere inside either attack injector.

### 2. Tests (`tests/test_generator.py`)
- `test_scenario_2_attack_events_use_eval_partition`: filters events by injected label ids, asserts `len == 35` AND `all(... == "eval_scenario_2")`.
- `test_scenario_3_attack_events_use_eval_partition`: asserts `len == 45` AND `all(... == "eval_scenario_3")`.
- Non-vacuity confirmed: `generate_baseline` returns `labels = []` (never appended, L61/L125), so `attack_event_ids` contains only injected malicious ids. The exact count assertions (35/45) prevent the `all(...)` from passing vacuously on an empty list.
- `test_baseline_events_remain_production_partition` guards the inverse: baseline stays `production`.

### 3. Test run (`pytest tests/test_generator.py -v --tb=short`, run with permissions all)
```
4 passed in 0.88s
```
All four tests PASSED. Evidence is fresh (run against current source).

### 4. Grep for remaining production partition on attack injectors
`simulation_partition=` occurrences:
- L100, L114 — baseline (benign, correct)
- L144 — tooling rollout (benign, correct)
- L169 — eval_scenario_1
- L201 — eval_scenario_2 (attack injector) ✓
- L232 — eval_scenario_3 (attack injector) ✓
- L261 — eval_scenario_4

No `production` partition remains on the scenario_2 or scenario_3 attack injectors.

## Attempts to refute (all failed)
- **Vacuous `all()`?** No — exact count assertions (35/45) + non-empty label sets.
- **Baseline labels leaking into attack_event_ids?** No — `generate_baseline` returns empty labels list.
- **Stale evidence?** No — tests run against current source in this verification.
- **Letter-vs-intent?** The claim is precisely about partition tagging of injected attack events; both source and tests confirm the intended `eval_scenario_*` tagging.

## Conclusion
The claim survives adversarial verification. Attack injectors S2/S3 tag events with `eval_scenario_2` / `eval_scenario_3`, no production partition remains on them, and the tests genuinely prove this (non-vacuous, passing, fresh).
