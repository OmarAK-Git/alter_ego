# S0.3 implementer result

**Packet:** S0.3 — Lock embedding narrative to runtime truth (`alter-ego-ngram-v1` / 128-d)  
**Date:** 2026-07-12

## Changes

| File | Rationale |
|---|---|
| `docs/SPEC_V3.md` | §3 hot path: removed “local embedding generation” ambiguity; §5 tech stack: replaced nomic-as-shipping with ngram/128-d + deferred nomic debt; §6.8: added locked shipping values matching `worker/vectorizer.py` and `batch/profile_builder/builder.py` |
| `README.md` | Feature table row now names `alter-ego-ngram-v1`, SHA-256, 128-d; explicitly not BERT/nomic |
| `CLAUDE.md` | Added model id `alter-ego-ngram-v1`; noted Alembic/ORM `nomic-embed-text` as S1.4 debt |
| `OPS.md` | Operational note aligned to `alter-ego-ngram-v1` / 128-d; nomic framed as schema debt |
| `memory-bank/progress.md` | S0.3 Notes column only — records narrative lock completion |

## Acceptance criteria

1. **Shipping narrative says ngram / `alter-ego-ngram-v1` / 128-d** — Done in all write-scope files that describe runtime embedding.
2. **Nomic framed as deferred/abandoned, not current runtime** — Done. Every nomic/768 mention is explicitly debt, historical default, or “not what scores today.”
3. **README/CLAUDE/OPS consistent with `worker/vectorizer.py`** — Done. Char 3-gram SHA-256, 128-d unit-norm, normalizer `1.0-char-3gram-hash-128`.
4. **No code or DB default changes** — Done. `core/models.py`, alembic, scoring weights untouched.

## Verification

| Check | Result |
|---|---|
| Grep write-scope for `nomic` | Only deferred/debt framing in SPEC_V3 §5/§6.8, CLAUDE, OPS, README (“not … nomic”) |
| Grep write-scope for `768` / `BERT` | Only negation or abandoned-context (not claimed as shipping) |
| Grep write-scope for `alter-ego-ngram-v1` / `128` | Present in SPEC_V3 §5/§6.8, README, CLAUDE, OPS |
| Cross-check vs `worker/vectorizer.py` | `dim=128`, `NORMALIZER_VERSION = "1.0-char-3gram-hash-128"`, SHA-256 3-gram hash, L2 norm |
| Cross-check vs `batch/profile_builder/builder.py` | `embedding_model_id="alter-ego-ngram-v1"`, `embedding_dimensionality=128` |

Shell not used (may be broken per packet); verification via Grep/Read only.

## Unresolved

- `memory-bank/progress.md` S0.3 Status column left `todo` (packet: do not mark packet done in `state.json`).
- Alembic / ORM / test fixtures still default `nomic-embed-text` — owned by S1.4, not this packet.
