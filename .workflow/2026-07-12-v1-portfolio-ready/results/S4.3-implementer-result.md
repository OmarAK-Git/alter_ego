implementation_model: composer-2.5

# S4.3 Implementer Result — Suppressed-decisions view (Path B defer)

## Decision implemented

**B — Defer** aging escalation + jitter per SPEC §13.1. Wired `confidence_floor` partition so triage vs suppressed is honest.

## Changes

| File | Rationale |
|---|---|
| `web/api.py` | Import `load_scoring_config`; `_get_confidence_floor()` helper; `/api/alerts` filters `confidence >= floor`; `/api/suppressed` filters `confidence < floor` (removed hardcoded 0.5) |
| `tests/web/test_api.py` | Partition tests (high→triage only, low→suppressed only) + cleared exclusion from both |
| `config/scoring_config.yaml` | Inline defer comment on `age_jitter_hours` (SPEC §11.4 jitter not wired) |
| `memory-bank/progress.md` | S4.3 marked done; `confidence_floor` reader extended to `web/api.py`; `age_jitter_hours` + `suppressed_decision_aging_days` documented as defer |

## Verification

```
pytest tests/web/test_api.py -v --tb=short
6 passed in 1.02s
```

```
ruff check web/api.py
(clean)
```

## Acceptance criteria

1. ✅ `/api/suppressed` uses config `confidence_floor` (0.6), not hardcoded 0.5
2. ✅ `/api/alerts` excludes low-confidence anomalies; no overlap with suppressed (unless cleared)
3. ✅ Partition + cleared-exclusion tests pass
4. ✅ `progress.md` records view ships; aging escalation + jitter deferred per §13.1
5. ✅ Focused web/API tests pass; ruff clean

## Deferred (out of scope)

- Aging indicators / pending-duration UI
- Auto-escalation of aged suppressed decisions into triage
- `±age_jitter_hours` deterministic jitter scheduling
- `suppressed_decision_aging_days` YAML knob (not in runtime config)
