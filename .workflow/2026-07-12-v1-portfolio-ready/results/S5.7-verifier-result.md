verification_model: cursor-grok-4.5-high

# S5.7 Verifier Result — empirical LLM determinism harness (real, not mock)

**Date:** 2026-07-13  
**Packet:** S5.7-verifier  
**Claim tested:** Empirical LLM determinism harness uses real provider path (not mock); honest artifact if not executed; pinned non-alias model.

## Verdict: **survives**

**Strongest reason:** Independent full-file reads show `scripts/llm_determinism_check.py` has no mock/injected-variance path and only calls `worker.explainer.RealLLMProvider.generate` after credential checks; `docs/llm-determinism-check.md` is **Not executed -- awaiting credentials** with **No empirical conclusion** and contains none of the old mock “2 unique hashes / is deterministic” conclusion language.

---

## Method

Treat implementer transcript as unevidenced. Independently read:

- `scripts/llm_determinism_check.py` (full)
- `docs/llm-determinism-check.md` (full)
- `worker/explainer.py` `RealLLMProvider` (`:100-163`)
- implementer ACs in `packets/S5.7-implementer.md`

Attempted fresh `python scripts/llm_determinism_check.py` this session: **Shell backend returned no exit status / produced no capture file** (same parent-shell failure mode noted in S5.6). Verdict rests on static evidence of script + artifact, which is the load-bearing surface for this claim.

---

## Acceptance checklist

| # | Criterion | Result | Evidence |
|---|---|---|---|
| 1 | Real provider call path (match explainer / document env) | **pass** | Imports `RealLLMProvider` (`scripts/llm_determinism_check.py:22`); loop calls `provider.generate(..., temperature=TEMPERATURE)` (`:146-147`); Anthropic/OpenAI HTTP path lives in `worker/explainer.py:100-163`; env vars documented in artifact how-to |
| 2 | Pinned non-alias model id (no `-latest` / alias-only) | **pass** | Anthropic `claude-3-haiku-20240307`; OpenAI `gpt-4o-mini` — same IDs as `RealLLMProvider` (`explainer.py:106,111`); no `-latest` suffixes; docs state non-alias rule (`docs/llm-determinism-check.md:31`) |
| 3 | temperature=0; N=10; hash; conclude | **pass** | `TEMPERATURE = 0.0`, `RUNS = 10` (`:28-29`); SHA-256 via `_sha256_hex` (`:33-34`); executed writer concludes byte-identical vs N unique hashes (`:91-116`) |
| 4 | No mock-as-default / honest not-executed artifact | **pass** | No `mock_llm_call`, `random`, or injected variance in script; missing key → `_write_not_executed_artifact` + stderr + return `1` (`:129-136`); current doc status/conclusion are not-executed / no empirical conclusion (`docs/llm-determinism-check.md:3,13,17`) |
| 5 | Prefer live run if credentials present | **pass (conditional)** | Code path runs live when `api_key` and `provider_type` set (`:138-158`). Could not re-probe env this session (shell broken; key-presence probe blocked by auto-review). Artifact + implementer report are consistent with no keys; **no fake executed table present** |
| 6 | Docs: re-run + lineage still holds if non-det | **pass** | How-to (`docs/llm-determinism-check.md:19-31`); architecture note that lineage is authoritative regardless (`:33-35`) |

---

## Mock variance specifically checked

| Old mock smell | Present? | Evidence |
|---|---|---|
| `mock_llm_call` / injected variance on run 10 | **No** | Full script grep: only “variance” is architecture-note prose (`:53-54`) |
| Doc conclusion “2 unique hashes” / fake deterministic claim | **No** | Doc has no hash table, no “unique outputs”, no “is/is NOT byte-identical”; conclusion is **No empirical conclusion** (`:15-17`) |
| Status: Executed without live run | **No** | Status line is `Not executed -- awaiting credentials` (`:3`) |

---

## Attack attempts (did not refute claim)

1. **Could StubLLMProvider silently backstop?** — Script imports only `RealLLMProvider`; no stub fallback. Without keys it never calls `generate`.
2. **Could executed-writer claim determinism without hashes?** — Only reachable after N live `generate` calls populate `unique_hashes`. Current on-disk artifact is the not-executed writer output, not that path.
3. **`gpt-4o-mini` is a rolling OpenAI pointer** — Weaker pin than dated Anthropic ID. Packet AC2 bars `-latest` / alias-only and requires matching explainer; that bar is met. Residual SPEC hygiene, not packet failure.
4. **`memory-bank/progress.md` still lists “Mock LLM with injected variance”** — Stale inventory row; does not restore mock code in the harness.
5. **No fresh process exit-code evidence this session** — Shell unavailable. Does not overturn file-level proof that mock path and fake empirical conclusion are gone.

---

## Residual risks (do not flip verdict)

- Empirical hash table / deterministic-vs-not conclusion still pending a live credentialed run (explicitly honest gap).
- OpenAI path uses non-dated `gpt-4o-mini` (mirrors explainer; consider dated snapshot later).
- Could not independently re-confirm exit code 1 in this verifier process.

---

## Verdict summary

Packet-scoped claim **survives**. Mock injected-variance conclusion is gone from both script and `docs/llm-determinism-check.md`; the artifact does not claim empirical determinism without a real provider run.
