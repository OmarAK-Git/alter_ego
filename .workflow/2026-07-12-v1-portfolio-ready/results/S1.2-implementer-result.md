# S1.2 Implementer Result

## Summary

Built `geolocations` histograms into profile artifacts and cohort data, and wired geolocation into drift KL using existing `drift_weights.geolocation`. Scorer already consumed `geolocations` via `_get_cohort_histogram`; no scorer changes required.

## Files changed

| File | Change |
|------|--------|
| `batch/profile_builder/builder.py` | Export `geolocation` in JSONL; add `histogram(geolocation)` to cohort/hist/recent DuckDB queries; persist `geolocations` in profile features; include `geolocation` in drift `deltas` KL sum |
| `tests/batch/test_profile_builder_geo.py` | New: geo histogram presence + drift when geo distribution shifts |
| `tests/batch/profile_builder/builder.py` | Assert `geolocations` populated from auth event geo field |

## Verification commands

```bash
pytest tests/batch/test_profile_builder_geo.py tests/batch/profile_builder/builder.py -v --tb=short
pytest tests/batch/ -v --tb=short
```

### Outcome

```
# Focused
3 passed, 4 warnings in 0.88s

tests/batch/test_profile_builder_geo.py::test_profile_builder_populates_geolocation_histogram PASSED
tests/batch/test_profile_builder_geo.py::test_drift_includes_geolocation_when_distributions_differ PASSED
tests/batch/profile_builder/builder.py::test_profile_builder_aggregates_correctly PASSED

# Batch suite
4 passed, 8 warnings in 2.73s
```

## Acceptance criteria

1. ✅ Profile build populates a geolocation histogram from auth event geolocation fields
2. ✅ Drift KL / cumulative drift includes geolocation via `drift_weights.geolocation`
3. ✅ Tests cover geo in profile artifact and drift path when distributions differ
4. ✅ Focused pytest evidence captured above

## Unresolved

None.
