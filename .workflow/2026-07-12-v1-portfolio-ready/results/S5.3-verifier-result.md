verification_model: cursor-grok-4.5-high

# S5.3 Verifier Result — Postgres INSERT-only DB roles (Path A)

**Date:** 2026-07-13  
**Packet:** S5.3-verifier  
**Claim tested:** S5.3 Path A complete — app role INSERT-only on audit/commit tables; profiles lifecycle-column UPDATE only; compose owner-vs-app split; SPEC honesty.

## Verdict: **survives**

**Strongest reason:** Independently read artifacts match all task-scoped acceptance criteria: migration `g6h7i8j9k0l1` creates `alter_ego_app` with true INSERT-only on append-only tables, column-level `UPDATE (promoted_at, superseded_at)` on `profiles` (not table INSERT-only), compose owner migration vs app runtime DSN split, and SPEC / SPEC_V3 / deployment language that does not falsely claim full INSERT-only on profiles. Root `SPEC.md` is byte-identical to `docs/SPEC.md`.

---

## Method

Treat implementer transcript as unevidenced. Static reads of migration, compose, SPEC banners, deployment matrix; migration revision chain; SPEC hash compare. Fresh `ruff` / `pytest` / live Postgres **not** obtained this session (shell backend unavailable after SPEC hash). SQLite pytest would not exercise Postgres roles anyway (`docs/deployment.md`).

---

## Acceptance checklist

| # | Criterion | Result | Evidence |
|---|---|---|---|
| 1 | App role INSERT-only on `audit_logs` (+ append-only as claimed) | **pass** | `alembic/versions/g6h7i8j9k0l1_add_app_db_roles.py:23-28,65-68` — `audit_logs`, `decisions`, `explanations`, `scoring_configs`: `GRANT SELECT, INSERT` + `REVOKE UPDATE, DELETE` |
| 2 | `profiles`: lifecycle-column UPDATE only | **pass** | Same file `:73-78` — `GRANT SELECT, INSERT`; `REVOKE UPDATE`; `GRANT UPDATE (promoted_at, superseded_at)`; `REVOKE DELETE` |
| 3 | Compose: migrations as owner; runtime app DSN | **pass** | `docker-compose.yml:23-30,47-48,59-60` — web startup `DATABASE_URL=…user… alembic upgrade head &&`; env `DATABASE_URL` for web/worker/batch uses `alter_ego_app` |
| 4 | Docs honest (V3 exception; §9.2 shipped; deployment matrix) | **pass** | `docs/SPEC_V3.md:139,259` — explicit **not** full table INSERT-only; `docs/SPEC.md:445-447` + root twin — shipped S5.3 + lifecycle exception; `docs/deployment.md:121-145` role matrix |
| 5 | Root SPEC synced | **pass** | SHA256 both files `1a59ec1fa97ec995f1604e115397960fad5d18c1662962a80102464b80fe4782` (IDENTICAL) |
| 6 | No false full INSERT-only claim on profiles | **pass** | SPEC_V3 §6.3 / deployment matrix / SPEC §9.2 all state lifecycle-column exception |
| 7 | Migration chains from head; ruff/pytest if re-run | **pass (chain)** / **inconclusive (ruff/pytest)** | `down_revision = f5a6b7c8d9e0`; linear chain `607152f795d1→…→f5a6b7c8d9e0→g6h7i8j9k0l1`. Fresh ruff/pytest not re-run (shell unavailable). Implementer claims unevidenced here. |

---

## Attack attempts (did not refute claim)

1. **Explainer / ingest `merge()` vs INSERT-only** — `worker/explainer.py:510` and `worker/ingest.py:34,64` use SQLAlchemy `merge` (UPDATE-capable). Under shipped grants, replay/re-ingest UPDATE paths fail. That is consistent with INSERT-only intent, not a missing grant relative to Path A acceptance. First-write INSERT still works. Residual ops caveat, not AC failure.
2. **Compose race** — `worker`/`batch` `depends_on` postgres only, not `web` migration completion. First boot can fail connecting as `alter_ego_app` before role exists. Real residual risk; AC3 only requires DSN/owner split wiring, which is present. Live compose not required by packet.
3. **SPEC §9.2 “broader grants” wording** — lists operational tables as broader; matrix shows only `alert_workflow_state` gains UPDATE; others are also SELECT+INSERT. Mild imprecision; deployment matrix is precise; not a false profiles INSERT-only claim.
4. **Sequences** — only `audit_logs_log_id_seq` and `containment_queue_queue_id_seq` need serial privileges (`core/models.py`); migration grants those (`:37-40,83-84`). Other PKs are strings.
5. **Live role enforcement** — not run (approval / no docker). Documented as manual in `docs/deployment.md:149-175`. Packet allows skip.

---

## Residual risks (do not flip verdict)

- Fresh `ruff check` / `pytest --ignore=tests/live` not captured this session.
- No live `psql` proof of REVOKE/column UPDATE behavior.
- Worker/batch vs web migration race on cold `compose up`.
- Eval `clear_db()` DELETE requires owner DSN (documented).

---

## Conclusion

Path A artifacts satisfy the stated acceptance criteria. Claim **survives**. Do not treat as live Postgres-enforcement proof.
