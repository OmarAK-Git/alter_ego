# S0.1 skeptic-verifier result

## Verdict: survives

The claim — "SPEC.md no longer overclaims CALIBRATED at the header; status is Phase 2A / residual with pointers to progress.md and calibration_final_metrics.json" — survives adversarial verification.

## Claim restated

Packet S0.1: demote `docs/SPEC.md` header from `CALIBRATED (Audit Grade)` to honest Phase 2A / residual language with metrics pointers. Header-only scope; later-section overclaims (§6.8) explicitly out of scope.

## Evidence gathered (read files directly, not implementer transcript)

| # | Criterion | Verdict | Evidence |
|---|---|---|---|
| 1 | Top status line no longer says `CALIBRATED (Audit Grade)` | PASS | `docs/SPEC.md:3` = `**Status:** Phase 2A — Partial (residual risk; S2 recall 0.0)`. Grep for `CALIBRATED|Audit Grade` across SPEC.md returns exactly one hit: `docs/SPEC.md:273` (§6.8), which is outside the header block (header ends at `---` on line 10). |
| 2 | Status reflects Phase 2A / residual (S2 recall 0.0) | PASS | `docs/SPEC.md:3` states `residual risk; S2 recall 0.0`. |
| 3 | Version line does not claim Phase 2 closed/calibrated | PASS | `docs/SPEC.md:6` = `**Version:** 2.2 — interim operating point at Threshold = 45.0 (Phase 2 not closed)`. |
| 4 | Header points at progress.md and/or calibration_final_metrics.json | PASS | `docs/SPEC.md:8` = `**Metrics authority:** memory-bank/progress.md ... docs/calibration_final_metrics.json`. Both pointer targets exist and corroborate: `memory-bank/progress.md:24,32-33` and `docs/calibration_final_metrics.json`. |
| 5 | Ignore §6.8 overclaims | N/A (respected) | Only remaining `Audit Grade` is `docs/SPEC.md:273`, out of scope. |

## Cross-check of header metrics against source of truth

Header claim (`docs/SPEC.md:8`): global recall ~0.42; S1/S3/S4 recall 1.0; S2 recall 0.0. Confirmed against `docs/calibration_final_metrics.json`:
- global recall `0.4166...` (~0.42) — line 4
- scenario_2_slow_roll recall `0.0` — line 17
- scenario_1/3/4 recall `1.0` — lines 12, 22, 27

No overclaim, no stale/gamed evidence found. Header numbers are faithful to the saved sweep.

## Strongest reason it survives

Every acceptance criterion is satisfied by the actual file contents (not the implementer's report), and the header's quantitative claims match `calibration_final_metrics.json` exactly. The only surviving `CALIBRATED (Audit Grade)` string is at `docs/SPEC.md:273` (§6.8), explicitly excluded from this packet's scope.
