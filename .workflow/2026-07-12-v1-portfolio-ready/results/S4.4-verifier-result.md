verification_model: cursor-grok-4.5-high

# S4.4 skeptic-verifier result

## Claim restated

S4.4 demo path (seed → triage → explain → contain simulated) is complete per packet ACs: documented path, no external LLM keys, automated API-chain test, `progress.md` S4.4 done with entrypoint, focused tests pass.

## Verdict

**survives**

## Strongest reason

Independent evidence shows the full chain works without LLM keys: `tests/web/test_demo_path.py` passes, and a non-pytest `TestClient` replay with only local `API_KEY=dev-key` (OPENAI/ANTHROPIC unset) reaches `validation_status=template_fallback` and queues `simulate_containment`, matching README/`progress.md` S4.4 entrypoints.

## Evidence gathered

### AC1 — Documented demo path

- `README.md` “Demo path” section: seed → triage → explain → simulated contain; CLI + UI steps; cleanup.
- `scripts/demo_path.py`: `seed_demo_alert` → `run_demo_path` (list triage → acknowledge workflow → explain → contain).
- Workflow transition present: PUT `/workflow` to `acknowledged` (packet: “ideally a workflow transition”).

### AC2 — No external API keys

- Env probe: `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` unset.
- Non-pytest replay with `API_KEY=dev-key` only: explainer logged `No LLM API key configured` → `template_fallback`; containment row `('demo_path_alert', 'simulate_containment')`.
- Local analyst `API_KEY` is documented server auth, not an external LLM key; without it outside pytest, privileged routes correctly 500 (auth config), which matches README’s `set API_KEY=dev-key`.

### AC3 — Automated test covers API chain

- `tests/web/test_demo_path.py::test_demo_path_api_chain` seeds, calls `run_demo_path`, asserts acknowledged state, explanation present, containment queue row, cleanup.

### AC4 — progress.md S4.4 done + entrypoint

- `memory-bank/progress.md` S4.4: `done` with `scripts/demo_path.py` + README + `tests/web/test_demo_path.py`.

### AC5 — Focused tests pass

```text
python -m pytest tests/web/test_demo_path.py -v --tb=short
→ 1 passed in ~0.95s

python -m ruff check scripts/demo_path.py tests/web/test_demo_path.py
→ All checks passed! (exit 0)
```

## Attack angles checked (did not refute)

| Angle | Outcome |
|---|---|
| Test only works via pytest auth bypass | True but intentional; live path documents `--api-key`; non-pytest replay with API_KEY succeeds |
| Weak assert `passed` \| `template_fallback` | Soft; independent replay observed `template_fallback` with no LLM keys |
| Full SPEC §11.5 (investigating/clear/lineage) | Out of packet scope; AC asks demo path + ideally one workflow transition |
| Phase 3 rollup still lists “demo path polish” open | Stale aggregate note vs S4.4 row `done`; does not break AC4 |

## Residual notes (non-blocking)

- Live `uvicorn` + CLI `seed-and-run` not smoke-tested in this verify pass (same `run_demo_path` exercised via `TestClient`).
- Phase 3 summary line in `progress.md` still says “demo path polish” open — worth cleaning, not an S4.4 AC miss.

## Commands run

```bash
python -m pytest tests/web/test_demo_path.py -v --tb=short
python -m pytest tests/web/test_demo_path.py::test_demo_path_api_chain --tb=line -q
python -c "…ruff subprocess…"   # All checks passed, code 0
python -c "…non-pytest TestClient replay with API_KEY=dev-key, no LLM keys…"
# earlier: non-pytest without API_KEY → HTTP 500 at acknowledge (expected)
```

## File reads

- `.workflow/.../packets/S4.4-verifier.md`, `results/S4.4-implementer-result.md` (claims treated as unevidenced)
- `scripts/demo_path.py`, `tests/web/test_demo_path.py`, `README.md`, `memory-bank/progress.md`
- `web/api.py` (`verify_api_key`, explain/contain/workflow)
- `worker/explainer.py` (template fallback on missing LLM key)
- `worker/recorder.py` (`simulate_containment`)
- `docs/SPEC.md` §11.5 (scope check)
