# S1.5 Implementer Result

## Summary

Added durable pytest smoke coverage in `tests/test_generator.py` confirming benign-only and correlated-benign generation paths tag `production` and run without crash, while all four attack scenarios remain on `eval_scenario_*` partitions after S1.1. No precision/recall metrics were computed or claimed; `calibration_final_metrics.json` was not touched.

## Files changed

| File | Change |
|------|--------|
| `tests/test_generator.py` | Added `test_benign_only_baseline_smoke_production_partition`, `test_correlated_benign_rollout_stays_production`, and `test_attack_partitions_remain_eval_scenario_after_s1_1` |

## Verification commands

```bash
pytest tests/test_generator.py -v --tb=short
pytest -k "benign or rollout or attack_partitions or partition or generator" -v --tb=short
```

### Outcome

```
7 passed in 1.50s   # full generator file

7 passed, 54 deselected, 1 warning in 2.36s   # focused -k filter

tests/test_generator.py::test_benign_only_baseline_smoke_production_partition PASSED
tests/test_generator.py::test_correlated_benign_rollout_stays_production PASSED
tests/test_generator.py::test_attack_partitions_remain_eval_scenario_after_s1_1 PASSED
tests/test_generator.py::test_scenario_2_attack_events_use_eval_partition PASSED
tests/test_generator.py::test_scenario_3_attack_events_use_eval_partition PASSED
tests/test_generator.py::test_baseline_events_remain_production_partition PASSED
tests/test_generator.py::test_event_generator_deterministic PASSED
```

## Acceptance criteria

1. ✅ Benign baseline smoke generates events with `production` partition and runs without crash
2. ✅ Correlated-benign `inject_tooling_rollout` path tags `production` (45 rollout events) and excludes `eval_scenario_*`
3. ✅ Full eval mix confirms S1–S4 attacks use `eval_scenario_1`–`eval_scenario_4`; benign events stay `production`
4. ✅ Pytest evidence captured above
5. ✅ **No new P/R claims** — partition smoke only; no calibration sweep or metrics refresh

## Unresolved

None.
