verification_model: cursor-grok-4.5-high

# S5.10 Verifier Result — Counterfactual corpus + harness (Path A)

**Date:** 2026-07-13  
**Packet:** S5.10-verifier  
**Claim tested:** Counterfactual consistency corpus + harness shipped (Path A).

## Verdict: **survives**

**Strongest reason:** Fresh scoped pytest is **13 passed** (exit 0), and the corpus harness asserts independent JSON expected IDs/score_deltas (`tests/fixtures/counterfactual_corpus.json`) against `build_top_k_counterfactuals` (`worker/explainer.py:373-396`) which is wired into `generate_explanation` at `:422` — not a circular-only check.

---

## Method

Treat implementer transcript as unevidenced. Independently:

1. Re-read researcher Path A ACs and implementer packet scope
2. Read `worker/explainer.py` helper + `generate_explanation` wiring
3. Read corpus JSON, harness, docs, SPEC_V3 §9 note
4. Attack for weak/circular assertions and missing AC coverage
5. Re-run focused pytest + ruff (not reuse implementer log)

---

## Fresh verification

```text
$ python -m pytest tests/worker/test_counterfactual_consistency.py tests/worker/test_explainer.py -v --tb=short
======================= 13 passed, 37 warnings in 0.47s =======================
EXIT 0

$ python -m ruff check worker/explainer.py tests/worker/test_counterfactual_consistency.py
All checks passed!
```

---

## Acceptance checklist (Path A ACs)

| # | Criterion | Result | Evidence |
|---|---|---|---|
| 1 | Extract `build_top_k_counterfactuals(contributions, k=3)`; wire into `generate_explanation` | **pass** | Helper `worker/explainer.py:373-396`; call site `:422`; used for LLM path `:495` and template fallback via `top_k_cf` arg |
| 2 | Corpus ≥3 cases (normal, &lt;K, empty) | **pass** | `tests/fixtures/counterfactual_corpus.json`: `normal_top_k`, `fewer_than_k`, `empty_contributions` |
| 3 | Harness asserts ID order, score_delta, text binding | **pass** | `_assert_counterfactual_consistency` in `tests/worker/test_counterfactual_consistency.py:26-45` |
| 4 | Integration test via `generate_explanation` + mock LLM | **pass** | `test_generate_explanation_counterfactual_integration` (`:68-132`) |
| 5 | `docs/counterfactual-consistency.md` | **pass** | Metric + corpus table + rerun commands present |
| 6 | SPEC_V3 §9 satisfied note (S5.10) | **pass** | `docs/SPEC_V3.md:341` |
| 7 | Focused pytest green (+ ruff clean per researcher) | **pass** | Fresh: 13 passed; ruff clean |

---

## Attack attempts (did not flip verdict)

1. **Circular integration test** — Integration compares `generate_explanation` output to `build_top_k_counterfactuals` on the same input (wiring-only). Correctness still rests on corpus JSON expected IDs/deltas, which are independent of the helper. Not a refute of Path A.
2. **Dead `expected.feature_names` in corpus** — Field never asserted; text binding checks live feature names from contributions instead. Coverage gap only.
3. **No template-fallback CF integration** — AC asked mock-LLM path only; template path still receives the same `top_k_cf` from `:422`. Residual.
4. **Ties / negative contributions untested** — Spec §10.2 asks whether top-K matches largest contributions; corpus covers distinct positive scores + empty/&lt;K. Adequacy for Path A; not a missing AC.
5. **Stale implementer pytest log** — Re-ran fresh; 13 passed matches implementer claim.

---

## Residual risks (do not flip verdict)

- Integration test alone would not catch a wrong helper if corpus expecteds were also wrong; keep expecteds hand-authored.
- Optionally assert `expected.feature_names` and add a template-fallback CF attachment case.
- Template claim loop (`generate_template_explanation` `:340`) still duplicates top-3 sort logic separately from the CF helper (claims only).

---

## Verdict summary

Packet-scoped Path A claim **survives**. Helper extracted and wired, ≥3-case corpus + harness cover ID order / score_delta / text binding, docs and SPEC_V3 §9 note present, and focused pytest freshly passes 13/13.
