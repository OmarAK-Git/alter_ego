# Packet S6.3 — implementer

## Objective

Ensure OPS.md has a clear standing rule: no weight/threshold change without a recorded full sweep + config governance.

## Context

- OPS.md already has a standing-rule bullet (S6.1 extended it with `docs/hardening-sweep-checklist.md` pointer).
- Governance pattern: `docs/scoring-config-governance-s3.md`.
- SPEC §9 / §10.1 scoring-config governance.
- OPS.md must stay under **60 lines**.
- Write scope is **OPS.md only** (+ result file). Do not expand into other docs unless a one-line cross-link already exists.

## Write scope

- `OPS.md` only (plus result artifact)
- Result: `.workflow/2026-07-12-v1-portfolio-ready/results/S6.3-implementer-result.md`
- Optional: `memory-bank/progress.md` is **out of write scope** — do not edit it (controller will).

## ACs

1. OPS.md contains an explicit standing rule forbidding weight/threshold changes without recorded sweep + governance (checklist and/or governance doc named).
2. Rule is operator-actionable (says what to run / what to write).
3. OPS.md remains ≤60 lines.
4. No edits to `config/scoring_config.yaml` or product code.
5. Do not mark `state.json` done.

If the rule already fully satisfies ACs after S6.1, make the minimal clarifying edit (or document no further change needed with evidence) — do not bloat OPS.

Header: `implementation_model: composer-2.5`
