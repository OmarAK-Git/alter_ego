# Packet S5.3 — skeptic-verifier

## Claim to refute

S5.3 Path A complete: Postgres app role with INSERT-only on audit/commit tables; profiles lifecycle-column UPDATE only; compose owner-vs-app split; SPEC honesty.

## Acceptance criteria

1. Alembic migration creates app role with INSERT-only on audit_logs (+ append-only tables as claimed).
2. profiles: not full table INSERT-only — column-level UPDATE only on promoted_at/superseded_at.
3. Compose: migrations as owner; runtime services use app role DSN.
4. Docs honest (SPEC_V3 exception; SPEC §9.2 shipped language; deployment role matrix).
5. Root SPEC synced if docs/SPEC changed.
6. No false full INSERT-only claim on profiles.
7. Migration chains from current head; ruff/pytest evidence if re-run.

Treat implementer as unevidenced. Task-scoped. Readonly.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S5.3-verifier-result.md`  
Header: `verification_model: cursor-grok-4.5-high`
