# Packet S6.2 — skeptic-verifier

## Claim

S6.2 is complete: residual-risk + open drift hypotheses doc exists under `docs/` with evidence-backed residuals (including embedding normalizer trap) and open research hypotheses; no scoring knobs changed.

## Goal (verbatim)

Residual-risk + open drift hypotheses doc for human research.

## Acceptance criteria

1. Doc lists concrete residual risks with evidence pointers (metrics JSON / operating-point / S5 residual).
2. Doc lists open drift hypotheses suitable for human research (not closed as solved).
3. Includes the stale `core/` embedding normalizer default as an engineering residual / fail-closed trap.
4. Honest: not CALIBRATED; no weight/threshold recommendations that skip governance.
5. `state.json` not marked done by implementer.

## Evidence to gather yourself

- Read the new/updated docs file(s)
- Spot-check cited numbers against `docs/phase2-s3-operating-point.md` / `docs/calibration_final_metrics.json`
- Confirm `DEFAULT_EMBEDDING_INPUT_NORMALIZER_VERSION` claim against `core/` (grep)
- Confirm `config/scoring_config.yaml` untouched
- Read implementer result; treat claims as unevidenced until checked
- Task-scoped only — ignore sprint/program gaps

## Write

`.workflow/2026-07-12-v1-portfolio-ready/results/S6.2-verifier-result.md`

Header: `verification_model: cursor-grok-4.5-high`

Verdict: `survives` or `refuted`.

readonly.
