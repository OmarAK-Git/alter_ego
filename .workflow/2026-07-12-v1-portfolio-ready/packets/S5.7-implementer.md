# Packet S5.7 — implementer

## Objective

Empirical LLM determinism check vs pinned non-alias model (SPEC §8.4 / V3 §9) — **not mock**.

## Context

`scripts/llm_determinism_check.py` currently uses a **mock** with injected variance. Progress flags this as debt. Replace with a real provider client path using a **pinned non-alias** model id, temperature=0, 10 identical prompts, hash outputs, write `docs/llm-determinism-check.md`.

## Write scope

- `scripts/llm_determinism_check.py`
- `docs/` (esp. `docs/llm-determinism-check.md` and any short how-to)
- Result: `results/S5.7-implementer-result.md`

## ACs

1. Script supports a real provider call path (OpenAI-compatible or Anthropic — match what `worker/explainer.py` uses if any; else document env vars).
2. Model id is pinned non-alias (no `-latest` / alias-only ids).
3. temperature=0; N=10 identical prompts; record hashes; conclude deterministic vs not.
4. No mock-as-default that fakes an empirical conclusion. If `API` key missing: exit non-zero with clear message **or** write an honest “not executed — awaiting credentials” artifact that does **not** claim empirical results.
5. If credentials exist in env, prefer running once and writing real results (external API call — if blocked by approval, stop and report `approval_gates` with exact command).
6. Docs explain how to re-run and how lineage architecture still holds if non-deterministic.

Do not mark state done. Header: `implementation_model: composer-2.5`
