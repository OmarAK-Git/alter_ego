# Packet S2.5 — implementer (Path B: downgrade)

## Decision (from researcher)

**B — Downgrade SPEC_V3.** Do NOT ship fake independent cohort-prior artifacts.

Evidence: `.workflow/2026-07-12-v1-portfolio-ready/results/S2.5-researcher-result.md`

## Objective

Document that v1 uses profile-embedded `cohort_data` only; independent frozen cohort-prior artifacts are deferred.

## Write scope (only)

- `docs/SPEC_V3.md` (§4 / §6.6 / acceptance criteria about frozen cohort-prior snapshots)
- `memory-bank/progress.md` (S2.5 note + status)
- Optionally `OPS.md` one-line deferred note if that matches repo convention

## Do not touch

- `batch/profile_builder/` (no stub cohort_builder)
- ORM / alembic (no empty CohortPrior table)
- scoring weights
- state.json (orchestrator updates)
- Do not mark packet done in state.json

## Acceptance criteria

1. SPEC_V3 no longer claims independent frozen cohort-prior artifacts as a Phase-1/v1 exit requirement without qualification.
2. Explicit downgrade note: v1 = profile-embedded `cohort_data`; independent versioned cohort artifacts deferred (e.g. Phase 4 / S5.11).
3. Three-step fallback + unsupported terminus remain described as implemented.
4. `memory-bank/progress.md` S2.5 closed with downgrade rationale (not silent).
5. No new code modules pretending independence.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S2.5-implementer-result.md`
