# Packet S1.1 — implementer

## Objective

Fix S2/S3 attack injection so `simulation_partition` is `eval_scenario_2` / `eval_scenario_3` (or equivalent `eval_scenario_*`), not `"production"`, so eval attack events are excluded from profile training like S1/S4.

## Write scope (only)

- `batch/synthetic/generator.py`
- `tests/` (add/update tests proving partition tags)

## Do not touch

- scoring weights, SPEC narrative packets, state.json
- Do not mark packet done

## Acceptance criteria

1. `inject_scenario_2_slow_roll` tags attack events with a non-production eval partition (prefer `eval_scenario_2`).
2. `inject_scenario_3_coordinated` tags attack events with `eval_scenario_3` (or consistent naming).
3. Benign/production events remain `production` where intentional.
4. Automated test(s) fail before / pass after proving S2/S3 attacks are not `production`.
5. Run: `pytest -k "partition or scenario_2 or scenario_3 or generator" -v --tb=short` (or the tests you add) with evidence in result.

## TDD

Write failing test first if no coverage exists, then fix generator.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S1.1-implementer-result.md`
