# Packet S6.1 — implementer

## Objective

Document a hardening sweep checklist (commands, seeds, artifacts to refresh) so a human can re-run empirical calibration sweeps after personal drift research.

## Context

- T3 run: `.workflow/2026-07-12-v1-portfolio-ready/`
- Active sprint S6; S5-EXIT-GATE done.
- Canonical sweep evidence: `docs/calibration_final_metrics.json` (seed 42, config v2.2, thr=45).
- Harness: `batch/eval/runner.py`; analysis patterns in `scratch/analyze_step*.py`.
- Operating point / residuals: `docs/phase2-s3-operating-point.md`.
- Governance pattern: `docs/scoring-config-governance-s3.md`.
- OPS.md must stay under ~60 lines — prefer a short OPS pointer + a dedicated `docs/` checklist.

## Write scope

- `docs/` (new checklist doc, e.g. `docs/hardening-sweep-checklist.md`)
- `OPS.md` (one short pointer to the checklist; do not bloat)
- `memory-bank/progress.md` (S6.1 row notes only if needed)
- Result: `.workflow/2026-07-12-v1-portfolio-ready/results/S6.1-implementer-result.md`

## ACs

1. A durable checklist exists under `docs/` listing: exact commands to run a full sweep, fixed seed(s), which artifacts to refresh (`docs/calibration_*.json`, governance note, operating-point doc), and what must not change without governance.
2. `OPS.md` points at that checklist in ≤2 lines (file stays under 60 lines).
3. Checklist is honest: not CALIBRATED; thr=45 residual FP/FN noted by pointer to operating-point doc.
4. No weight/threshold edits in `config/scoring_config.yaml`.
5. Do not mark `state.json` packet done (controller owns that).

## Do Not

- Change scoring weights/thresholds
- Install deps / edit `.claude` / clone repos
- Write outside write scope
- Claim Phase 2 CALIBRATED

Header: `implementation_model: composer-2.5`
