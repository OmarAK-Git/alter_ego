# Packet S4.4 — implementer

## Objective

Demo path: seed → triage → explain → contain (simulated) — SPEC §11.5 / portfolio Phase 3.

## Context

- Analyst workflow APIs already exist: alerts list/detail, explain, workflow PUT, contain (simulated).
- Synthetic generator exists (`batch/synthetic/generator.py`) with seeded RNG.
- README does not yet document a single end-to-end demo walkthrough.
- Goal: a reproducible operator path (script and/or README steps) that seeds enough data to show triage → explain → simulated containment — not a full product polish pass.

## Write scope

- `web/` (only if a thin demo/seed endpoint or helper is needed; prefer script over new API surface)
- `batch/synthetic/` (seed helpers only if needed)
- `scripts/` — OK to add `scripts/demo_path.py` (or similar) even if not in state files_or_sources; if you prefer keep under `batch/synthetic/` or `scratch/`, note why
- Prefer documenting in `README.md` a short “Demo path” section
- Tests under `tests/` proving the workflow chain against TestClient / in-memory DB where practical
- `memory-bank/progress.md` — S4.4 status
- `.workflow/2026-07-12-v1-portfolio-ready/results/S4.4-implementer-result.md`

If adding `scripts/demo_path.py`, that path is allowed for this packet (orchestrator expands write scope).

## Do not touch

- scoring weights / anomaly_threshold
- state.json
- Live LLM calls required for demo (use StubLLMProvider / template path)
- Phase exit verification

## Acceptance criteria

1. Documented demo path in README (or linked script) covering: seed data → open/list triage alerts → generate explanation → queue simulated containment (and ideally one workflow state transition).
2. Script or test can run without external API keys (stub/template explainer OK).
3. At least one automated test covers the API chain (triage visible → explain → contain) or the demo script’s core functions.
4. progress.md S4.4 marked done with pointer to the demo entrypoint.
5. Focused tests pass.

## Constraints

- Do not mark state.json done.
- Stop for approval_gates if you need dependency installs or writes outside scope.
- Keep the demo lean — no new SIEM, no real IAM disable.

## Report

Header: `implementation_model: composer-2.5`
