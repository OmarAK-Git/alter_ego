# Packet S2.3 — implementer

## Objective

`decision_id` / canonical serialization includes `scorer_algorithm_version` and `embedding_model_version` (SPEC_V3 §6.1).

## Write scope (only)

- `worker/scorer.py`
- `core/schemas/decisions.py` (if fields needed on DecisionRecord)
- `tests/` (determinism / decision_id tests)

## Do not touch

- scoring weights/thresholds
- state.json / memory-bank
- Do not mark packet done

## Acceptance criteria

1. `decision_id` inputs include at least: `event_id`, `profile_version`, `scoring_config_version`, `scorer_algorithm_version`, and `embedding_model_version` (when embeddings participate — always for current ngram path).
2. Changing algorithm version OR embedding model version changes `decision_id`; same inputs reproduce the same id.
3. Prefer canonical serialization (stable key order / explicit concat documented in result) over ad-hoc string glue without documenting the formula.
4. Pin or define a `scorer_algorithm_version` constant (e.g. module-level) used in the hash.
5. Tests prove (a) stability and (b) sensitivity to algorithm/embedding version changes.
6. Run focused pytest for decision_id / determinism.

## Current gap

Today roughly:
```python
decision_id = hashlib.sha256(
    f"{resolved_event.event_id}{profile.profile_version}{config.get('version', '2.1')}".encode()
).hexdigest()
```
Missing algorithm + embedding versions.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S2.3-implementer-result.md`
