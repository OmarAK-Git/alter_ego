# Packet S5.9 — implementer

## Objective

Schema-version / embedding-metadata mismatch detection at scorer startup (V3 §9 / SPEC §5.6).

## Context

Profiles record `embedding_model_id`, version, dimensionality. Scorer should refuse or halt when active profiles disagree with the configured runtime embedding (`alter-ego-ngram-v1` / 128-d). Prefer a startup/check function callable from `process_unscored_events` or module init — document where it runs.

## Write scope

- `worker/scorer.py`
- `tests/`
- Result: `results/S5.9-implementer-result.md`
- May import constants from `worker/vectorizer.py` / profile schemas if already exported (read OK; prefer not editing them unless necessary — if needed, expand carefully and note)

## ACs

1. Detectable check compares runtime embedding metadata vs profile metadata (and/or schema version if present).
2. On mismatch: do not silently score — raise or return structured halt covering affected entities / global gate (document).
3. Matching metadata allows scoring as today.
4. Tests cover match vs mismatch.
5. Focused pytest passes.

Do not mark state done. Header: `implementation_model: composer-2.5`
