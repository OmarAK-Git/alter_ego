# Packet S6.1 — skeptic-verifier

## Claim

S6.1 is complete: a durable hardening sweep checklist exists under `docs/`, OPS.md points to it briefly, and no scoring knobs were changed.

## Goal (verbatim)

Document hardening sweep checklist (commands, seeds, artifacts).

## Acceptance criteria

1. A durable checklist exists under `docs/` listing: exact commands to run a full sweep, fixed seed(s), which artifacts to refresh (`docs/calibration_*.json`, governance note, operating-point doc), and what must not change without governance.
2. `OPS.md` points at that checklist in ≤2 lines (file stays under 60 lines).
3. Checklist is honest: not CALIBRATED; thr=45 residual FP/FN noted by pointer to operating-point doc.
4. No weight/threshold edits in `config/scoring_config.yaml`.
5. `state.json` packet must not be marked done by implementer (controller owns that) — ignore progress.md status optimism.

## Evidence to gather yourself

- Read `docs/hardening-sweep-checklist.md` (or whatever path implementer created)
- Read `OPS.md` (line count + pointer)
- Diff / confirm `config/scoring_config.yaml` unchanged for weights/thresholds
- Read implementer result: `.workflow/2026-07-12-v1-portfolio-ready/results/S6.1-implementer-result.md`
- Treat implementer claims as unevidenced until checked
- Ignore phase/sprint-level gaps (task-scoped only)

## Write

`.workflow/2026-07-12-v1-portfolio-ready/results/S6.1-verifier-result.md`

Header: `verification_model: cursor-grok-4.5-high`

Verdict: `survives` or `refuted` with strongest reason + file:line evidence.

readonly: do not modify product files.
