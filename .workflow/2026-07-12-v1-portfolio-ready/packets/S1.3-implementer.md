# Packet S1.3 — implementer

## Objective

Auto-write containment queue when anomaly score ≥ `containment_threshold` and confidence floor is met; actually wire `containment_threshold` from scoring_config (today: flag-only / manual API).

## Write scope (only)

- `worker/scorer.py`
- `worker/recorder.py`
- `config/scoring_config.yaml` — may *read*/document; change numeric values only if adding a missing key (do not change existing calibrated numbers without note)
- `web/api.py` — only if needed to share queue write path
- `tests/`

## Do not touch

- state.json; no commit; no unrelated features

## Acceptance criteria

1. `containment_threshold` from YAML is loaded and compared in scoring/recording path.
2. When score ≥ containment_threshold AND confidence gate satisfied, a containment queue row/action is written (same simulated containment semantics as manual API — not real IAM disable).
3. Below threshold: no auto queue write (may still anomaly-flag at anomaly_threshold).
4. Tests cover auto-queue vs not.
5. pytest evidence in result.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S1.3-implementer-result.md`
