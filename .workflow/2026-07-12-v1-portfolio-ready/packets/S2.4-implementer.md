# Packet S2.4 — implementer

## Objective

Observation-count confidence `n/(n+k)`; put `confidence_k` in `scoring_config.yaml` (SPEC_V3 §6.5 / §7).

## Write scope (only)

- `worker/scorer.py`
- `config/scoring_config.yaml` (add `confidence_k` only — do not change weights/thresholds unless required for governance consistency; prefer additive key)
- `tests/`

## Do not touch

- Other feature weights / anomaly_threshold / drift_threshold
- state.json / memory-bank
- Do not mark packet done

## Acceptance criteria

1. `confidence_k` exists in `config/scoring_config.yaml` (documented default, e.g. 10.0).
2. `score_event` computes `decision.confidence = n / (n + confidence_k)` where `n` is observation count from profile (`total_events` preferred).
3. Changing `confidence_k` in config changes confidence for fixed `n` (test).
4. Larger `n` → higher confidence for fixed `k` (test).
5. If formula already present with hard-coded default only, still add YAML key and wire tests proving config is read (not only default fallback).
6. Run focused pytest for confidence.

## Governance note

Adding `confidence_k` is a config schema addition, not a weight/threshold retune. Do not bump calibrated claim language. If config `version` must bump for loader reasons, note it in the result.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S2.4-implementer-result.md`
