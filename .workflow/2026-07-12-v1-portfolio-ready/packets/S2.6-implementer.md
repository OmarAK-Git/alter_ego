# Packet S2.6 — implementer

## Objective

`total_volume_delta`: implement for real **or** remove/strip from config/README/feature claims so portfolio docs do not imply a working sixth feature.

## Current truth

`worker/scorer.py` hard-codes `score_vol = 0.0` and always flags `volume_delta_suppressed_no_current_count`. YAML weight unused. README still lists it as a feature.

## Write scope (only)

- `worker/scorer.py`
- `config/scoring_config.yaml`
- `README.md`
- `tests/` as needed
- `memory-bank/progress.md` (status note for S2.6)
- Optionally `docs/SPEC.md` feature list if it overclaims

## Do not touch

- Other feature weights / anomaly_threshold / drift_threshold
- state.json
- Do not mark packet done in state.json

## Decision guidance

Prefer **honest strip/defer** unless a real volume delta can be computed from existing resolved_events (count in a recent window vs profile baseline) with tests — without inventing schema fields.

If strip/defer:
1. Stop listing it as an active calibrated feature in README / SPEC feature table claims.
2. Either remove YAML weight **or** keep weight but document suppressed/deferred; contribution may remain zero with explicit flag (already present).
3. Update progress.md overstatements register.

If implement:
1. Compute real non-zero contribution when volume spikes.
2. Tests prove contribution changes with event counts.
3. Remove permanent suppression flag except when data truly unavailable.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S2.6-implementer-result.md` — state which path and why.
