# Packet S5.8 — implementer

## Objective

Full pgvector / embedding dimensionality migration playbook (SPEC §4.3 / V3 §6.8/§9).

## Context

v1 uses `alter-ego-ngram-v1` 128-d vectors. Alembic/ORM may still mention historical nomic 768-d. Need an operator playbook covering: detect mismatch, ALTER TABLE / versioned columns approach (a) per SPEC, rebuild profiles, resume scoring. Honest about current 128-d lock.

## Write scope

- `docs/` only (new playbook + cross-links if needed)
- Result: `results/S5.8-implementer-result.md`

## ACs

1. Playbook document exists under `docs/` (e.g. `docs/pgvector-embedding-migration.md`).
2. Covers: current model/dim lock; why dim changes break pgvector columns; migration steps (schema + profile rebuild + scorers offline window); rollback; verification checks.
3. Does not claim nomic is runtime.
4. Cross-link from SPEC §4.3 or deployment docs with a short pointer (minimal edit).
5. Doc-only; no Python required.

Do not mark state done. Header: `implementation_model: composer-2.5`
