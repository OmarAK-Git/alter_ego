# Packet S5.4 — implementer

## Objective

Scheduled audit hash-chain integrity job / assertion per SPEC §9.2 item 3 / SPEC_V3 §6.10/§9.

## Context

- Hash chaining exists on `AuditLogModel` (`previous_log_hash` + `compute_hash()`).
- SPEC requires a **scheduled integrity check** that verifies hash-chain continuity (and ideally counts vs expected decision-related audit activity) and surfaces discrepancy as a sensor-health style signal.
- No dedicated integrity job module exists yet under `batch/`.

## Write scope (`files_allowed`)

- `core/models.py` (only if needed for shared verify helper — prefer keep hash logic DRY)
- `batch/` (new module e.g. `batch/audit_integrity.py` or under existing batch package)
- `tests/test_audit.py` (and/or new focused test under `tests/`)
- `.workflow/2026-07-12-v1-portfolio-ready/results/S5.4-implementer-result.md`
- Optional tiny doc note under `docs/` only if required for honesty — prefer code+tests; if you must touch docs, stay minimal

## Do not touch

- `state.json`, scoring weights, Docker/compose (unless you only add a documented exec recipe — prefer stay in files_or_sources)
- Phase exit verification

## Acceptance criteria

1. Callable integrity check walks audit_logs in order, recomputes hashes, verifies each `previous_log_hash` linkage; returns structured pass/fail with break details.
2. Detects at least: broken chain (tampered previous hash), and optionally count mismatch vs decisions if cheap.
3. Runnable as a batch/CLI entry (e.g. `python -m batch.audit_integrity`) suitable for cron/`docker compose exec batch`.
4. On failure, emits a clear non-silent signal (raise / non-zero exit / structured incident record — document choice).
5. Tests cover: healthy chain passes; deliberately broken chain fails.
6. `pytest tests/test_audit.py -v --tb=short` (and any new test file) passes; TDD preferred.

## Constraints

- Do not mark state done.
- Stop for approval_gates before installs / out-of-scope writes.

## Report

`results/S5.4-implementer-result.md` — header `implementation_model: composer-2.5`
