# Packet S5.12 — implementer

## Objective

Threat-model polish in repo + README aligned to implemented behavior (SPEC Phase 4 / portfolio honesty after S5.1–S5.11).

## Write scope

- `README.md`
- `docs/SPEC.md` (threat model / phase tables only as needed) + sync root `SPEC.md`
- `docs/` (small honesty polish if needed)
- `memory-bank/progress.md` Phase 4 / S5.12 row OK
- Result: `results/S5.12-implementer-result.md`

## ACs

1. README phase/status table no longer claims missing 4-container deploy / DB INSERT-only roles / migration playbook / etc. when S5 shipped them; still honest about Partial / Not CALIBRATED.
2. Threat-model bullets that say “Addressed via X” match what actually ships after S5 (qualify where Path B deferred).
3. Pointers to `docs/deployment.md`, pgvector playbook, llm-determinism-check, counterfactual-consistency as appropriate.
4. Root SPEC synced if docs/SPEC edited.
5. No false CALIBRATED / 100% recall claims.

Do not mark state done. Header: `implementation_model: composer-2.5`
