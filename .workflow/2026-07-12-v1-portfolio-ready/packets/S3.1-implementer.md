# Packet S3.1 — implementer

## Objective

Full eval sweep under current config after S1/S2 fixes (Hybrid C re-sweep). Produce fresh precision/recall evidence; do **not** claim CALIBRATED and do **not** overwrite `docs/calibration_final_metrics.json` (that is S3.2).

## Write scope (only)

- `scratch/` — sweep driver and/or intermediate outputs if needed
- `batch/eval/` — only if a small harness fix is required to run the sweep under current code (prefer reuse of `runner.py` / `calibrate.py` paths)
- `.workflow/2026-07-12-v1-portfolio-ready/results/S3.1-implementer-result.md` — required report
- Optional local sqlite DB / `events.jsonl` / `ground_truth.jsonl` in repo root or `scratch/` (do **not** commit DB churn; gitignore covers calibrate DBs)

## Do not touch

- `docs/calibration_final_metrics.json`, `docs/calibration_pr_curve.json` (S3.2)
- `config/scoring_config.yaml` weights / thresholds (no retune in S3.1)
- `state.json` — do not mark packet done
- Phase/sprint exit verification

## Method (preferred)

1. Generate the standard four-scenario mix via `batch/synthetic/generator.py` (`EventGenerator(seed=42)`, baseline + S1–S4 injects as in `__main__`) → `events.jsonl` + `ground_truth.jsonl`.
2. Run the day-window pipeline (`batch.eval.runner.run_pipeline` or equivalent) against a **dedicated** sqlite DB (e.g. `alter_ego_calibrate_s31.db` via `DATABASE_URL`).
3. Compute metrics with `calculate_metrics` at the **current** operating point (`anomaly_threshold` from `config/scoring_config.yaml`, currently 45) and record per-scenario recall.
4. Optionally compute a threshold sweep / PR points into `scratch/` or the result markdown — do not write `docs/calibration_*.json`.
5. Confirm S2/S3 attack events use `eval_scenario_*` partitions (S1.1) so they are excluded from training.

## Acceptance criteria

1. A full four-scenario eval pipeline completed under current code + config (not a unit-test smoke).
2. Result report includes: global precision/recall/F1/tp/fp/fn, and per-scenario recall for S1–S4 (especially **scenario_2_slow_roll**).
3. Operating threshold used is documented (must match current YAML `anomaly_threshold` unless explicitly noting a diagnostic sweep).
4. No weight/threshold changes in `scoring_config.yaml`.
5. `docs/calibration_final_metrics.json` unchanged (leave for S3.2).
6. Commands run + raw metric JSON captured in the implementer result.

## Approval gates (stop and report)

Dependency installs, `.codex`/`.claude` edits, clones, writes outside write scope, destructive git.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S3.1-implementer-result.md`

Header must record: `implementation_model: composer-2.5`

Do **not** mark the queue/state item done.
