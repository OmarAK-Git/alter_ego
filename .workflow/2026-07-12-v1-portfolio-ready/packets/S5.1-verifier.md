# Packet S5.1 — skeptic-verifier

## Claim to refute

S5.1 is complete: Dockerfiles + four-container compose (web/worker/batch/postgres) meet task acceptance criteria.

## Goal / acceptance (authoritative — ignore implementer reasoning)

1. Dockerfiles build images usable for web, worker, batch (shared base OK).
2. `docker-compose.yml` defines four services: `web`, `worker`, `batch`, `postgres`.
3. `web` exposes FastAPI/analyst surface; worker/batch have documented run commands.
4. `DATABASE_URL` (or equivalent) wired to compose postgres; local-dev credentials only.
5. Docs under `docs/` describe bring-up; no false K8s/kind shipped claims unless manifests exist.
6. `.dockerignore` (or equivalent) keeps build context lean.
7. Valid Compose YAML; `docker compose config` if docker available (absence of docker is not an automatic refute if YAML/files are otherwise sound).

## Diff / changed files (from implementer — treat as unevidenced until you check)

- `Dockerfile`
- `.dockerignore`
- `docker-compose.yml`
- `docs/deployment.md`
- `.workflow/2026-07-12-v1-portfolio-ready/results/S5.1-implementer-result.md`

## Commands

- Read compose + Dockerfile + docs yourself
- `python -c` YAML parse of `docker-compose.yml` (or `docker compose config` if docker exists)
- Grep docs for overclaims (kind/k8s/IaC “shipped” without artifacts)
- Confirm service names match SPEC §4.4

## Scope discipline

- **verification.scope: task** — only S5.1 acceptance criteria.
- Ignore phase-level / sprint-level gaps (S5.2 IaC, DB roles, etc.).
- Treat implementer claims as unevidenced until independently checked.
- Readonly: do not modify files, git state, or environment.

## Report

Write `.workflow/2026-07-12-v1-portfolio-ready/results/S5.1-verifier-result.md`  
Header: `verification_model: cursor-grok-4.5-high`  
Verdict: `survives` or `refuted` with strongest reason + evidence (commands, file:line).
