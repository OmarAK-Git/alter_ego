# Packet S0.1 — implementer

## Objective

Demote `docs/SPEC.md` header status from “CALIBRATED (Audit Grade)” to honest Phase 2A / residual language. Point readers at `memory-bank/progress.md` and the saved metrics JSON for the real operating point.

## Original goal (program)

Finish ALTER_EGO v1 to an honest portfolio-ready bar — no false CALIBRATED claims.

## Write scope (only)

- `docs/SPEC.md`
- `memory-bank/progress.md` (S0.1 row status / note only)

## Do not touch

- `config/scoring_config.yaml` weights/thresholds
- Any `alter_ego_*.db`
- `__pycache__`, other packets’ files
- Do not mark `state.json` packet `done` (controller does that after verifier)

## Acceptance criteria

1. SPEC.md top status line no longer says `CALIBRATED (Audit Grade)`.
2. Status reflects Phase 2A / residual (S2 recall 0.0; global recall ~0.42 per progress.md).
3. Version/last-updated line does not claim a fully calibrated closed Phase 2.
4. Header or nearby note points at `memory-bank/progress.md` and/or `docs/calibration_final_metrics.json` (or actual metrics path if different).
5. `memory-bank/progress.md` S0.1 row updated to `doing`/`done` note consistent with the edit (prefer leave status for controller; you may set Notes column).

## Verification (you run what you can)

- Grep SPEC.md for remaining `CALIBRATED` in the header/status area; residual mentions of residual risk elsewhere are OK.
- Confirm progress.md still documents S2 recall 0.0 as the reason.

## Report schema

Write nothing to state.json. Return:
- files changed + one-line rationale each
- exact new status line(s)
- anything unresolved
