# Packet S4.1 — skeptic-verifier

## Claim under test

S4.1 (Explainer slot-isolated low-trust fields) is complete and meets acceptance criteria.

## Goal (verbatim)

Explainer slot-isolated low-trust fields

## Acceptance criteria (only these — ignore phase/sprint gaps)

1. Low-trust values (command lines, file paths, URLs, free-text) are in labeled delimited slots, not free-form prompt prose.
2. Slot values are escaped and length-capped.
3. Prompt instructs the model not to interpret slot contents as instructions.
4. High-trust fields remain named/structured bindings.
5. Tests cover injection escape, length cap; existing fallback/prohibited tests still pass.
6. `pytest tests/worker/test_explainer.py -v --tb=short` passes.

## Evidence to gather yourself

- Implementer result (unevidenced until you check): `.workflow/2026-07-12-v1-portfolio-ready/results/S4.1-implementer-result.md`
- Diff / read: `worker/explainer.py`, `tests/worker/test_explainer.py`
- Commands:
  - `pytest tests/worker/test_explainer.py -v --tb=short`
  - Optionally `ruff check worker/explainer.py tests/worker/test_explainer.py`

## Instructions

- Treat implementer claims as unevidenced until you verify.
- **Ignore** phase-level or sprint-level gaps (this is `verification.scope: task`).
- Verdict: `survives` or `refuted` with strongest reason and command evidence.
- Write: `.workflow/2026-07-12-v1-portfolio-ready/results/S4.1-verifier-result.md`
- Header: `verification_model: cursor-grok-4.5-high`
