# Packet S5.5 — implementer

## Objective

Staleness circuit breaker + active-alert mandatory escalation (SPEC §5.7 / V3 §9).

## Decision source

Researcher: Path A ship minimal escalation. Halt already exists; add active-alert detection + mandatory queue + tests. `max_profile_build_block_days` is **S5.6** — do not implement.

If researcher result file missing, use ACs below (same content).

## Write scope (`files_allowed`)

- `worker/scorer.py`
- `web/api.py`
- `tests/`
- `core/models.py`, `core/schemas/` (only if needed for extend-halt persistence)
- `alembic/` (only if extend-halt needs a column)
- `.workflow/2026-07-12-v1-portfolio-ready/results/S5.5-implementer-result.md`
- Optional: write `results/S5.5-researcher-result.md` if absent (copy of research conclusions)

## Do not touch

- `state.json`, `config/scoring_config.yaml` weights/thresholds (hardcode SLA hours OK)
- `batch/profile_builder/` (blocking already shipped; S5.6 owns build-block days)
- Phase exit

## Acceptance criteria

1. Staleness halt preserved: age > `max_profile_staleness_days` → `staleness_halt`, score 0.
2. Halt + uncleared alert (`new|acknowledged|investigating`) → flag `staleness_active_alert_escalation` (and basic `sensor_health_staleness` OK).
3. Halt without active alert → escalation flag absent.
4. `GET /api/mandatory-escalations` lists escalation items without requiring `is_anomaly=True`.
5. Queue items link to uncleared alert decision_id(s) for entity.
6. Clear via existing workflow removes from mandatory queue.
7. Extend-halt with required justification persisted (schema expansion allowed in scope above) OR if truly blocked, stop and report — do not fake.
8. Pytest covers scorer combo + API queue.
9. Do not wire `max_profile_build_block_days`.

## Constraints

- Do not mark state done.
- TDD preferred.
- Header: `implementation_model: composer-2.5`
