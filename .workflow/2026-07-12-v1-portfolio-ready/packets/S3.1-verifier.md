# Packet S3.1 — skeptic-verifier

## Claim

S3.1 is done: a full four-scenario eval sweep ran under current config after S1/S2 fixes; metrics and partition checks are evidenced; docs calibration artifacts and scoring_config weights/thresholds were not changed; no CALIBRATED claim.

## Acceptance criteria (task scope only)

1. Full four-scenario eval pipeline completed (not unit-test smoke).
2. Report includes global P/R/F1/tp/fp/fn and per-scenario recall for S1–S4 (esp. scenario_2_slow_roll).
3. Operating threshold documented and matches current YAML `anomaly_threshold`.
4. No weight/threshold changes in `config/scoring_config.yaml`.
5. `docs/calibration_final_metrics.json` unchanged.
6. Commands + raw metric JSON captured.

## Evidence paths

- Implementer result: `.workflow/2026-07-12-v1-portfolio-ready/results/S3.1-implementer-result.md`
- Raw metrics: `scratch/s31_metrics.json`
- Sweep driver: `scratch/run_s31_sweep.py`
- Packet: `.workflow/2026-07-12-v1-portfolio-ready/packets/S3.1-implementer.md`

## Instructions

- Treat implementer claims as unevidenced until independently checked.
- Re-read artifacts; spot-check that metrics JSON matches the report; confirm `anomaly_threshold` in YAML is 45; confirm calibration_final_metrics.json still matches pre-S3.1 saved point (or at least was not overwritten with the new FP-heavy numbers).
- Check whether the sweep used the packet-preferred generator path (`batch.synthetic.generator` / `__main__` injects including `inject_scenario_3_coordinated`) vs a divergent scenario (e.g. `scenario_3_subtle`). If divergent, decide whether that still satisfies “full four-scenario eval under current config” or refute.
- **Ignore** phase/sprint-level gaps (S3.2–S3.6 incomplete, FP operating-point not promoted, SPEC honesty scrub) — those are later packets / exit gate.
- Do not modify files. Verdict: `survives` or `refuted`.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S3.1-verifier-result.md`

Header: `verification_model: cursor-grok-4.5-high`
