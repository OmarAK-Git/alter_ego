# Packet S3.2 — implementer

## Objective

Refresh `docs/calibration_final_metrics.json` and `docs/calibration_pr_curve.json` from the S3.1 sweep evidence (Hybrid C re-sweep). Keep language honest — **do not** claim CALIBRATED.

## Context from S3.1 (verified)

- DB: `alter_ego_calibrate_s31.db`
- At `anomaly_threshold=45`: P≈0.019, R≈0.817, FP=3448, FN=15; S2 recall **1.0**; S3 subtle FN=15
- Raw: `scratch/s31_metrics.json`
- Prior docs artifact still had old point (P=1.0, R≈0.42, S2 R=0.0) — replace with fresh sweep numbers

## Write scope (only)

- `docs/calibration_final_metrics.json`
- `docs/calibration_pr_curve.json`
- `scratch/` if needed for PR-curve generation helper
- `.workflow/2026-07-12-v1-portfolio-ready/results/S3.2-implementer-result.md`

## Do not touch

- `config/scoring_config.yaml` thresholds/weights (S3.6 governance if any change proposed)
- `state.json` — do not mark done
- SPEC/progress narrative (S3.4 / S3.5)

## Method

1. Point `DATABASE_URL` at `sqlite:///alter_ego_calibrate_s31.db` (must already exist from S3.1).
2. Run `batch.eval.calibrate.generate_pr_curves` (or equivalent) → write `docs/calibration_pr_curve.json`.
3. Write `docs/calibration_final_metrics.json` with:
   - `calibrated`: metrics at **current YAML** `anomaly_threshold` (45) — include full scenario breakdown
   - Optional `baseline` if cheap to compute via `run_baseline`
   - Metadata fields allowed: `source` / `sweep` / `config_version` / `generator_seed` / note that this is post-S1/S2 integrity re-sweep (S3.1)
4. Do **not** auto-promote a new threshold into YAML even if PR curve suggests one; note recommended alternate operating points in the implementer result for S3.4/S3.6.

## Acceptance criteria

1. Both docs JSON artifacts refreshed from S3.1 DB (not hand-copied stale narrative).
2. `calibration_final_metrics.json` scenario_2_slow_roll recall matches S3.1 evidence (~1.0).
3. PR curve covers a threshold sweep (same style as prior file: threshold / P / R / F1 / tp / fp / fn).
4. No scoring_config weight/threshold edits.
5. No CALIBRATED claim in the JSON or result markdown.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S3.2-implementer-result.md`  
Header: `implementation_model: composer-2.5`
