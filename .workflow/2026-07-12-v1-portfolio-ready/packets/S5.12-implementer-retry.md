# Packet S5.12 — implementer (retry 1)

## Gap from verifier (refuted)

1. SPEC §14 still claims debate transcripts are **committed** at `docs/architecture-debate-v1.md` / `v2.md` but files do not exist. Soften to honest provenance (produced via debates; transcripts optional / not shipped).
2. Sync root `SPEC.md`.
3. Fix `memory-bank/progress.md` knob inventory if it still says `max_profile_build_block_days` is unread/defer (S5.6 wired it).

## Write scope

Same as S5.12: README.md, docs/, SPEC.md root, memory-bank/progress.md, result file.

Do not mark state done. Header: `implementation_model: composer-2.5`
