# Packet S2.2 — implementer

## Objective

Evidence-binding test: reconstruct `raw_score` from decision contributions within tolerance (SPEC_V3 §7).

## Write scope (only)

- `tests/worker/` (primary: add reconstruction test)
- `worker/scorer.py` only if score aggregation is currently inconsistent with contributions and a minimal fix is required for the test to be honest

## Do not touch

- scoring weights/thresholds (unless a clear bug makes reconstruction impossible — prefer documenting formula used)
- state.json / memory-bank / SPEC narrative
- Do not mark packet done

## Acceptance criteria

1. A test loads or builds a real `DecisionRecord` from `score_event` (not a hand-waved fake score).
2. Reconstructs score from `contributions` using the documented aggregation rule (state the formula in the test docstring / result).
3. Asserts reconstructed value equals decision `score` (or pre-damping `raw_total` if damping applies — be explicit which field/semantics) within a tight tolerance (≤ 1e-6 absolute or justify wider).
4. Covers at least one path with multiple non-zero contributions.
5. Run: `pytest -k "evidence_binding or reconstruct or raw_score" -v --tb=short` (or the exact new test path).

## Notes

- SPEC formula text: `raw_score = Σ (contribution_i × weight_i × confidence_i)` — code may already fold weight into `contribution_score`. Document actual invariant the code guarantees.
- If low-confidence damping changes `score` vs sum of contributions, test both: sum == undamped raw; and/or score == damped result with flag present.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S2.2-implementer-result.md`
