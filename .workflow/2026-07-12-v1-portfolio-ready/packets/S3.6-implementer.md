# Packet S3.6 — implementer

## Objective

Scoring-config governance record for S3. **No threshold/weight was changed** in S3.1–S3.5 (YAML still `anomaly_threshold: 45`, config v2.2). Record that fact explicitly; do **not** apply thr=55. Document standing rule: no silent knob changes without sweep + governance.

## Context

- S3.1/S3.2 re-sweep used existing config 2.2 @ thr=45
- PR curve best-F1 thr=55 was **diagnostic only** (S3.2/S3.4) — not applied
- `worker/config_store.py` already supports hash-chained `save_config` records

## Write scope

- `config/scoring_config.yaml` — **comments/metadata only** (no numeric retunes). Optional note that S3 re-sweep attested v2.2 @ thr=45 unchanged.
- `worker/config_store.py` — only if a small fix is required for governance recording; prefer leave alone if already adequate
- `docs/scoring-config-governance-s3.md` (new lean attestation) **or** append section to `docs/phase2-s3-operating-point.md`
- `OPS.md` — one-liner that S3 left knobs unchanged; thr=55 not applied
- `memory-bank/progress.md` — S3.6 → done; optionally fix stale Phase 1 map lines (geo/containment) noted by S3.5 verifier
- `.workflow/2026-07-12-v1-portfolio-ready/results/S3.6-implementer-result.md`

## Do not touch

- anomaly_threshold, drift_threshold, feature weights (no retune)
- `docs/calibration_*.json`
- `state.json`
- Claiming CALIBRATED

## Acceptance criteria

1. Written governance attestation: S3 used scoring_config v2.2; **no** weight/threshold change; thr=55 considered not applied.
2. Pointers to S3.1 sweep evidence + calibration JSON.
3. Standing rule restated (OPS or docs): no weight/threshold change without recorded sweep + governance.
4. YAML numeric knobs unchanged vs pre-S3.6 (byte-stable on values; comments OK).
5. progress.md S3.6 marked done.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S3.6-implementer-result.md`  
Header: `implementation_model: composer-2.5`
