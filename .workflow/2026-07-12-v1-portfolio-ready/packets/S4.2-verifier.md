# Packet S4.2 — skeptic-verifier

## Claim under test

S4.2 (Explainer queue-depth limit + deterministic template fallback under load) is complete.

## Goal (verbatim)

Explainer queue-depth limit + deterministic template fallback under load

## Acceptance criteria (task scope only)

1. Configurable max in-flight/pending LLM explanation jobs via scoring_config.
2. At/over depth → deterministic template fallback **without** calling LLM provider.
3. Overflow logged for audit; decisions not dropped.
4. Under normal depth, LLM path still runs.
5. Tests prove overflow vs under-capacity behavior.
6. Focused explainer pytest passes.

## Evidence

- Implementer result (unevidenced): `.workflow/2026-07-12-v1-portfolio-ready/results/S4.2-implementer-result.md`
- Files: `worker/explainer.py`, `web/api.py`, `config/scoring_config.yaml`, `tests/worker/test_explainer.py`
- Commands: `pytest tests/worker/test_explainer.py -v --tb=short`; `ruff check worker/explainer.py web/api.py tests/worker/test_explainer.py`
- Confirm anomaly_threshold / feature weights unchanged (only new queue-depth key OK)

## Instructions

- Treat implementer claims as unevidenced until checked.
- Ignore phase/sprint gaps.
- Verdict: `survives` or `refuted`.
- Write `.workflow/2026-07-12-v1-portfolio-ready/results/S4.2-verifier-result.md`
- Header: `verification_model: cursor-grok-4.5-high`
