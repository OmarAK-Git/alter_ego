# Packet S3.4 — implementer

## Objective

S2 now catches (recall 1.0). Document the **operating point** and **remaining error modes**. Do **not** claim CALIBRATED (global precision ~0.019 @ thr=45).

## Evidence sources (read)

- `docs/calibration_final_metrics.json` (S3.2)
- `docs/calibration_pr_curve.json` (best-F1 thr=55 noted but not applied)
- `.workflow/2026-07-12-v1-portfolio-ready/results/S3.1-implementer-result.md`
- `scratch/s32_summary.json` if present

## Write scope (only)

- `memory-bank/progress.md` — update Saved eval point + S3.4 row to done; Phase 2 status honesty
- `docs/` — one short residual/operating-point note (prefer `docs/phase2-s3-operating-point.md` or update an existing phase2 residual section — keep lean)
- `.workflow/2026-07-12-v1-portfolio-ready/results/S3.4-implementer-result.md`

## Do not touch

- `config/scoring_config.yaml` (threshold stays 45 unless S3.6 explicitly changes it)
- `docs/calibration_*.json` (already fresh)
- SPEC.md full scrub (S3.5) — only touch SPEC if a single cross-link is needed; prefer not
- `state.json`

## Required content

1. **Operating point:** thr=45 (YAML current); global P/R/F1/tp/fp/fn; per-scenario recalls.
2. **S2:** caught (1.0) — integrity fixes worked for slow-roll under this harness.
3. **Residuals:** 3448 FP @45; scenario_3_subtle FN=15 (recall 0.667); thr=55 alternate (better F1, lower recall) documented as **not applied**.
4. **Status language:** Phase 2A / closed-with-residual — **not** CALIBRATED.
5. Mark S3.4 done in progress.md table.

## Acceptance criteria

1. progress.md Saved eval point matches new metrics (not stale R≈0.42 / S2 R=0.0).
2. A docs note states operating point + residual modes without CALIBRATED.
3. Alternate thr=55 mentioned as diagnostic only.
4. No YAML edits.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S3.4-implementer-result.md`  
Header: `implementation_model: composer-2.5`
