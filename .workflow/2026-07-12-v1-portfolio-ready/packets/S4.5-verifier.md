# Packet S4.5 — skeptic-verifier

## Claim under test

S4.5 first-class `replay_run_id` on decisions/audit path is complete.

## Acceptance criteria

1. Pydantic `DecisionRecord` has optional/nullable `replay_run_id`.
2. ORM stores nullable `replay_run_id`.
3. `run_replay` sets `replay_run_id` on new decisions (not only flags).
4. Original decisions have null/absent `replay_run_id`.
5. API detail responses include `replay_run_id` when present.
6. Focused tests pass; progress.md updated.

## Evidence

- Implementer: `.workflow/2026-07-12-v1-portfolio-ready/results/S4.5-implementer-result.md`
- Files: `core/schemas/decisions.py`, `core/models.py`, `batch/replay_runner.py`, `web/api.py`, alembic version if any, tests
- Commands: focused pytest for replay + related API tests; ruff on touched files

Ignore phase/sprint gaps.

Write `.workflow/2026-07-12-v1-portfolio-ready/results/S4.5-verifier-result.md`  
Header: `verification_model: cursor-grok-4.5-high`
