# Packet S4.2 — implementer

## Objective

Explainer queue-depth limit + deterministic template fallback under load (SPEC §8.6).

## Context

- SPEC §8.6: configurable queue depth; on overflow, decisions are **not** dropped — they get deterministic template explanation immediately; dropped-explanation event logged for audit.
- Template fallback already exists in `worker/explainer.py` (`generate_template_explanation`).
- API entry: `POST /api/alerts/{decision_id}/explain` in `web/api.py`.
- No `explainer_queue_depth` (or similar) knob exists yet in `config/scoring_config.yaml` — add one (schema addition, **not** a weight/threshold retune; do not bump calibrated claim language).

## Write scope

- `worker/explainer.py`
- `web/api.py`
- `config/scoring_config.yaml` — **add** queue-depth key + comment only; do not change anomaly_threshold / feature weights
- `tests/` (prefer `tests/worker/test_explainer.py` and/or a focused new test module; API tests OK under `tests/`)
- `.workflow/2026-07-12-v1-portfolio-ready/results/S4.2-implementer-result.md`

## Do not touch

- `state.json`
- anomaly_threshold, drift_threshold, feature weights
- S4-EXIT-GATE / phase exit verification
- Files outside write scope

## Acceptance criteria

1. Configurable max concurrent/in-flight (or pending) LLM explanation jobs via scoring_config (document key name + default).
2. When at/over depth, `generate_explanation` (or the API path that calls it) returns deterministic template fallback **without** calling the LLM provider.
3. Overflow is logged for audit (app logger and/or audit-visible note on the ExplanationRecord `validation_notes` / status) — decisions are not dropped.
4. Under normal depth, LLM path still runs (existing behavior preserved).
5. Tests prove overflow → template_fallback without provider.generate being called; under-capacity still can call provider.
6. Focused pytest for explainer (+ any new API tests) passes.

## Constraints

- Do **not** mark the packet done in state.json.
- Do **not** run phase/sprint exit verification.
- Stop and report `approval_gates` before dependency installs, harness config edits, clones, or writes outside scope.
- TDD preferred.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S4.2-implementer-result.md`  
Header: `implementation_model: composer-2.5`
