# Packet S1.5 — implementer

## Objective

Smoke that benign-only / correlated-benign paths still run under fixed eval partitions (after S1.1). No new precision/recall claims.

## Write scope (only)

- `batch/eval/` and/or `batch/synthetic/` and/or `scratch/` as needed for a small smoke
- `tests/` preferred for durable smoke

## Do not touch

- state.json; scoring weights; do not refresh calibration_final_metrics.json with new claims

## Acceptance criteria

1. A smoke test or scripted path generates baseline (benign) events with `production` partition and runs without crash.
2. Optionally correlated-benign / rollout path still tagged production and excluded from eval attack partitions.
3. Document commands run; pytest preferred.
4. Explicitly state: no new P/R claims.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S1.5-implementer-result.md`
