# Packet S0.3 — implementer

## Objective

Lock embedding narrative to runtime truth: deterministic char 3-gram hashing, model id `alter-ego-ngram-v1`, **128-d**. Note `nomic-embed-text` as abandoned/deferred schema default debt — do not claim nomic is shipping.

## Write scope (only)

- `docs/SPEC_V3.md`
- `README.md`
- `CLAUDE.md`
- `OPS.md`
- optionally `memory-bank/progress.md` S0.3 Notes only

## Do not touch

- `state.json`, alembic migrations (S1.4 owns code defaults), `core/models.py`, scoring weights
- Do not mark packet done

## Acceptance criteria

1. Narrative claims in write-scope files that describe the *shipping* embedding say ngram / `alter-ego-ngram-v1` / 128-d (not nomic as current runtime).
2. Any nomic mention is framed as deferred/abandoned or historical schema default, not as what scores today.
3. README/CLAUDE/OPS stay consistent with `worker/vectorizer.py` behavior.
4. Do not change code or DB defaults (that's S1.4).

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S0.3-implementer-result.md`
