# Packet S5.3 — researcher (bounded)

## Question

How should v1 enforce Postgres INSERT-only roles for audit (+ profile commit) tables given profile promotion currently UPDATEs rows?

## Investigate

- `core/models.py` tables: `audit_logs`, `profiles`, related
- `worker/profile_store.py` (or equivalent) promote/supersede UPDATE paths
- SPEC §9.2 + SPEC_V3 committed-profile INSERT-only claims
- Existing alembic revisions; whether any roles already exist
- Feasible within write scope `alembic/`, `docs/`, `docker-compose.yml` only (no Python app changes unless you recommend expanding scope)

## Decide

- **A ship:** Alembic + compose app role with INSERT-only on `audit_logs` (and profiles if possible without breaking promotion — e.g. column-level UPDATE on `superseded_at` only, or separate migrator/superuser for promotion)
- **B partial:** INSERT-only on audit tables only; honest SPEC downgrade for full profile-table INSERT-only until promotion is redesignable
- **C downgrade:** Doc-only Path B if DB roles cannot ship honestly in this scope

## Deliverable

`.workflow/2026-07-12-v1-portfolio-ready/results/S5.3-researcher-result.md` with decision, evidence, implementer write scope + ACs.

Readonly; do not implement.
