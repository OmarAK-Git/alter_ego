# Packet S2.8 — implementer

## Objective

Apply S0.4 inventory: wire remaining **cheap** knobs or strip orphans from calibrated claims; refresh inventory honesty after S1/S2 work.

## Write scope (only)

- `config/scoring_config.yaml` (comments / remove unused delete-candidates if safe; no weight/threshold retunes without note)
- `docs/SPEC.md` / `OPS.md` / `memory-bank/progress.md` (inventory refresh)
- `tests/` only if needed for knob-read assertions
- Code wiring only for **cheap** unread knobs that are safe (e.g. delete `decay_lambdas` if truly unread, or comment-strip; wire `min_clean_observation_count` only if a real reader exists)

## Do not touch

- anomaly_threshold, drift_threshold, feature weights (no calibration retune)
- state.json
- Do not mark packet done in state.json

## Acceptance criteria

1. Refresh `memory-bank/progress.md` scoring-config inventory to reflect post-S1/S2 reality (`confidence_k` wired, `containment_threshold` wired if S1.3 done, geo if S1.2 done, `total_volume_delta` defer per S2.6).
2. Remaining **delete** candidates (`decay_lambdas.*`) removed from YAML **or** explicitly kept with "unused / delete-candidate" comment and inventory status `delete`→`deferred-orphan` with OPS note — no silent unused knobs presented as calibrated.
3. Remaining **wire** items either wired cheaply with a test, or reclassified to defer with owning packet.
4. OPS.md one-liner updated to current inventory truth.
5. No fake "calibrated" language for unwired knobs.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S2.8-implementer-result.md` — before/after inventory counts.
