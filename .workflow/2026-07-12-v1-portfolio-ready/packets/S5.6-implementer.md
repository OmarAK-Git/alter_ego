# Packet S5.6 — implementer

## Objective

Wire `max_profile_build_block_days` supervisor escalation (SPEC §5.5 / V3 §9). Independent of S5.5 staleness escalation.

## Context

- YAML key `max_profile_build_block_days` exists (default 30) but is **unread**.
- Active-alert build blocking already shadows profiles in `batch/profile_builder/builder.py`.
- When an entity remains build-blocked longer than the threshold without alert clear, emit supervisor escalation (auditable signal / structured result — not silent).

## Write scope

- `batch/profile_builder/builder.py`
- `config/scoring_config.yaml` (wire existing key only — **do not change weight/threshold values** without governance; reading the key is OK)
- `tests/`
- Result: `results/S5.6-implementer-result.md`

## ACs

1. Builder reads `max_profile_build_block_days` from scoring config.
2. Entities blocked by active alert beyond threshold produce a supervisor-escalation signal (flag, log, returned list, or audit — document choice; must be testable).
3. Entities under threshold do not escalate.
4. Tests cover over-threshold vs under-threshold.
5. Do not change numeric default without governance note; prefer wire-as-is.
6. `pytest` focused tests pass.

Do not mark state done. Header: `implementation_model: composer-2.5`
