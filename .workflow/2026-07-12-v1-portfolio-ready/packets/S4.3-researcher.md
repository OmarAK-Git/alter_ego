# Packet S4.3 — researcher (fork)

## Question

For S4.3 (Suppressed-decisions aging/jitter **or** defer note): recommend **ship** aging escalation + jitter, or **defer** with an explicit note?

## Constraints from plan/SPEC

- SPEC §11.4 requires: aging indicators, auto-escalation after `suppressed_decision_age`, ±`age_jitter_hours` jitter.
- SPEC §13.1 cut line: "Suppressed-decisions view (without escalation if necessary; the view itself is non-negotiable)".
- Packet objective: "Suppressed-decisions aging/jitter OR defer note (**view itself required**)".
- Knobs: `age_jitter_hours` currently deferred/unwired; check if `suppressed_decision_age` exists in YAML.
- Prefer cheap ship if aging indicators + escalation are small; otherwise defer escalation/jitter but document that the view exists.

## Investigate

- `web/api.py` `/api/suppressed`
- `web/static/` suppressed UI
- `config/scoring_config.yaml` related keys
- Whether confidence_floor is used for suppression vs hardcoded 0.5
- Prior S2.8 inventory notes on `age_jitter_hours`

## Deliverable

Write `.workflow/2026-07-12-v1-portfolio-ready/results/S4.3-researcher-result.md` with:
1. Decision: **A ship** or **B defer**
2. Evidence (file:line)
3. Exact implementer write scope for the chosen path
4. Acceptance criteria for implementer

Header: `research_model: composer-2.5` (or whatever model you run as)
