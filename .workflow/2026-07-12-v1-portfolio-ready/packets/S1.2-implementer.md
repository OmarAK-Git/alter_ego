# Packet S1.2 — implementer

## Objective

Build `geolocations` into profile histograms and include geolocation in drift KL (so `drift_weights.geolocation` and `geolocation_rarity` have real distributions).

## Write scope (only)

- `batch/profile_builder/builder.py`
- `worker/scorer.py` (only if needed for geo rarity/drift path)
- `tests/`

## Do not touch

- scoring weight *values* in YAML (may read them)
- state.json; no commit

## Acceptance criteria

1. Profile build populates a geolocation histogram (or equivalent) from auth event geolocation fields.
2. Drift KL / cumulative drift includes geolocation as a feature dimension using existing `drift_weights.geolocation` when present.
3. Tests cover: geo present in profile artifact; drift path includes geo when distributions differ.
4. Run focused pytest with evidence.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S1.2-implementer-result.md`
