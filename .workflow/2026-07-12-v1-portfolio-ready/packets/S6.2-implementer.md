# Packet S6.2 — implementer

## Objective

Write a residual-risk + open drift hypotheses doc that feeds human drift-methodology research (SPEC §6.4 / S6 handoff).

## Context

- Operating-point residuals: `docs/phase2-s3-operating-point.md` (3448 FP @ thr=45; scenario_3 FN=15; thr=55 not applied).
- HUMAN-DRIFT-RESEARCH acknowledged; personal research proceeds in parallel.
- S5-EXIT-GATE residual to fold in: `DEFAULT_EMBEDDING_INPUT_NORMALIZER_VERSION="1.0"` in `core/` mismatches runtime `1.0-char-3gram-hash-128` — latent fail-closed trap (production safe via builder; tests fixed).
- Drift engine: `batch/profile_builder/builder.py` (KL recent vs baseline, cohort-median normalize, 7-day half-life); `drift_threshold` 5.0.
- Path B deferrals: cohort prior-update gates (S5.11), lifecycle (S2.7), calendar dual-score (S4.6).
- Do not invent new CALIBRATED claims.

## Write scope

- `docs/` (new doc preferred: `docs/residual-risk-drift-hypotheses.md`)
- `memory-bank/progress.md` (S6.2 row note OK)
- Result: `.workflow/2026-07-12-v1-portfolio-ready/results/S6.2-implementer-result.md`

## ACs

1. Doc lists concrete residual risks with evidence pointers (metrics JSON / operating-point / S5 residual).
2. Doc lists open drift hypotheses suitable for human research (not closed as solved).
3. Includes the stale `core/` embedding normalizer default as an engineering residual / fail-closed trap.
4. Honest: not CALIBRATED; no weight/threshold recommendations that skip governance.
5. Do not mark `state.json` done.

## Do Not

- Edit `OPS.md` (S6.3 owns standing rule polish; S6.1 already pointed checklist)
- Change `config/scoring_config.yaml`
- Edit product code (doc-only packet)

Header: `implementation_model: composer-2.5`
