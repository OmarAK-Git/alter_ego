# Packet S1.4 — implementer

## Objective

Align ProfileArtifact / DB defaults / builder metadata to shipping embedding: `alter-ego-ngram-v1`, dimensionality **128**. Replace `nomic-embed-text` defaults where they claim current runtime.

## Write scope (only)

- `core/schemas/profiles.py`
- `core/models.py`
- `batch/profile_builder/builder.py`
- `alembic/` (new revision preferred over rewriting old migrations if safer)
- `tests/` as needed

## Do not touch

- state.json; scoring weights; vectorizer algorithm itself (already correct)
- Do not force destructive DB migration against prod data without documenting rollback in result

## Acceptance criteria

1. New profiles / schema defaults use ngram model id and dim 128 (not nomic as default).
2. Builder metadata written on promote matches.
3. Tests or existing tests updated; pytest evidence.
4. If alembic: additive revision updating server defaults; note in result.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S1.4-implementer-result.md`
