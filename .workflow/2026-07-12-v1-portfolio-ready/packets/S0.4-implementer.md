# Packet S0.4 — implementer

## Objective

Inventory every knob in `config/scoring_config.yaml` into **wire / defer / delete** (or “already wired”) lists. Write the inventory into `memory-bank/progress.md` and/or `OPS.md`. Do not change weights/thresholds.

## Write scope (only)

- `config/scoring_config.yaml` — READ mostly; only add comments if clearly helpful and non-behavioral (prefer zero YAML edits)
- `memory-bank/progress.md`
- `OPS.md`

## Do not touch

- Actual weight/threshold values (no calibration)
- Code wiring (that is later packets)
- `state.json`

## Acceptance criteria

1. Every top-level / nested tunable in scoring_config.yaml appears in an inventory with one of: `wired`, `wire`, `defer`, `delete` (or equivalent clear labels).
2. Inventory lives in progress.md and/or OPS.md with date 2026-07-12.
3. No numeric scoring parameter values changed.
4. Brief note which deferred items map to later packets (S1.3 containment_threshold, etc.) when known.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S0.4-implementer-result.md`
