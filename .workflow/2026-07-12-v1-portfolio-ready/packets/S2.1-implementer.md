# Packet S2.1 — implementer

## Objective

Resolver collision/split fixtures emit `low_resolution_confidence` (SPEC_V3 §7 / SPEC decision flags).

## Write scope (only)

- `worker/resolver.py`
- `worker/scorer.py` (only if needed to surface the `low_resolution_confidence` decision flag from low `resolution_confidence`)
- `tests/worker/test_resolver.py`
- `tests/worker/` (add focused tests as needed)

## Do not touch

- scoring weights/thresholds in `config/scoring_config.yaml`
- SPEC narrative packets, state.json, memory-bank
- Do not mark packet done in state.json

## Acceptance criteria

1. A **collision** fixture exists: two distinct raw entity refs that collide on the same canonical identity (or equivalent conflict) produce `resolution_confidence` below a clear low threshold (document the threshold in the result).
2. A **split** fixture exists: one raw entity ref with ambiguous/conflicting resolution produces low `resolution_confidence`.
3. Decision path (resolver output and/or scorer flags) emits the flag string `low_resolution_confidence` when resolution confidence is low — matching SPEC decision `flags` vocabulary.
4. Happy-path `user_*` / `svc_*` resolutions remain high confidence and do **not** get the flag.
5. Automated tests fail-before/pass-after prove collision + split cases; run:
   `pytest tests/worker/test_resolver.py -v --tb=short` (plus any new focused file you add).

## TDD

Write failing tests first, then minimal resolver (and scorer flag wiring if needed).

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S2.1-implementer-result.md`
