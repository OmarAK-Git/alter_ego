# Packet S5.3 — implementer

## Objective

Postgres INSERT-only app role + REVOKE for audit/profile commit tables — Path **A** per researcher.

## Decision source

`.workflow/2026-07-12-v1-portfolio-ready/results/S5.3-researcher-result.md`

## Write scope (`files_allowed`)

- `alembic/` (new revision under `alembic/versions/`)
- `docker-compose.yml`
- `docs/SPEC.md`, `docs/SPEC_V3.md`, `docs/deployment.md`
- `SPEC.md` (root — sync from `docs/SPEC.md`)
- `memory-bank/progress.md` (S5.3 row / honesty notes)
- `.workflow/2026-07-12-v1-portfolio-ready/results/S5.3-implementer-result.md`

## Do not touch

- `state.json`
- Application Python (`worker/`, `core/models.py` behavior) unless absolutely required — prefer SQL grants only
- `config/scoring_config.yaml`
- Live `alembic upgrade` / `docker compose up` against shared/prod (approval gate — skip; ship SQL + docs + compose wiring)

## Acceptance criteria

1. Alembic migration creates role `alter_ego_app` (or documented name) with:
   - INSERT + SELECT (no UPDATE/DELETE) on `audit_logs`, and preferably also append-only tables `decisions`, `explanations`, `scoring_configs` as researcher specified
   - On `profiles`: INSERT + SELECT + column-level UPDATE only on lifecycle columns (`promoted_at`, `superseded_at`); no UPDATE on payload columns
   - Adequate grants for operational tables the app must write (`events`, `resolved_events`, `alert_workflow_state`, `containment_queue`, sequences)
2. `docker-compose.yml`: migrations run as owner/superuser; runtime `web`/`worker`/`batch` connect as app role (password via compose env / documented local-dev secret — match existing local-dev honesty).
3. Docs: SPEC_V3 §6.3 amended for lifecycle-column exception on profiles; SPEC §9.2 mechanism described as shipped at DB-role layer; `docs/deployment.md` has role matrix + how to verify INSERT-only.
4. Root `SPEC.md` synced if `docs/SPEC.md` changed.
5. No false claim of full table-level INSERT-only on `profiles`.
6. Do not require live Postgres in this environment; migration must be valid SQLAlchemy/Alembic revision chaining from current head.

## Constraints

- Do not mark state `done`.
- Stop for approval_gates before pip installs, harness edits, clones, writes outside scope, or applying migrations to non-local shared DBs.

## Report

`.workflow/2026-07-12-v1-portfolio-ready/results/S5.3-implementer-result.md`  
Header: `implementation_model: composer-2.5`
