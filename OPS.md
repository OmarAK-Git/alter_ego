# Operational Guide

Generated by the ultimate-agentic-workflow skill. Keep this file under 60 lines; status and progress belong in the active plan or `.workflow/`.

## Build And Run

- Install: `pip install -e ".[dev]"`
- Run API: `uvicorn web.api:app --reload`
- DB: `docker compose up -d` (Postgres + pgvector) or sqlite via `DATABASE_URL`

## Validation

- Test: `pytest -v --tb=short`
- Lint: `ruff check .`
- Typecheck: `mypy .`
- Migrate: `alembic upgrade head`

## Operational Notes

- Prefer `config/scoring_config.yaml` + `docs/calibration_final_metrics.json` over README/CLAUDE narrative for thresholds and scenario recall claims.
- Embeddings are deterministic char 3-gram SHA-256 hashes → **128-d** unit-norm vectors (`alter-ego-ngram-v1`, `worker/vectorizer.py`), not 768-d BERT / nomic. Schema/ORM defaults match; older Alembic revisions historically mentioned `nomic-embed-text` (aligned in `e4f5a6b7c8d9`). `drift_threshold` is **5.0** in scoring_config (not 1.0).
- Schema/ORM `embedding_input_normalizer_version` defaults to `worker.vectorizer.NORMALIZER_VERSION` (`1.0-char-3gram-hash-128`). Explicit mismatches still fail-closed under S5.9.
- Cohort priors are profile-embedded (`features["cohort_data"]`) only in v1; independent versioned cohort artifacts and advanced §7.3 prior-update gates deferred (S2.5 / S5.11 Path B); scoring-time novelty suppression ships.
- Profile lifecycle states (dormant/reactivated/onboarding/role_transition) deferred in v1 (S2.7); alert workflow badges ≠ profile lifecycle.
- Calendar dual-score and telemetry-gap two-tier deferred in v1 (S4.6); single `score` field; staleness halt only for profile freshness.
- Do not commit local `alter_ego_calibrate_v*.db`, `alter_ego_eval.db`, or `__pycache__` experiment churn.
- **Standing rule:** no weight/threshold edits in `config/scoring_config.yaml` without (1) full sweep per `docs/hardening-sweep-checklist.md` and (2) governance record per `docs/scoring-config-governance-s3.md` or `ConfigStore.save_config(author, change_reason)`.
- **Headline recall rule (follow-on, BTF1.1):** any scenario recall claim requires attribution decomposition causally traceable to the inject — see Design 1 spec + `memory-bank/progress.md` Design 2 follow-on. S2 R=1.0 Series A is retracted (`residual-risk-drift-hypotheses.md` §2.6).
- **S3 attestation:** knobs unchanged — `scoring_config` v2.2 @ `anomaly_threshold=45` through S3.1 re-sweep; PR-curve thr=55 diagnostic **not applied** (see `docs/scoring-config-governance-s3.md`).
- **§5.5 / Series D (2026-07-19):** D4 time-axis fix shipped; Series D baseline in `docs/calibration_series_d_metrics.json` + `docs/scoring-config-governance-series-d.md`. D4 engagement 12840 (vs Series C 0 — only permitted C→D claim). S2 R=0.714; S2 stays blocked with shadow drift visible. **Not CALIBRATED.** C→D FP/P/R comparisons prohibited. Attestation YAML hygiene deferred (separate S6.3; zero behavioral diff).

## Dependency And Repo Setup

- `.codex` / `.claude` setup and dependent `git clone` work require explicit user approval before execution.
- Approval requests must include exact commands, write targets, network/data risks, and rollback.
- Prefer project-local setup over global changes unless the user asks for global config.

## Codebase Patterns

- Source: `core/`, `worker/`, `batch/`, `web/`
- Tests: `tests/`
- Scoring knobs: `config/scoring_config.yaml` v2.2; inventory in `memory-bank/progress.md` §Scoring config knob inventory (2026-07-13): **28 wired / 0 wire / 7 defer / 0 delete** (S2.8). Orphan `decay_lambdas` stripped. Unwired defer: `min_clean_observation_count`, `total_volume_delta`, calendar/gap/build-block/aging keys.
- Spec: `docs/SPEC.md` (authoritative); keep root `SPEC.md` byte-identical — sync via full UTF-8 copy after doc edits (S2.7/S2.8 pattern)
- Live agent memory: `memory-bank/` (activeContext, tasks, progress)
- T3 runs: `.workflow/<slug>/` — validate with `ultimate-agentic-workflow/scripts/verify_run.py`
- Closed T3: `.workflow/2026-07-12-v1-portfolio-ready/` (S0–S6 drained; S6-EXIT-GATE passed). New T3 work starts a new `.workflow/<slug>/`; update `state.json` before `memory-bank/`.
- Autopilot: UAW T3 packet loop (researcher / implementer / test-runner / skeptic-verifier). This repo’s queue = `state.json` packets (no `autopilot-queue.json`). Drain sequential when write scopes overlap. Do not invent EXIT-GATE packets missing from `state.json`.
- Models (mid-loop Task dispatch): prefer implement `composer-2.5`, verify `cursor-grok-4.5-high` when using UAW autopilot fields in `state.json`. Sprint/chat gates: pick a strong model in the UI.
- Phase EXIT-GATE / `chat_gate`: always run full `pytest -v --tb=short --ignore=tests/live` + `ruff check .` — packet-scoped verifiers miss cross-packet regressions (S5-EXIT-GATE caught S5.9×S2.4 normalizer fixture halt).
- Stop gate: `.claude/stop-gate.json` (pytest + ruff). Optional `learn_gate` nudges `/retro` once per session — not wired by default.
